# Accuracy audit — application-security-baseline

Chapter: `content/part-2/14-application-security-baseline.mdx`
Scope: SQL/command/path injection, SSRF, XSS (html/template auto-escaping vs text/template), TLS, security headers, secrets-out-of-logs, govulncheck; Go-version attributions. REPORT ONLY — no content file edited.

## Flagged / touched rows

| Claim (as written) | Verdict | Source (quoted) | Proposed action (NOT applied) |
|---|---|---|---|
| "`crypto/tls` in Go 1.24 defaults to TLS 1.2 minimum with a safe cipher suite list — you should not override `MinVersion` downward." (line 684) | **OUTDATED** (wrong version pin) | pkg.go.dev/crypto/tls: "By default, TLS 1.2 is currently used as the minimum." go.dev/doc/go1.18: "If Config.MinVersion is not set, it now defaults to TLS 1.2 for client connections." The TLS 1.2 client default landed in **Go 1.18**, not 1.24. | Fix the version pin: attribute the TLS-1.2-minimum default to **Go 1.18** (client side; server side reached parity by Go 1.19). The "should not override MinVersion downward" guidance is correct and stays. |
| SQL injection: parameterized query sends template and args as "separate wire messages"; placeholder `$1`/`?` is filled by the driver as a typed value, never SQL (lines 177, 193–202, 1332) | CORRECT | This is the database wire-protocol behavior (prepared statements / parameter binding). Conceptually accurate; matches the SQL-injection canon. (No primary fetch needed — mechanism is the bind-parameter contract, not a Go-version claim.) | Leave. |
| Command injection: `exec.Command("/bin/sh","-c",userInput)` is the footgun; `exec.Command(binary, arg1, arg2)` passes args straight to `execve` with no shell; add `"--"` before user filenames (lines 212, 220–234, 1333) | CORRECT | os/exec.Command runs the named program directly (no shell) when given separate args; only an explicit shell invocation tokenizes. Standard, correct. The GoPlayground example (`exec.Command("echo", …)`) correctly demonstrates no shell tokenization. | Leave. |
| Path traversal: `filepath.Join`+`filepath.Clean`+`strings.HasPrefix(clean, root+sep)`; Clean reduction rules (collapse slashes, drop `.`, resolve inner `..`, drop leading `..` on rooted path); trailing separator needed so `/data/receipts` doesn't match `/data/receipts-other/` (lines 274, 309, 354–365, 1334) | CORRECT | path/filepath.Clean documented reduction rules match the four listed; `Join("/data/receipts","../../etc/passwd")` cleans to `/etc/passwd`; the sibling-prefix reasoning is sound. (Mechanism + stdlib behavior; verified by the worked example.) | Leave. |
| "As of Go 1.24, `filepath.Clean` handles platform-specific separators correctly on Windows (backslash) and Unix (slash)." (line 365) | IMPRECISE (misleading version pin) | `filepath.Clean`'s platform-separator handling is **not** a Go-1.24 feature — it has been platform-correct since early Go (the `path/filepath` package is OS-aware by design via `os.PathSeparator`). | Soften: drop "As of Go 1.24" — it implies a recent change that did not occur. Reword to "`filepath.Clean` handles platform-specific separators (backslash on Windows, slash on Unix); on Windows normalize with `filepath.ToSlash` before the prefix check." Low severity, but the pin is unwarranted. |
| SSRF: attacker-controlled webhook → `http.Get` hits `169.254.169.254` (AWS instance-metadata, returns IAM creds to any process); defense = custom `net.Dialer.Control` validating the **resolved** IP before dial; block loopback/RFC-1918/link-local; re-check on redirects; allow-list beats block-list (lines 384–495) | CORRECT | The `net.Dialer.Control` callback fires after name resolution and before connect, so it sees the resolved IP (defeats DNS-rebinding); `169.254.0.0/16` is link-local (IMDS). CIDR list (127/8, 10/8, 172.16/12, 192.168/16, 169.254/16, ::1/128, fc00::/7, fe80::/10) is correct. Standard SSRF-defense mechanism; internally consistent. | Leave. (Minor real-world note, not a defect: `Control` sees the address `net` resolved for *this* dial, but a belt-and-suspenders implementation also pins the dialed IP to the checked one; the chapter's redirect caveat already covers the main gap.) |
| XSS: `html/template` is context-aware and auto-escapes per context; `text/template` outputs verbatim (XSS); import path is the only change; `template.HTML(userInput)` is an explicit bypass that re-introduces XSS (lines 516, 524–528, 607–616, 1336) | CORRECT | This is the documented `html/template` contract — it is a context-aware auto-escaping wrapper over `text/template`; `template.HTML` is a declared "known safe" type that bypasses escaping. Matches stdlib docs and the worked example (`<script>` → `&lt;script&gt;`). | Leave. |
| html/template state machine: states `stateText`/`stateAttr`/`stateURL`/`stateJS`/`stateCSS`…; escapers (`stateURL` blocks `javascript:`/`data:`; `stateJS` JSON-encodes; `stateCSS` hex-encodes); "As of Go 1.24, the state machine lives in `html/template/context.go` and `escape.go`." (lines 633–645) | UNVERIFIABLE (internals + version pin) | The state-machine description matches `html/template`'s design at a high level, but the exact state names, the per-state escaper list, and the "As of Go 1.24 … context.go/escape.go" file attribution were not confirmed against a fetched source (html/template source not opened). | Downgrade the confident specifics: keep the conceptual "context-aware escaping selects the right escaper per context (HTML body, attribute, URL, JS, CSS)," but drop or hedge the exact unexported state-constant names and the "As of Go 1.24 … context.go/escape.go" pin (those files have existed far longer than 1.24 and the names are implementation details). Recommend verifying against the `html/template` source before asserting. |
| Security headers: HSTS `max-age=63072000; includeSubDomains` (2 years), CSP `default-src 'self'`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`; HSTS is a one-way ratchet (lines 699–725, 1337) | CORRECT | 63072000 s = 730 days ≈ 2 years (verified). Each header's effect (HSTS force-HTTPS, CSP origin restriction, nosniff anti-MIME-sniff, DENY anti-clickjacking, Referrer-Policy leakage control) is per the web platform specs. HSTS ratchet caveat is accurate. | Leave. |
| Secrets: `Secret` type with `String()` and `LogValue() slog.Value` returning `[REDACTED]`; slog calls `LogValue()` if the type implements `slog.LogValuer`; prefer structured fields over format strings (lines 803–823, 850–851, 1338) | CORRECT | `slog.LogValuer`/`LogValue()` is the documented slog hook (resolved before the handler formats); `fmt.Stringer` covers fmt verbs. Mechanism correct; the test in Exercise 5 demonstrates it. | Leave. |
| govulncheck: official Go security-team tool, backed by `vuln.go.dev`; call-graph analysis reports only **reachable** (transitively-called) vulnerabilities; install `golang.org/x/vuln/cmd/govulncheck@latest`; exits non-zero on a called vuln → CI gate; pair with `go mod verify` (lines 880–929, 1339) | CORRECT | go.dev/security/vuln: "govulncheck analyzes your codebase and only surfaces vulnerabilities that actually affect you, based on which functions in your code are transitively calling vulnerable functions"; "The Go vulnerability database (vuln.go.dev)"; install path `go install golang.org/x/vuln/cmd/govulncheck@latest`. | Leave. |
| Allow-list > deny-list; strict JSON decode (`http.MaxBytesReader` + `dec.DisallowUnknownFields()`); validate/normalize at the boundary (lines 73–168, 1331) | CORRECT | `http.MaxBytesReader` caps body reads; `(*json.Decoder).DisallowUnknownFields` makes unknown keys a decode error — both documented stdlib behaviors. Conceptually sound. | Leave. |

## CORRECT (verified, left silently): summary count

Verified-correct, load-bearing claims: **11+** (parameterized queries, exec.Command no-shell, filepath Clean+containment, SSRF resolved-IP dialer + CIDR list, html/template vs text/template + template.HTML bypass, HSTS 2-year math + header effects, slog.LogValuer redaction, govulncheck reachability + install + DB, strict-decode controls, allow-list rationale).

## Sources fetched (URLs actually opened)

- https://pkg.go.dev/crypto/tls
- https://go.dev/doc/go1.18
- https://go.dev/security/vuln/

## Tally

- WRONG / OUTDATED: 1 — "crypto/tls in **Go 1.24** defaults to TLS 1.2 minimum." The TLS-1.2-minimum default shipped in **Go 1.18** (client), not 1.24.
- IMPRECISE: 2 — (a) "As of Go 1.24, `filepath.Clean` handles platform separators" (no such 1.24 change; it has always been OS-aware); (b) html/template state-machine "As of Go 1.24 … context.go/escape.go" file/version pin.
- UNVERIFIABLE (downgrade recommended): 1 — the exact html/template internal state-constant names and per-state escaper list (source not fetched).
- CORRECT (confirmed): 11+ load-bearing claims

**Worst finding:** OUTDATED version attribution — line 684 pins the `crypto/tls` TLS-1.2 minimum default to "Go 1.24," but it has been the default since **Go 1.18** (client connections; server parity by 1.19), per go.dev/doc/go1.18: "If Config.MinVersion is not set, it now defaults to TLS 1.2 for client connections." A reader is handed a wrong mental model of when this protection arrived. Fix: re-pin to Go 1.18. (Secondary: two unwarranted "As of Go 1.24" pins on `filepath.Clean` and the html/template internals that imply recent changes that did not happen — recommend removing the pins.)

Frontmatter unchanged: yes (report-only; no content file touched).
