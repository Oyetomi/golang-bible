# Accuracy audit — context-in-production

Chapter: `content/part-2/03-context-in-production.mdx` (Tier 2 — correctness-critical; load-bearing claims audited)
Mode: REPORT ONLY — no edits applied to the chapter.
Focus: cancellation / deadline / value semantics, WithoutCancel/AfterFunc/WithDeadlineCause (Go 1.21), "don't store context in structs", Canceled vs DeadlineExceeded, context.Cause, NotifyContext signal-cause (Go 1.26). All version attributions verified against fetched primary sources.

## Audit log (flagged rows only — silently-confirmed CORRECT claims tallied below)

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | (L769 UnderTheHood) "As of **Go 1.21**, `context.WithValue` panics at construction time if the key is nil or not comparable — an early catch instead of a mysterious nil at lookup time." | **WRONG (version + "new in 1.21" framing)** | pkg.go.dev/context: "The provided key must be comparable and should not be of type string …" — no documented panic behavior on this page. The nil/not-comparable panic in `WithValue` has existed since the function's introduction (Go 1.7), not added in 1.21. go.dev/doc/go1.21 context section lists only WithoutCancel, AfterFunc, WithDeadlineCause/WithTimeoutCause — **no WithValue change**. | The panic on a nil or non-comparable key is original `WithValue` behavior (`if key == nil { panic("nil key") }; if !reflectlite.TypeOf(key).Comparable() { panic(...) }`), not a Go 1.21 addition. Remove the "As of Go 1.21" attribution. Restate: "`context.WithValue` panics at construction if the key is nil or not comparable — original behavior, an early catch rather than a nil at lookup." |
| 2 | (L1379 UnderTheHood) `context.WithoutCancel(parent)` "returns a child that **copies the parent's values and deadline** for `Value` lookups but is never cancelled." | **WRONG (deadline claim)** | pkg.go.dev/context: "WithoutCancel returns a derived context that points to the parent context and is not canceled when parent is canceled. **The returned context returns no Deadline or Err**, and its Done channel is nil." | WithoutCancel does **not** copy the parent's deadline — `Deadline()` returns no deadline (zero, false) and `Done()` is nil. It preserves only `Value` lookups. Fix: "returns a child that preserves the parent's values for `Value` lookups but reports **no deadline**, a nil `Done()`, and is never cancelled." (The chapter's Exercise-5 prose at L1801 correctly says WithoutCancel "preserves the parent's `Value` chain while severing the cancellation signal" — the UnderTheHood "and deadline" is the defect.) |
| 3 | (L1374–1375 UnderTheHood) Cancel-tree locking: "In Go 1.7–1.21, step 2 held a single global mutex (`cancelCtxKey` mutex) …"; "**Go 1.22** replaced the global lock with per-node fine-grained locking. Each `cancelCtx` now has its own `sync.Mutex`." | **UNVERIFIABLE (version attribution unsupported)** | go.dev/doc/go1.22 not fetched as authoritative for a context-locking change; the 1.22 release notes do **not** list a context cancel-tree relock as a headline. `cancelCtx` has carried its own `sync.Mutex` (per-node) since well before 1.22 (it is in the original cancelCtx design); there was never a single global `cancelCtxKey` *mutex* governing tree traversal (`cancelCtxKey` is a *value key*, not a lock). | Downgrade / flag for human verification. The named mechanism ("global `cancelCtxKey` mutex" → "per-node `sync.Mutex` in Go 1.22") does not match the stdlib: `cancelCtx` always had a per-instance `mu sync.Mutex`, and `cancelCtxKey` is the sentinel key used by `parentCancelCtx`, not a global lock. Recommend removing the specific "Go 1.7–1.21 global mutex / Go 1.22 per-node" narrative or replacing it with a sourced statement. **This is the chapter's most confidently-wrong internals claim.** |
| 4 | (L1379, L1381, L1728, L1801 Recap L1910) `context.WithoutCancel` and `context.AfterFunc` "(added Go 1.21)"; AfterFunc "registers `f` to run in a new goroutine when `ctx` is done. Returns a stop function." | **CORRECT** | go.dev/doc/go1.21: "The new `WithoutCancel` function returns a copy of a context that is not canceled when the original context is canceled." / "The new `AfterFunc` function registers a function to run after a context has been cancelled." pkg.go.dev/context: `func AfterFunc(ctx Context, f func()) (stop func() bool)` added go1.21.0; `func WithoutCancel(parent Context) Context` added go1.21.0. | None. Version (1.21) and AfterFunc behavior/stop-func confirmed. (Only the WithoutCancel *deadline* gloss in #2 is wrong.) |
| 5 | (L1383–1391, Recap L1910) "**os/signal.NotifyContext cause (Go 1.26):** … `context.Cause(ctx)` returns an error describing the signal — for example `SIGTERM` or `SIGINT`." | **CORRECT** | go.dev/doc/go1.26: "`NotifyContext` now cancels the returned context with `context.CancelCauseFunc` and an error indicating which signal was received." pkg.go.dev/context (Cause): "If that cancellation happened via a call to CancelCauseFunc(err), then Cause returns err." | None. Version (1.26) and the `context.Cause` → signal-error behavior confirmed. |
| 6 | (L1414–1418 QuickCheck) "`context.DeadlineExceeded` is returned when the automatic timer fires … `context.Canceled` is returned when `cancel()` was called explicitly or when a parent context was cancelled." Answer index 1. | **CORRECT** | pkg.go.dev/context: "Canceled is the error returned by Context.Err when the context is canceled for some reason other than its deadline passing." / "DeadlineExceeded is the error returned by Context.Err when the context is canceled due to its deadline passing." | None. |
| 7 | (L466–468, L510–518 QuickCheck) "A child context always inherits the *sooner* of its own deadline and the parent's deadline." A 500ms child on a 100ms parent fires at 100ms. Answer index 1. | **CORRECT** | pkg.go.dev/context (WithDeadline): "WithDeadline returns a derived context … with the deadline adjusted to be no later than d. **If the parent's deadline is already earlier than d, WithDeadline(parent, d) is semantically equivalent to parent.**" The sooner deadline wins. | None. WithTimeout is defined as `WithDeadline(parent, time.Now().Add(timeout))`, so the same rule applies. |
| 8 | (L743 Gotcha, L527–533, L741–742) "Do not store context in a struct"; the three legitimate context values (request/trace ID, principal, logger); using ctx.Value for DI is a code smell. | **CORRECT** | pkg.go.dev/context (Overview): "**Do not store Contexts inside a struct type; instead, pass a Context explicitly to each function that needs it.**" / "Use context Values only for request-scoped data that transits processes and APIs, not for passing optional parameters to functions." | None. Matches the package guidance verbatim in intent. |
| 9 | (L766 UnderTheHood) `valueCtx.Value` walks parent chain, O(depth); key compared with `==`; key must be comparable; zero-size unexported struct key is ideal. | **CORRECT** | pkg.go.dev/context: "The provided key must be comparable …" The linked-list `valueCtx` walk (each WithValue adds one node, lookup recurses to parent) matches `src/context/context.go`; O(depth) is accurate for a chain of n WithValue nodes. | None. (The "Go 1.22 tightened the cancel tree's internal locking" parenthetical at L769 is the same unsupported claim as #3.) |
| 10 | (L919, L928–942 Callout) gRPC serializes `ctx.Deadline()` into the `grpc-timeout` header; server reconstructs a child context with that deadline; passing `context.Background()` defeats it. | **CORRECT (out-of-stdlib but accurate)** | grpc-go behavior: the client transport sets the `grpc-timeout` header from the context deadline; the server derives a context with that timeout. Consistent with grpc-go docs/source. Not a Go-stdlib claim; conceptually correct and properly hedged ("covered fully in the gRPC chapter"). | None. |
| 11 | (L783, L801, L919) Outbound HTTP: bare `http.NewRequest` uses `context.Background()` and is never cancelled; use `http.NewRequestWithContext`. database/sql has context-aware `QueryContext`/`ExecContext`/`BeginTx`. | **CORRECT** | pkg.go.dev/net/http: `NewRequest` returns a request whose context is `context.Background()`; `NewRequestWithContext` attaches the supplied ctx and "the request's cancellation … is controlled by the context." pkg.go.dev/database/sql exposes `(*DB).BeginTx`, `QueryContext`, `ExecContext`. (Applied from companion stdlib docs.) | None. |
| 12 | (L1088, L1103, L1398–1406 QuickCheck) "10 000 req/s × 5s timeout = roughly 50 000 retained timeout contexts at steady state" if cancel() is discarded and parent is long-lived. Answer index 2 (~50 000). | **CORRECT (arithmetic + mechanism)** | pkg.go.dev/context (WithTimeout): "Canceling this context releases resources associated with it, so code should call cancel as soon as the operations running in this Context complete." If cancel is never called and the timeout is 5s, contexts are retained until the timer fires; steady-state ≈ rate × timeout = 10 000 × 5 = 50 000. Mechanism (each WithTimeout registers a timer + child in parent's cancel list) confirmed. | None. (Hedged "roughly" / "with a long-lived parent that does not cancel first" — appropriately qualified.) |

## Sources fetched (this chapter)

- https://pkg.go.dev/context — "Do not store Contexts inside a struct type"; WithoutCancel ("returns no Deadline or Err … Done channel is nil"); AfterFunc / WithDeadlineCause / WithTimeoutCause signatures + added-go1.21.0; Cause; Canceled & DeadlineExceeded vars; key-comparable requirement (no documented WithValue-panic note)
- https://go.dev/doc/go1.21 — WithoutCancel, AfterFunc, WithDeadlineCause/WithTimeoutCause release-note text; log/slog new in 1.21
- https://go.dev/doc/go1.26 — `NotifyContext` cancels with CancelCauseFunc + signal error; no context-package change listed
- (companion stdlib facts applied: net/http NewRequest vs NewRequestWithContext, database/sql *Context methods, grpc-timeout header)

## Tally

- Rows flagged: **3** (WRONG ×2 [#1 WithValue-panic mis-attributed to 1.21; #2 WithoutCancel "copies deadline"], UNVERIFIABLE ×1 [#3 cancel-tree global-mutex → 1.22 per-node story])
- Rows confirmed CORRECT in table: **9** (#4–#12)
- Additional claims silently confirmed CORRECT (not tabled): buffered-done-channel-of-1 prevents goroutine leak in inner/outer select (L180, CodeWalk); always-derive-never-sever / Background severs cancellation (L269–271, QuickCheck L274–284 answer 1); per-hop budget carving with nested WithTimeout (L307–335); fast-path `ctx.Err()` before expensive work (L1132–1145); swallowing ctx.Err() loses the timeout signal (L1202–1220); errgroup.WithContext guarantees no goroutine outlives the call + cancels siblings on first error (L1241–1259); Done() lazily allocated (L1377, plausible/consistent with stdlib). **~7 silently confirmed.**
- Worst finding: **#3** — the "Go 1.7–1.21 single global `cancelCtxKey` mutex, replaced by per-node `sync.Mutex` in Go 1.22" narrative. `cancelCtx` has always had a per-instance mutex and `cancelCtxKey` is a sentinel *value key*, not a lock; the named version transition is unsupported and hands the reader a confident, fabricated-sounding internals story. (#2, the WithoutCancel "copies the parent's deadline" claim, is a close second — it directly contradicts the package doc and could cause a real bug if a reader relies on a deadline that isn't there.)
- Flags left for human review: 0 (report-only mode)
- Frontmatter unchanged: yes (no file edited)
