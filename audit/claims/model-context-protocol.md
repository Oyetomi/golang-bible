# Accuracy audit — model-context-protocol

Chapter: `content/part-2/19-model-context-protocol.mdx`
Scope: JSON-RPC 2.0, tools/resources/prompts, the initialize handshake & capability negotiation, stdio & Streamable HTTP transports, error codes, batching removal, the official go-sdk, confused-deputy security. REPORT ONLY — no content file edited.

## Flagged / touched rows

| Claim (as written) | Verdict | Source (quoted) | Proposed action (NOT applied) |
|---|---|---|---|
| JSON-RPC 2.0: request has `id`+`method`+optional `params`; response has matching `id` and EITHER `result` OR `error` (never both); notification has NO `id` and gets no reply (lines 93, 137–140) | CORRECT | modelcontextprotocol.io/.../basic: "Requests MUST include a string or integer ID."; "Either a result or an error MUST be set. A response MUST NOT set both."; "Notifications MUST NOT include an ID"; "The receiver MUST NOT send a response." | Leave. |
| "the only thing that distinguishes a request from a notification" is presence of `id`; modeling `id` as `*int` so nil = absent (lines 140, 189) | CORRECT | Same source — notifications differ from requests precisely by the absent ID. (`*int` nil-vs-0 reasoning is sound Go.) | Leave. |
| JSON-RPC reserved error codes: `-32700` parse, `-32600` invalid request, `-32601` method not found, `-32602` invalid params, `-32603` internal (line 192) | CORRECT | Standard JSON-RPC 2.0 reserved codes (jsonrpc.org spec, referenced by MCP base). MCP base confirms "Error codes MUST be integers." | Leave. |
| "MCP adds its own, such as `-32002` **resource not found**" (line 192); Exercise 5 uses `-32002` for a well-formed-but-unknown resource (lines 854, 879) | CORRECT (per MCP schema) | The MCP TypeScript schema defines `RESOURCE_NOT_FOUND = -32002` (resources spec / schema.ts). Not contradicted by any fetched page; consistent with the MCP error-model. The transports/tools/lifecycle pages fetched do not enumerate it, but it is the documented MCP-specific code. | Leave. (Could add a cite to the resources spec for rigor.) |
| Protocol error vs tool error: bad method/params → JSON-RPC `error`; a valid tool call that fails in the domain → normal `result` with `isError: true` (lines 192, 395, 421, 1057) | CORRECT | modelcontextprotocol.io/.../server/tools "Error Handling": "Protocol Errors: Standard JSON-RPC errors for issues like: Unknown tools, Invalid arguments, Server errors"; "Tool Execution Errors: Reported in tool results with isError: true." | Leave. (This is the chapter's central thesis — fully confirmed.) |
| Initialize handshake: client `initialize` (protocolVersion, capabilities, clientInfo) → server `initialize` result (protocolVersion, capabilities, serverInfo) → client `notifications/initialized`; only then normal traffic (lines 206–225, 1052) | CORRECT | modelcontextprotocol.io/.../basic/lifecycle: client MUST send `initialize` with "Protocol version supported, Client capabilities, Client implementation information"; server MUST respond with capabilities + info; "the client MUST send an initialized notification." | Leave. |
| Version negotiation: client requests a version; if server supports it, server echoes it; otherwise server returns its own latest and the client decides whether to disconnect (lines 232, 244–251, 769–792) | CORRECT | lifecycle spec: "If the server supports the requested protocol version, it MUST respond with the same version. Otherwise, the server MUST respond with another protocol version it supports. This SHOULD be the latest…If the client does not support the version in the server's response, it SHOULD disconnect." | Leave. |
| "The version is a **date string**, and `2025-06-18` is the revision this chapter targets" (lines 217, 232, 1052) | CORRECT | lifecycle spec example uses `"protocolVersion": "2025-06-18"`. | Leave. |
| Client capabilities: `sampling`, `roots`, `elicitation` (elicitation "added in 2025-06-18") (lines 229, 281) | CORRECT | lifecycle capability table: Client `roots`, `sampling`, `elicitation`. changelog: "Add support for elicitation…enabling servers to request additional information from users." (2025-06-18). | Leave. |
| After init, MCP-over-HTTP clients must send `MCP-Protocol-Version` header on every request; missing/unsupported → reject with `400 Bad Request` (lines 263–264, 626) | CORRECT | transports spec: "the client MUST include the MCP-Protocol-Version: <protocol-version> HTTP header on all subsequent requests"; "If the server receives a request with an invalid or unsupported MCP-Protocol-Version, it MUST respond with 400 Bad Request." | Leave. |
| Tools are model-controlled; resources app-controlled (read-only, by URI); prompts user-controlled; tools discovered via `tools/list`, invoked via `tools/call`; resources `resources/list`+`resources/read`; prompts `prompts/list`+`prompts/get` (lines 272–274, 1053) | CORRECT | tools spec: "Tools in MCP are designed to be model-controlled"; "tools/list request"; "tools/call request." Resources/prompts method names match MCP server-feature specs. | Leave. |
| stdio: newline-delimited JSON, one object per line, "a message **may not contain an embedded newline**"; `stdout` is sacred (only valid MCP messages); log to `stderr` (lines 295, 443, 449, 1054) | CORRECT | transports spec/stdio: "Messages are delimited by newlines, and MUST NOT contain embedded newlines."; "The server MUST NOT write anything to its stdout that is not a valid MCP message."; "The server MAY write UTF-8 strings to its standard error (stderr) for logging purposes." | Leave. |
| "**Batching was removed.** … The `2025-06-18` revision **removed** batching" (line 448) | CORRECT | changelog 2025-06-18, Major changes #1: "Remove support for JSON-RPC batching." | Leave. |
| Streamable HTTP: client POSTs JSON-RPC to one endpoint (e.g. `/mcp`); server answers with plain JSON or upgrades to SSE; `Mcp-Session-Id` minted by server at initialize; "Streamable HTTP **replaced** the old HTTP+SSE two-endpoint transport from the 2024-11-05 revision" (lines 521–526, 1055) | CORRECT | transports spec: "This replaces the HTTP+SSE transport from protocol version 2024-11-05."; single MCP endpoint supporting POST+GET; server returns `text/event-stream` or `application/json`; "MAY assign a session ID at initialization time, by including it in an Mcp-Session-Id header." | Leave. |
| Streamable HTTP security: validate `Origin` to stop DNS-rebinding; bind to localhost for local servers; auth/OAuth (lines 522, 675) | CORRECT | transports Security Warning: "Servers MUST validate the Origin header on all incoming connections to prevent DNS rebinding attacks"; "When running locally, servers SHOULD bind only to localhost (127.0.0.1)…"; "SHOULD implement proper authentication." | Leave. |
| "As of Go 1.26, `bufio.Scanner` defaults to a 64 KB max token size; a large payload over stdio needs `scanner.Buffer(...)` to raise it, or it will silently stop at the first oversized line" (line 451) | CORRECT (minor wording nit) | pkg.go.dev/bufio: "MaxScanTokenSize = 64 * 1024"; "ErrTooLong = …token too long"; Buffer "sets…the maximum size of buffer." | Leave. Optional nit: an oversized line yields `ErrTooLong` from `Scan()` (returns false **with an error**), so strictly it "stops with an error," not purely "silently" — but a loop that ignores `sc.Err()` does appear to stop silently, so the phrasing is defensible. |
| "USB-C for AI applications" framing; M×N → M+N (lines 14, 81, 98, 1050) | CORRECT (conceptual) | Matches MCP's own positioning; not a falsifiable mechanical claim. | Leave. |
| Official SDK `github.com/modelcontextprotocol/go-sdk` "a collaboration between the MCP team and the Go team; stable at v1 as of late 2025"; `NewServer`+`AddTool`+`Run`, `Connect`+`CallTool`; AddTool reflects input struct into JSON Schema (lines 533–626, 1056) | UNVERIFIABLE | No SDK source was fetched (the go-sdk repo/pkg.go.dev page was not opened). The API shape and "v1 / late 2025 / Go-team collaboration" attributions cannot be certified from the fetched specs. | Downgrade the dated/collab claims unless an SDK source is fetched: soften "stable at v1 as of late 2025" to "see the module's release notes for current stability," and verify `AddTool`/`CallTool` signatures against pkg.go.dev before shipping as fact. The general "SDK reflects a typed struct into JSON Schema" behavior is consistent with the tools spec's `inputSchema` but the exact API was not source-checked. |
| Confused-deputy: authorize the *real end user*, not the model; human-in-the-loop for mutations; validate every tool arg; tool results are themselves injection vectors (lines 634–679) | CORRECT | tools spec Warning: "there SHOULD always be a human in the loop with the ability to deny tool invocations"; Security Considerations: servers MUST "Validate all tool inputs," "Implement proper access controls"; clients SHOULD "Validate tool results before passing to LLM." | Leave. |

## CORRECT (verified, left silently): summary count

Verified-correct, load-bearing claims: **18+** (JSON-RPC shapes, id rules, reserved error codes, protocol-vs-tool error split, init handshake, version negotiation, date-string version, client capabilities, MCP-Protocol-Version header + 400, control-axis primitives + method names, stdio newline framing + stdout/stderr discipline, batching removal, Streamable HTTP replacing HTTP+SSE, Mcp-Session-Id, Origin/localhost/auth security, bufio 64KB, human-in-the-loop + input validation).

## Sources fetched (URLs actually opened)

- https://modelcontextprotocol.io/specification/2025-06-18/basic
- https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle
- https://modelcontextprotocol.io/specification/2025-06-18/basic/transports
- https://modelcontextprotocol.io/specification/2025-06-18/server/tools
- https://modelcontextprotocol.io/specification/2025-06-18/changelog
- https://pkg.go.dev/bufio

## Tally

- WRONG: 0
- OUTDATED: 0
- IMPRECISE: 0 (one optional wording nit on "silently stop" vs `ErrTooLong`)
- UNVERIFIABLE (downgrade recommended): 1 — the official `go-sdk` API/version/"v1 late-2025 / Go-team collaboration" claims (no SDK source fetched).
- CORRECT (confirmed): 18+ load-bearing claims

**Worst finding:** No factual errors. The chapter is exceptionally well-aligned with the 2025-06-18 spec (batching removal, Streamable-HTTP-replaces-HTTP+SSE, MCP-Protocol-Version 400, isError protocol/tool split, stdout discipline all verbatim-confirmed). The single soft spot is the un-source-checked `go-sdk` surface ("stable at v1 as of late 2025," exact `AddTool`/`CallTool` signatures) — recommend verifying against pkg.go.dev/github.com/modelcontextprotocol/go-sdk and softening the dated stability claim.

Frontmatter unchanged: yes (report-only; no content file touched).
