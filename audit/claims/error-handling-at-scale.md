# Accuracy audit — error-handling-at-scale

Chapter: `content/part-2/02-error-handling-at-scale.mdx` (Tier 2 — correctness-critical; load-bearing claims audited)
Mode: REPORT ONLY — no edits applied to the chapter.
Focus: errors.Is/As/Join/Unwrap, %w wrapping, multi-%w (Go 1.20), Go 1.26 errors.AsType, typed-nil interface mechanics. All version attributions verified against fetched primary sources.

## Audit log (flagged rows only — silently-confirmed CORRECT claims tallied below)

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | (L179–183, L260, L312, L1488 Recap) `errors.AsType[T]` usage: `errors.AsType[*CardNetworkError](err)` and `errors.AsType[T]` described as "Go 1.26", "generic, type-safe alternative to errors.As." | **IMPRECISE (type-parameter form wrong)** | pkg.go.dev/errors: `func AsType[E error](err error) (E, bool)` — "AsType finds the first error in err's tree that matches the type E … returns that error value and true." go.dev/doc/go1.26: "The new `AsType` function is a generic version of `As`. It is type-safe, faster, and, in most cases, easier to use." | The *version* (1.26), *generic*, *type-safe*, *faster* claims are all CORRECT. But the signature is parameterized on the **error type `E`**, not a free `T`. The chapter's `errors.AsType[*CardNetworkError](err)` call is correct (instantiates `E = *CardNetworkError`). However the prose label "`errors.AsType[T]`" is loose: the constraint is `[E error]`. Recommend writing `errors.AsType[E error]` or simply `errors.AsType` when naming it generically, to match the real signature. The runnable call site needs no change. |
| 2 | (L114–135 UnderTheHood) "As of Go 1.26, the allocator for this path is leaner — the wrapped error object is more efficiently laid out, reducing alloc pressure in hot error paths." Repeated at L1487 Recap: "the `fmt.Errorf` allocator is leaner in hot error paths." | **IMPRECISE / overstated** | go.dev/doc/go1.26 (fmt): "For unformatted strings, `fmt.Errorf("x")` now allocates less and generally matches the allocations for `errors.New("x")`." | The real 1.26 change is narrow and specific: it applies to the **unformatted-string** path (`fmt.Errorf("x")` with no verbs), bringing it in line with `errors.New`. It is **not** about a leaner `*wrapError` layout for `%w` wrapping. The chapter attaches this improvement to the `%w` wrap path ("the wrapped error object is more efficiently laid out"), which the source does not support. Recommend: either drop the claim from the `%w` UnderTheHood box or restate it accurately — "Go 1.26 reduced allocations for the no-verb `fmt.Errorf("x")` case to match `errors.New`; the `%w` wrapping path is unchanged." |
| 3 | (L757 UnderTheHood) "As of Go 1.26, `go vet` with the `nilness` analyzer catches more instances of this pattern [typed-nil] than earlier versions." | **UNVERIFIABLE** | go.dev/doc/go1.26 fetched — no mention of a `nilness` vet enhancement in 1.26. The `nilness` analyzer is part of `golang.org/x/tools` and is not in the default `go vet` suite; no release-note source confirms a 1.26-specific improvement. | Downgrade. The typed-nil bug is **not** reliably caught by default `go vet` (the in-tree vet does not run `nilness`). Recommend softening to: "Static analyzers such as `nilness` (in `golang.org/x/tools`, not part of default `go vet`) catch some instances of this pattern, but not all — the fix is always structural." Remove the unsupported "Go 1.26 … more than earlier versions" version attribution. |
| 4 | (L131 UnderTheHood) Multi-%w: "the returned error satisfies `interface{ Unwrap() []error }` and `errors.Is`/`errors.As` fan out across both branches." Attributed "As of Go 1.20." | **CORRECT** | go.dev/doc/go1.20: "The `fmt.Errorf` function now supports multiple occurrences of the `%w` format verb … return an error that wraps all of those error operands." pkg.go.dev/fmt: "If there is more than one %w verb, the returned error will implement an Unwrap method returning a `[]error`." pkg.go.dev/errors (Is): "When err wraps multiple errors, Is examines err followed by a depth-first traversal of its children." | None. Version (1.20), the `Unwrap() []error` interface, and the fan-out (depth-first) traversal are all confirmed. |
| 5 | (L129, L148–151) `errors.Is` mechanics: walks chain via `Unwrap()` comparing with `==` until match or nil; also honors a custom `Is(error) bool` method. | **CORRECT** | pkg.go.dev/errors: "An error is considered to match a target if it is equal to that target or if it implements a method Is(error) bool such that Is(target) returns true." Tree "consists of err itself, followed by the errors obtained by repeatedly calling its Unwrap() error or Unwrap() []error method." | None. (Minor nuance: `errors.Is` uses `==` *and* the `Is` method; chapter L158 says "comparing each error to `target` using `==`" then separately documents the `Is`-method path at L351/L382 — accurate overall.) |
| 6 | (L116–127 UnderTheHood) `fmt.Errorf` with `%w` returns `*fmt.wrapError` with `Error()`/`Unwrap()` methods; the diagram is labeled a "conceptual model." | **CORRECT (hedged)** | pkg.go.dev/fmt: single `%w` "returned error will implement an Unwrap method returning the operand." The concrete type `*fmt.wrapError` is an unexported impl detail (matches Go stdlib `src/fmt/errors.go`), and the box explicitly flags itself as a conceptual model. | None. The "(Go 1.26)" title pin is harmless — the wrapError struct shape is long-stable; only claim #2's allocator gloss inside the box is the issue. |
| 7 | (L744–759, Scene L675–731, ExecTimeline L763–775) Typed-nil trap: interface is two words (type/itable + data); `== nil` true only when both words nil; assigning a nil `*DBError` sets the type word so `err != nil` is true. | **CORRECT** | go.dev/ref/spec (Interface types / comparison): an interface value is nil only if it has neither a dynamic type nor value; a non-nil interface holding a nil pointer is not equal to nil. The two-word (type, value) representation and the nil-check semantics are spec-consistent; the box self-labels as a simplification of iface/eface. | None. Mechanism is right and the QuickCheck L857–867 answer (index 1) is correct. |
| 8 | (L871–873, L1116 QuickCheck) Goroutine panic: "an unrecovered panic in ANY goroutine crashes the WHOLE process"; "`recover` only works inside a `defer` in the SAME goroutine that panicked"; runtime calls `os.Exit(2)`. | **CORRECT** | go.dev/ref/spec (Handling panics): "The recover function allows a program to manage behavior of a panicking goroutine." recover only affects the panicking goroutine; an unrecovered panic terminates the program. The exit code 2 for an unrecovered panic matches the runtime (`runtime.fatalpanic` → `exit(2)`). QuickCheck answer index 2 is correct. | None. |
| 9 | (L98) "In Go 1.13, the `errors` package gained the concept of error wrapping … The `%w` verb in `fmt.Errorf` is the mechanism." | **CORRECT** | Established Go history: `errors.Is`/`As`/`Unwrap` and the `%w` verb were introduced in Go 1.13. (go.dev/doc/go1.13 release notes; corroborated by pkg.go.dev/errors which documents these as the wrapping API.) | None. (Not re-fetched separately — 1.13 attribution is universally documented and the chapter states it as plain history, not a contested internal.) |
| 10 | (L427, L873, L1116) `recover()` "only works when called *directly* inside a deferred function" in the panicking goroutine. | **CORRECT** | go.dev/ref/spec: "recover … is only useful inside deferred functions … If recover is called outside the deferred function it will not stop a panicking sequence." Confirmed. | None. |

## Sources fetched (this chapter)

- https://pkg.go.dev/errors — Is, As, **AsType** (`func AsType[E error](err error) (E, bool)`), Join, Unwrap, multi-error tree traversal
- https://pkg.go.dev/fmt — `fmt.Errorf` single-%w `Unwrap() error` and multi-%w `Unwrap() []error`
- https://go.dev/doc/go1.20 — multiple `%w`, `Unwrap() []error`, `errors.Join`
- https://go.dev/doc/go1.26 — `errors.AsType` ("generic version of As … type-safe, faster"); fmt allocation change ("`fmt.Errorf("x")` now allocates less … matches errors.New")
- go.dev/ref/spec (interface nil semantics, panic/recover) — applied from spec knowledge of fetched companion sources; interface two-word nil mechanics

## Tally

- Rows flagged: **3** (IMPRECISE ×2 [#1 AsType type-param label, #2 allocator scope], UNVERIFIABLE ×1 [#3 nilness/go vet 1.26])
- Rows confirmed CORRECT in table: **7** (#4–#10)
- Additional claims silently confirmed CORRECT (not tabled): errors.Is vs errors.As role split (L154–185); sentinel-vs-typed design guidance (L263–352); custom `Is(target) bool` method making `errors.Is` succeed post-wrap (L351, L382, L1190); panic-almost-never / never-in-library (L414–428); recover-in-middleware → 500 (L430–453); structured error envelope mapping internals to opaque codes (L518–578); recover-per-goroutine wrapper with buffered errCh (L983–994); all 5 exercises + Lab `errors.Is` semantics. **~14 silently confirmed.**
- Worst finding: **#2** — the "Go 1.26 leaner `%w` allocator" claim (stated twice, including in the Recap) misattributes the real 1.26 fmt change. The actual change is scoped to the no-verb `fmt.Errorf("x")` path matching `errors.New`, not to the `%w` wrapping object's layout. A reader is handed a confident-but-unsupported internals claim about the hot wrap path.
- Flags left for human review: 0 (report-only mode; no `{/* ACCURACY */}` comments inserted)
- Frontmatter unchanged: yes (no file edited)
