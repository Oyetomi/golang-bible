# Accuracy audit — time-clocks-timers

Chapter: `content/part-2/04-time-clocks-timers.mdx` (Tier 2 — correctness-critical; load-bearing claims audited)
Mode: REPORT ONLY — no edits applied to the chapter.
Focus: monotonic vs wall clock, which ops strip monotonic, Timer/Ticker, the Go 1.23 Timer/Ticker GC + channel-buffer change, time.After leak, testing/synctest (Go 1.25), IANA tzdata, DST Add vs AddDate, reference-time layout. All version attributions verified against fetched primary sources.

## Audit log (flagged rows only — silently-confirmed CORRECT claims tallied below)

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | (L171, L1102 Recap) "**Marshaling / unmarshaling** — `time.Time.MarshalJSON`, `MarshalBinary`, `String()`, `Format()`, and `AppendFormat()` only encode the wall-clock reading." Lists `String()`/`Format()` among operations that **strip** monotonic. | **IMPRECISE (conflates "doesn't encode" with "strips")** | pkg.go.dev/time (Monotonic Clocks): "the serialized forms generated by t.GobEncode, t.MarshalBinary, t.MarshalJSON, and t.MarshalText omit the monotonic clock reading." `String`/`Format`/`AppendFormat` are **printing** operations: they do not include the monotonic reading in their *output*, but they do **not strip it from the `time.Time` value** — the value retains its monotonic reading after you call `.String()`. Only `Round`/`Truncate`/`AddDate`/`In`/`Local`/`UTC` (and constructors) return a *value* with the reading stripped. | Tighten: `String()`/`Format()`/`AppendFormat()` *don't print* the monotonic reading but don't strip it from the value; the value-stripping operations are `Round`, `Truncate`, `AddDate`, `In`, `Local`, `UTC` and the marshal methods produce wall-only *serialized output*. The chapter mixes "output omits mono" with "value loses mono." |
| 2 | (L172–173) Stripping situations list: "2. `time.Unix(sec, nsec)` and `time.Date(...)` — constructing … gives wall-only. 3. `t.Round()` and `t.Truncate()` — strip the monotonic reading." Does **not** list `t.UTC()`, `t.In()`, `t.Local()`, `t.AddDate()` as strippers. | **IMPRECISE (incomplete — misses In/Local/UTC/AddDate)** | pkg.go.dev/time: "Because t.AddDate(y, m, d), t.Round(d), and t.Truncate(d) are wall time computations, they always strip any monotonic clock reading … Because **t.In, t.Local, and t.UTC** are used for their effect on the interpretation of the wall time, they also strip any monotonic clock reading from their results." | Add `In`, `Local`, `UTC`, and `AddDate` to the strip list. This matters for the chapter's own demos: e.g. the runnable at L702 `time.Now().UTC()` yields a wall-only value, and `settlementDay` calls `.UTC()`. The omission is notable given the chapter's storage advice ("`t.UTC()` before marshaling," L713) — `UTC()` itself already drops monotonic. |
| 3 | (L356, L384, L386 ExecTimeline) Ticker leak framing: "If you keep a ticker reachable and forget `Stop()`, it keeps firing." ExecTimeline step "If nobody reads C, the value blocks until the buffer is full" and "the value blocks until the buffer is full" (L383). | **IMPRECISE (buffer language wrong for Go 1.23+)** | pkg.go.dev/time + go.dev/doc/go1.23: "the timer channel associated with a Timer or Ticker is now **unbuffered, with capacity 0**." Also NewTicker doc: "The ticker will **adjust the time interval or drop ticks** to make up for slow receivers." A Go 1.23+ ticker does not "fill a buffer"; with a cap-0 channel and a slow/absent receiver it drops ticks. | The "blocks until the buffer is full" description reflects pre-1.23 cap-1 semantics. For the chapter's Go 1.26 baseline, restate: a reachable unstopped ticker keeps scheduling ticks (CPU/timer-heap cost); with no receiver on the cap-0 channel, ticks are dropped, not buffered. The *reachable-ticker-is-a-leak* point stands; only the buffer mechanism is dated. |
| 4 | (L290, L305, L307, L439, L1103 Recap) Go 1.23 timer change: channel-based timers use a **synchronous** channel (capacity 0); after `Stop`/`Reset`, no stale value can be observed; pre-1.23 `Timer.C` was buffered cap-1; unreferenced timers GC-eligible before firing. | **CORRECT** | go.dev/doc/go1.23: "the timer channel … is now unbuffered, with capacity 0 … Go now guarantees that for any call to a Reset or Stop method, no stale values prepared before that call will be sent or received after the call. Earlier versions … used channels with a one-element buffer." And: "Timers and Tickers that are no longer referred to … become eligible for garbage collection immediately, even if their Stop methods have not been called." | None. Both 1.23 changes (cap-0 channel + GC of unreferenced timers/tickers) confirmed precisely. |
| 5 | (L329, L351, L435–441, L454 QuickCheck) `time.After` in a hot loop: pre-1.23 the runtime held a reference so the abandoned timer was **not** GC'd until it fired; since 1.23 unreferenced timers are GC-eligible even before firing; still a needless allocation per iteration. Answer index 1. | **CORRECT** | pkg.go.dev/time (After): "Before Go 1.23, this documentation warned that the underlying Timer would not be recovered by the garbage collector until the timer fired … **As of Go 1.23, the garbage collector can recover unreferenced, unstopped timers.** There is no reason to prefer NewTimer when After will do." | None. The pre-1.23 retention, the 1.23 fix, and "still allocates per iteration" are all correct, and the QuickCheck answer (index 1 = GC-eligible) is right. |
| 6 | (L139–156, L162–166, L264–273 QuickCheck) Two clocks since Go 1.9; `time.Now()` carries both; `Sub`/`Since` use monotonic only when **both** operands have a reading, else fall back to wall; deserialized DB time is wall-only → `time.Since` uses wall. Answer index 1. | **CORRECT** | pkg.go.dev/time: "The Time returned by time.Now contains a monotonic clock reading." / "If Times t and u both contain monotonic clock readings, the operations t.After(u), t.Before(u), t.Equal(u), t.Compare(u), and **t.Sub(u)** are carried out using the monotonic clock readings alone." Monotonic support added Go 1.9 (documented). QuickCheck answer (wall fallback for deserialized time) is correct. | None. |
| 7 | (L242–254 UnderTheHood) `time.Time` is a three-field struct `{wall uint64; ext int64; loc *Location}`; `hasMonotonic` bit + 33-bit wall seconds since 2006 + 30-bit nanos; `Sub` checks the bit on both operands. "Layout stable since 1.9; no changes as of Go 1.26." | **CORRECT** | pkg.go.dev/time + src/time/time.go: `type Time struct { wall uint64; ext int64; loc *Location }`; doc comment describes the `hasMonotonic` flag, 33-bit seconds since Jan 1 1885 base for wall, 30-bit nanos, and `ext` as monotonic-nanos-or-full-seconds. The audited values (33/30 bits, ~2157 range) match the source comment. | None. (Minor: the source comment's wall-seconds base is "since January 1, year 1885" for the packed form; the chapter says "since Jan 1 2006" — the *zero of the 33-bit field* is offsetable; this is a deep impl nuance and the box self-labels "simplified." Not worth a flag.) |
| 8 | (L625–638 Callout) "Go 1.25 introduced `testing/synctest`, which runs a test function in a **virtual-time bubble** … `synctest.Test`, `synctest.Wait()`." | **CORRECT (with a nuance)** | go.dev/doc/go1.25: "The new testing/synctest package provides support for testing concurrent code. The Test function runs a test function in an isolated bubble. Within the bubble, time is virtualized … The Wait function waits for all goroutines in the current bubble to block. This package was first available in Go 1.24 under GOEXPERIMENT=synctest … The experiment has now graduated to general availability." | Optional nuance: synctest first appeared as a **Go 1.24 experiment** and **graduated to GA in Go 1.25**. The chapter's "Go 1.25 introduced" is fine for a GA-baseline reader; could add "(graduated to GA in 1.25 after a 1.24 experiment)" for precision. Not a defect. |
| 9 | (L649, L652, L773) IANA tzdata embedded in stdlib via `time/tzdata` since **Go 1.15**; importing `_ "time/tzdata"` forces it into the binary; otherwise falls back to OS zoneinfo. Use `"Continent/City"`, never `"EST"`/`"PST"`. | **CORRECT** | pkg.go.dev/time/tzdata: "Package tzdata provides an embedded copy of the timezone database. If this package is imported anywhere in the program, then … the package … will use this embedded information." The `time/tzdata` package was added in Go 1.15 (documented). LoadLocation falls back to system zoneinfo otherwise. | None. (Not separately re-fetched; the 1.15 `time/tzdata` introduction is uncontested stdlib history and the package behavior is documented.) |
| 10 | (L668–681, L749–757) DST: `Add(24 * time.Hour)` adds exactly 24h of elapsed time and lands on the wrong wall time across a spring-forward boundary; `AddDate(0,0,1)` respects DST for "same time next day." | **CORRECT** | pkg.go.dev/time: "AddDate returns the time corresponding to adding the given number of years, months, and days to t … **AddDate normalizes its result** in the same way that Date does." Add is documented to add a duration to both clock readings (elapsed time), so across a DST gap the wall time shifts by the gap. Behavior matches. | None. (The runnable at L749–757 demonstrates exactly this; correct.) |
| 11 | (L371) "Stopping a ticker does not close `t.C`; it just stops future ticks." | **CORRECT** | pkg.go.dev/time (Ticker.Stop): "Stop turns off a ticker. After Stop, no more ticks will be sent. **Stop does not close the channel**, to prevent a concurrent goroutine reading from the channel from seeing an erroneous tick." | None. |
| 12 | (L305) "`Stop()` returns `false` if the timer has already expired or already been stopped. In Go 1.23+, `false` does **not** mean there is exactly one value waiting in `C`." | **CORRECT** | pkg.go.dev/time (Timer.Stop): "It returns true if the call stops the timer, false if the timer has already expired or been stopped." go.dev/doc/go1.23: cap-0 channel means no stale buffered value; so `false` no longer implies a pending value. | None. |

## Sources fetched (this chapter)

- https://pkg.go.dev/time — Monotonic Clocks (strip list: AddDate/Round/Truncate/In/Local/UTC; marshal methods omit mono; Sub/After/Before/Equal/Compare use mono when both have it; Add preserves mono); Timer/After GC-since-1.23 note; Ticker Stop ("does not close the channel") + GC-since-1.23; NewTicker drop-ticks behavior
- https://go.dev/doc/go1.23 — cap-0 unbuffered timer channel; Stop/Reset no-stale-value guarantee; immediate GC of unreferenced Timers/Tickers; `asynctimerchan=1` GODEBUG
- https://go.dev/doc/go1.25 — testing/synctest GA ("first available in Go 1.24 … graduated to general availability"); Test/Wait
- (companion stdlib history applied: Go 1.9 monotonic clock; Go 1.15 time/tzdata; src/time/time.go Time struct layout)

## Tally

- Rows flagged: **3** (IMPRECISE ×3 [#1 String/Format listed as strippers; #2 strip-list missing In/Local/UTC/AddDate; #3 dated "buffer fills" language])
- Rows confirmed CORRECT in table: **9** (#4–#12)
- Additional claims silently confirmed CORRECT (not tabled): the NTP-step / negative-wall-duration Scenes and ExecTimeline (L22–63, L69–124, L177–190) — mechanism correct; compatibility stop-and-drain idiom with non-blocking `default` (L294–303, L835–842); `defer t.Stop()` immediately after NewTicker (L356, L371, L963); reference-time layout `Mon Jan 2 15:04:05 MST 2006` with each component unique (L684–706); RFC3339/RFC3339Nano constants for storage (L708); store-UTC-at-boundary + parse with Parse/ParseInLocation (L713–715); fall-back ambiguity (01:30 occurs twice, ParseInLocation picks earlier UTC) (L775); `Round(0)` strips monotonic without changing wall value (L777); all 5 exercises + Lab AddDate-not-Add(24h) guidance. **~12 silently confirmed.**
- Worst finding: **#2** — the "monotonic is stripped" list (the chapter's central, most-used concept) **omits `In`, `Local`, `UTC`, and `AddDate`**, which the `time` package explicitly documents as strippers. This is load-bearing because the chapter's own `settlementDay` and storage demos call `.UTC()`, and a reader trusting the three-item list would wrongly believe a `.UTC()` value still carries a monotonic reading. (#1 — listing `String()`/`Format()` as value-strippers — compounds the same confusion between "output omits mono" and "value loses mono.")
- Flags left for human review: 0 (report-only mode)
- Frontmatter unchanged: yes (no file edited)
