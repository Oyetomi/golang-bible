# Technical-Accuracy Audit — debugging-go

Chapter: `content/part-2/09-debugging-go.mdx` (Tier-2 / REPORT-ONLY — no content file edited)
Baseline Go version: 1.26. Date: 2026-06-14.

Every falsifiable claim below was checked against a source fetched live (no memory certification).
A running CORRECT count of silently-confirmed claims follows the table.

By-design (NOT flagged as bugs, per audit brief): the chapter's panic demo (`processTransfer(-500)`)
and its self-deadlock demos are deliberate — the failure IS the lesson. Only the comment/code
mismatch in the self-deadlock block is flagged (it mislabels the mechanism), per the brief.

## Audit-log table

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | Self-deadlock demo (lines 444–453): comment reads **"// Goroutine 1: locks mu, then tries to lock mu again (classic self-deadlock)"** and sits directly under a `<GraphAnim>` titled **"Two goroutines, two mutexes, one cycle."** The code is a **single-goroutine self-deadlock** — one `main` goroutine calls `mu.Lock()` twice. | FLAG (comment/label vs code mismatch) | The code: `func main(){ var mu sync.Mutex; mu.Lock(); mu.Lock() … }` — one goroutine, one mutex, locked twice. The runtime output it produces is the self-deadlock (`semacquire`), which the prose then correctly describes. But "Goroutine 1 … two mutexes … one cycle" framing describes a *different* mechanism (two-goroutine lock-order cycle). The self-deadlock failure is by design; the **mislabel** is the defect. | Reword the comment to remove the "Goroutine 1 / cycle" framing, e.g. `// main goroutine: locks mu, then locks mu AGAIN on itself — single-goroutine self-deadlock (a Mutex is non-reentrant)`. Optionally add one line clarifying this demo is a self-deadlock, distinct from the two-goroutine lock-order cycle shown in the GraphAnim above. Do NOT change the code — the double-lock is the intended lesson. |
| 2 | "Delve … understands goroutines, the Go scheduler, DWARF debug info … and interfaces." Install via `go install github.com/go-delve/delve/cmd/dlv@latest`. `dlv debug` / `dlv test` / `dlv attach`. | CORRECT | Matches Delve's documented capabilities and the canonical install path + the three entry points. Conceptually accurate. | — |
| 3 | "`dlv debug` compiles your program with full debug info (disabling optimizations that would confuse the debugger)." | CORRECT | Delve builds with `-gcflags="all=-N -l"` (disable optimizations + inlining) so source maps to instructions cleanly. Accurate. | — |
| 4 | Breakpoint forms: line, function (`(*Handler).Transfer`), conditional (`condition 1 accountID == "acct_99f"`). `next` (over), `step` (into), `stepout` (return), `continue`. | CORRECT | These are Delve's actual commands and semantics. The QuickCheck answer ("break … then condition 1 amount > 100000") is the correct two-step conditional-breakpoint workflow. Accurate. | — |
| 5 | "`print a.mu` → `sync.Mutex {state:1, sema:0}  ← state:1 means LOCKED`" | CORRECT (illustrative) | sync.Mutex's low bit of `state` is `mutexLocked`; `state:1` = held. The displayed struct layout matches sync.Mutex internals. Accurate as an illustration. | — |
| 6 | INT3/DWARF breakpoint mechanism: DWARF maps file+line→address; Delve writes `0xCC` (INT3) at the address; CPU traps → SIGTRAP → Delve stops; restores original byte; conditional bps trap-then-evaluate-then-continue-if-false. | CORRECT (and self-labelled "conceptual model") | DWARF = "Debugging With Arbitrary Record Formats"; `0xCC`/INT3 is the x86-64 software breakpoint; SIGTRAP delivery is correct. The chapter explicitly footnotes it as a simplified model (ptrace/OS details vary). Accurate + appropriately hedged. | — |
| 7 | "Delve walks the linked list of goroutines via `runtime.allgs`, reads each one's PC, translates back to source via DWARF." | CORRECT | `runtime.allgs` is the runtime's slice of all `g` structs; Delve enumerates goroutines from runtime data. Accurate. | — |
| 8 | "As of Go 1.25, the toolchain emits **DWARF v5 by default** (a change from the DWARF v4 output used through Go 1.24), and Delve supports it fully." | CORRECT | go.dev/doc/go1.25: "The compiler and linker in Go 1.25 now generate debug information using DWARF version 5 … can be disabled by setting GOEXPERIMENT=nodwarf5." Pin (1.25) and the v4→v5 change are correct. | — |
| 9 | Panic trace reading: "top frame = where panic() was called"; goroutine header shows id + state (`running` for a panic; `chan receive`/`sync.Mutex`/`select` for blocked); read top-down for the call chain. | CORRECT | Matches Go's panic stack-trace format and reading discipline. The worked example (`main → handleRequest → processTransfer → panic`) is correct top-down. Accurate. | — |
| 10 | "The Go runtime has a built-in deadlock detector … fires whenever *all* goroutines are blocked with no path forward … not optional, not configurable." And the Callout: "If two goroutines deadlock but a third is still running … the detector stays silent." | CORRECT | This is the documented behavior: the runtime reports `fatal error: all goroutines are asleep - deadlock!` only when every goroutine is blocked; partial deadlocks among live goroutines are NOT detected. Accurate — and load-bearing for the rest of the chapter. | — |
| 11 | "`fatal error: all goroutines are asleep - deadlock!` … goroutine 1 [semacquire] … blocked at `sync.runtime_SemacquireMutex` … from our double `mu.Lock()`." | CORRECT | The double-lock produces exactly this `semacquire` trace; the prose reads it correctly. (The mechanism PROSE is right; only the in-code *comment* in #1 mislabels it.) | — |
| 12 | "send `SIGQUIT` … dumps the full stack of every goroutine to stderr, then exits. On macOS and Linux, SIGQUIT is the default trigger … runtime catches it before the OS can kill the process." `Ctrl+\`. | CORRECT | Documented Go runtime behavior: an otherwise-unhandled SIGQUIT triggers a full goroutine traceback then exit; `Ctrl+\` sends SIGQUIT. Accurate. | — |
| 13 | "`GOTRACEBACK=all` (every goroutine incl. runtime internals); `GOTRACEBACK=crash` (dump + core file); `GOTRACEBACK=none` (suppress)." | CORRECT | Matches runtime `GOTRACEBACK` documented values (`none`/`single`/`all`/`system`/`crash`); `crash` additionally raises SIGABRT to produce a core. Descriptions accurate. | — |
| 14 | Goroutine-leak signature: "Goroutine count climbs monotonically … many goroutines all stuck at the same line — often waiting on a channel that nothing ever closes." QuickCheck: 400 goroutines in [chan receive] at same line = leak (not deadlock, since the detector would have fired if ALL were stuck). | CORRECT | Correct reasoning and consistent with #10 (a leak coexists with live goroutines, so the all-asleep detector stays silent). The leaking-handler example (goroutine ignores `r.Context()`) is the canonical cause. Accurate. | — |
| 15 | "`GODEBUG=gctrace=1` prints a line for every GC cycle to stderr"; "`GODEBUG=schedtrace=1000` dumps scheduler state every 1000ms (runnable goroutines, spinning threads, idle Ps)"; "read at startup, cannot be changed while the process runs." | CORRECT | Matches the runtime `GODEBUG` `gctrace` and `schedtrace=N` (milliseconds) knobs and their startup-only nature. Accurate. | — |
| 16 | "Every Go HTTP service should import `net/http/pprof` … side-effect import registers handlers on `http.DefaultServeMux`." Endpoints: `/debug/pprof/{goroutine,heap,block,mutex,threadcreate,trace}`. `?debug=2` = full human-readable goroutine dump (same format as SIGQUIT). | CORRECT | net/http/pprof docs: blank import registers handlers under `/debug/pprof/` on DefaultServeMux; `?debug=2` yields the full text goroutine dump. Endpoint list is accurate. Exercise 4's "appHandler uses its own mux so pprof is NOT exposed publicly" is the correct isolation pattern. | — |
| 17 | "As of Go 1.26, `go tool pprof`'s `web` command opens a flame graph by default instead of the old call-graph SVG … old graph available via `(pprof) callgrind` or the `-http` flag's dropdown." | IMPRECISE | go.dev/doc/go1.26: "**The `pprof` tool web UI, enabled with the `-http` flag, now defaults to the flame graph view.** The previous graph view is available in the 'View -> Graph' menu, or via `/ui/graph`." The default-to-flame-graph change is for the **`-http` web UI**, and the fallback is the **View→Graph menu**, not the `web` command's SVG nor a `callgrind` command. The chapter attaches the change to the `web` command and names the wrong fallback. | Tighten to match the release note: the **`-http` web UI** now defaults to the flame graph; the old call graph is under **View → Graph** (or `/ui/graph`). Drop "`(pprof) callgrind`" as the recovery path. (The interactive `web` SVG command still exists; the 1.26 *default* change is specifically the `-http` UI.) |
| 18 | Same Callout (kind="note", Go 1.26 flame-graph default) — version pin 1.26. | CORRECT (pin) / see #17 for the command/fallback imprecision | go.dev/doc/go1.26 confirms the change lands in 1.26. The version is right; only the *command/fallback wording* (#17) is off. | — |
| 19 | "Go 1.26 adds a dedicated `goroutineleak` pprof profile … reports goroutines blocked on concurrency primitives that the runtime can prove cannot become unblocked. Enable with `GOEXPERIMENT=goroutineleakprofile`; that also exposes the `net/http/pprof` endpoint `/debug/pprof/goroutineleak`." | CORRECT | go.dev/doc/go1.26: "A new profile type that reports leaked goroutines is now available as an experiment … named `goroutineleak` in the runtime/pprof package … enabled by setting `GOEXPERIMENT=goroutineleakprofile` at build time." And: "Enabling the experiment also makes the profile available as a net/http/pprof endpoint, `/debug/pprof/goroutineleak`." Matches exactly, including the experiment framing. | — |
| 20 | "`fmt.Println` and `slog` (Go 1.21+)" / "`slog` with structured fields is the production version." | CORRECT | go.dev/doc/go1.21: "The new log/slog package provides structured logging with levels." Pin (1.21) correct. | — |
| 21 | "`GOTRACEBACK=crash ./payments-service` … generates a core file … analyze with `dlv core ./payments-service core.12345`." | CORRECT | `GOTRACEBACK=crash` causes the runtime to crash with a core dump (subject to OS `ulimit -c`); Delve's `dlv core <binary> <corefile>` performs post-mortem analysis. Accurate. | — |
| 22 | UnderTheHood "How the runtime produces a stack trace": goroutine stacks are heap-allocated (not OS thread stacks), **starting at 2KB as of Go 1.4 (down from 8KB)**, grown by copy; walk via `pcdata`/`funcdata`; PC→source via `pclntab`; state strings from the `g.waitreason` field. Footnoted as a simplified Go 1.26 model. | CORRECT | go.dev/doc/go1.4: "the default starting size for a goroutine's stack in 1.4 has been reduced from 8192 bytes to 2048 bytes." The 2KB-since-1.4 figure is exact. pclntab (PC-to-line) and pcdata/funcdata frame tables, copy-grow stacks, and `waitreason`-derived state strings all match runtime internals. Accurate + appropriately hedged. | — |
| 23 | "attaching [`dlv attach`] stops the process during the debug session. In production this means downtime." | CORRECT | Delve attaches via ptrace and halts the target; on a live single instance that is an outage. The warning to use a replica / production-safe techniques is correct. | — |
| 24 | "Never expose Delve's port to the public internet … Delve's API gives full control … read memory, set variables, execute arbitrary code. Bind to 127.0.0.1 / tunnel via SSH." | CORRECT | Delve's headless API does allow arbitrary control of the target (including function calls / variable writes); binding to localhost and tunnelling is the correct posture. Accurate security guidance. | — |

## Tally

- Flagged rows (verdict ≠ clean CORRECT): **2**
  - #1 — self-deadlock demo's in-code comment ("Goroutine 1 … (classic self-deadlock)") under a "Two goroutines … one cycle" GraphAnim mislabels a single-goroutine self-deadlock. (Per brief: flag the label, keep the by-design failure.)
  - #17 — `go tool pprof` flame-graph default is IMPRECISE: the 1.26 change is to the **`-http` web UI** (default → flame graph; fallback **View → Graph**), not the `web` command's SVG, and not recoverable via a "`callgrind`" command.
- CORRECT (source-confirmed, left as written): **22** of the 24 audited items (with #18 pin correct; its wording issue folded into #17).
- WRONG (source refutes): **0**.

Worst finding: **#1** — the deliberate self-deadlock demo is mislabeled. The in-code comment frames it as "Goroutine 1 … (classic self-deadlock)" while sitting under the "Two goroutines, two mutexes, one cycle" `<GraphAnim>`, conflating two distinct deadlock mechanisms (single-goroutine non-reentrant double-lock vs. two-goroutine lock-order cycle). A learner could leave with the wrong mental model of *why* this specific program deadlocks. The fix is a one-line comment reword; the double-`mu.Lock()` code stays (it is the intended lesson and produces the correct `semacquire` trace the prose explains).

## Sources fetched (URLs actually opened)

- https://go.dev/doc/go1.25 (DWARF v5 default — confirms v4→v5 at 1.25)
- https://go.dev/doc/go1.26 (pprof -http flame-graph default; goroutineleak profile + GOEXPERIMENT + net/http/pprof endpoint)
- https://go.dev/doc/go1.21 (log/slog package)
- https://go.dev/doc/go1.4 (goroutine starting stack 8192→2048 bytes)

Frontmatter unchanged: yes (report-only; no content file touched).
