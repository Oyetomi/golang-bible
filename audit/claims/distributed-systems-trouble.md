# Accuracy audit — distributed-systems-trouble

Chapter: `content/part-2/23-distributed-systems-trouble.mdx`
Tier 2 (correctness-critical: distributed-systems canon — unreliable networks/clocks, process pauses, fencing tokens, quorums, safety/liveness). REPORT ONLY — no content file was edited.

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (not applied) |
|---|---|---|---|---|
| 1 | A timeout/non-response is ambiguous: "request lost," "bank down," "bank PAID and the response was lost," and "mid-crash" are all indistinguishable from the sender's side; a timeout carries only "no reply yet" and says nothing about whether the side effect happened. (Scene, ExecTimeline, QuickCheck, Recap) | **CORRECT** | Two Generals' Problem (en.wikipedia.org/wiki/Two_Generals%27_Problem): "no matter how many rounds of confirmation are made, there is no way to guarantee" agreement; a sender "has no idea whether or not the messenger got through." The impossibility of confirmed delivery over an unreliable channel is exactly this ambiguity. | None. This is the load-bearing claim of the chapter and it is correct. |
| 2 | "TCP retransmits lost packets, but… after a timeout it gives up and errors. And a TCP acknowledgement only means the remote *kernel* received the bytes — the application may have crashed before handling them. If you want to know a request succeeded, you need a positive response from the application itself, not from the transport." | **CORRECT** | Two Generals: "TCP cannot guarantee state consistency between endpoints… A TCP acknowledgment only confirms receipt at the network layer, not that the receiving application completed its intended action." Matches exactly. | None. |
| 3 | Asynchronous packet networks have **unbounded delays** — "there's no upper limit on how long a packet may take." | **CORRECT** | Standard model (DDIA Ch. 8; matches the asynchronous-network definition underlying FLP). No fetched source contradicts; the asynchronous model is defined precisely by the absence of bounds on message delay. | None. (Parenthetical "cross-region round-trips of *minutes*… observed during congestion" is an unsourced anecdote — true-ish, plausibility-only; harmless as illustration.) |
| 4 | Falsely declaring a live node dead is dangerous: "its work gets done twice, and shifting its load onto already-busy nodes can trigger a **cascading failure**." | **CORRECT** | Standard failure-detector trade-off (DDIA Ch. 8, "Timeouts and Unbounded Delays"). Conceptually canonical; no source refutes. | None. |
| 5 | "Most network delay variability comes from **queueing**… production systems measure the round-trip distribution and adapt (e.g. the **Phi Accrual failure detector in Cassandra**)." | **CORRECT** (attribution verified by name only) | Phi Accrual is the Hayashibara et al. adaptive failure detector and Cassandra does use it (well-established). No primary fetch obtained for the Cassandra attribution specifically; framed as an example, low risk. | None needed; if desired, soften "e.g." remains accurate. |
| 6 | Time-of-day (wall) clock: synced by NTP, "can **jump backward** (when NTP corrects drift) or skip (leap seconds, DST)." Monotonic clock "only ever moves *forward* at a steady rate; its absolute value is meaningless… the *difference* between two readings is a trustworthy elapsed duration." Use wall for "when," monotonic for "how long." | **CORRECT** | Standard clock canon (DDIA Ch. 8, "Monotonic Versus Time-of-Day Clocks"); Go `time` docs confirm the monotonic/wall split (see row 8). The "wall can jump backward, monotonic only forward" distinction is exactly right. | None. |
| 7 | LWW with skewed clocks: "a causally *later* write can carry an *earlier* timestamp, so LWW silently drops the newer value." | **CORRECT** | DDIA Ch. 5 (LWW + clock skew → lost writes) and Ch. 8. Standard; the GoPlayground model is faithful. | None. |
| 8 | "As of Go 1.9, `time.Now()` captures **both** a wall-clock reading *and* a monotonic reading… `time.Since`/`Sub` uses the **monotonic** component… operations that strip the monotonic reading — `t.Round(0)`, marshaling to JSON, sending over the wire — leave only the wall clock." | **CORRECT** | pkg.go.dev/time (Monotonic Clocks): monotonic support added in Go 1.9; `Sub`/`Since` use the monotonic reading when present; `t.Round(0)` strips the monotonic reading; serialization/`Time` transmission drops it. (Verified against the documented Go `time` monotonic-clock semantics; consistent with the audit's Go-internals baseline.) | None. |
| 9 | "Google's Spanner faces this honestly with its **TrueTime** API, which returns `[earliest, latest]` instead of a single instant, and waits out the uncertainty before committing — so it never orders two transactions wrong." | **CORRECT** (interval+commit-wait per primary paper; not on reachable secondary pages) | Spanner OSDI 2012 (Corbett et al.): `TT.now()` returns an interval `[earliest, latest]` bounding absolute time; Spanner uses **commit-wait** (wait out ε) to guarantee external consistency. The modern Cloud docs page confirms only the *guarantee* — "Spanner provides external consistency, which is a stronger property than linearizability" — and that TrueTime "generate[s] monotonically increasing timestamps"; it does **not** restate the interval/commit-wait mechanism. The interval+wait detail is well-established from the primary paper but I could not re-confirm it via a clean fetch. | None (claim is accurate). Optional: cite the Spanner paper inline so the `[earliest, latest]`+commit-wait detail rests on the primary source rather than memory. |
| 10 | "even with the Green Tea GC (default in Go 1.26), stop-the-world pauses are workload-dependent, and the scheduler can park a goroutine." | **UNVERIFIABLE** (Green-Tea-as-default-in-1.26) | Per the audit guide's own landmine list: "The 1.25 *Green Tea* GC is an **opt-in experiment** — don't describe it as the default." I could not fetch go.dev/doc/go1.26 to confirm Green Tea became the *default* in 1.26. The directional point (STW pauses are workload-dependent; goroutines can be parked) is correct and uncontroversial. | Soften the Green-Tea-default attribution: e.g. "even Go's concurrent GC still has (brief, workload-dependent) stop-the-world pauses, and the scheduler can park a goroutine." Drop "(default in Go 1.26)" unless go1.26 release notes confirm Green Tea is the default — the landmine list flags exactly this over-claim. |
| 11 | Process pause: "A running process can be **frozen**… for an arbitrary length of time, *without noticing*. Causes: GC stop-the-world, VM suspended for live migration, OS swapping a page from disk, scheduler context-switch, a stray `SIGSTOP`." | **CORRECT** | DDIA Ch. 8 ("Process Pauses") lists exactly these causes (GC STW, VM live-migration suspend, paging/swap, context switches, SIGSTOP). Faithful. | None. |
| 12 | Lease + pause → split-brain: a leader checks "lease valid," is frozen 15s, lease expires, a new leader takes over and writes, then the frozen leader wakes "still believing 'lease: valid ✓'… and writes too. Two writers → corrupted file." | **CORRECT** | DDIA Ch. 8 ("The leader and the lock") gives this exact scenario, including the GC-pause-after-lease-check timeline. Faithful. | None. |
| 13 | Fencing token: "a number the lock/lease service returns with every grant, and it **increases every time** a lease is handed out… storage remembers the highest token and rejects any write with a lower one… (Same idea as Raft's *term number*, Paxos's *ballot*, Kafka's *epoch*.)" | **CORRECT** | DDIA Ch. 8 ("Fencing tokens") defines monotonically increasing tokens, storage rejecting lower tokens; explicitly relates them to ZooKeeper `zxid`/`cversion`. Raft term / Paxos ballot / Kafka epoch are the standard analogs. The GoPlayground/Lab model (reject token < highest) is correct. | None. (Minor: the prose says reject "lower," the code/Lab say reject `token < highest` — i.e. equal tokens accepted. Equal-token replays are out of scope for the basic fencing model; not a defect, since fencing assumes strictly increasing grants.) |
| 14 | HBase reference: "the exact mechanism (HBase's old bug, fixed) that keeps a paused-then-revived leader from clobbering data." | **CORRECT** (attribution) | DDIA Ch. 8 cites HBase's lack of fencing as the motivating real-world bug for fencing tokens. Matches the literature. | None. |
| 15 | Majority quorum: "agreement from **more than half** the nodes… there can only ever be *one* majority at a time, so two conflicting decisions can't both win… With 3 nodes you tolerate 1 failure; with 5, you tolerate 2." (Define, GoPlayground `votesDead > totalNodes/2`) | **CORRECT** | Quorum (en.wikipedia.org/wiki/Quorum_(distributed_computing)): write quorum rule "Vw > V/2" prevents two writes proceeding simultaneously; any two majorities overlap. 2f+1 nodes tolerate f crash failures (3→1, 5→2) is the standard crash-fault bound. `votesDead > totalNodes/2` is strict majority — correct (e.g. 3 of 5 true, 2 of 5 false). | None. |
| 16 | Byzantine fault: "A node that *lies*… is a **Byzantine fault**, and tolerating it (the *Byzantine Generals Problem*) is far more expensive… needed for aerospace and cryptocurrencies, but not inside Meridian's datacenter." | **CORRECT** | DDIA Ch. 8 ("Byzantine Faults") — exactly this framing (honest-but-faulty assumed; Byzantine = arbitrary/malicious; aerospace + untrusting-parties as the use cases; in-house datacenter trusts its nodes). Faithful. | None. |
| 17 | System model: "**partially synchronous** (well-behaved most of the time, occasionally not) with **crash-recovery** faults (nodes crash and may return, keeping disk but losing memory)"; safety = "nothing bad ever happens" (holds always), liveness = "something good eventually happens" (may wait for the network to recover). | **CORRECT** | DDIA Ch. 8 ("System Model and Reality," "Safety and liveness") defines the partially-synchronous model, crash-recovery faults, and the safety/liveness distinction with exactly this "always vs eventually" treatment. The safety/liveness classification is the Alpern–Schneider canon. | None. |
| 18 | Exercise 5 / Lab: classify "no two leaders simultaneously" and "fencing tokens unique" as **safety**, "a request eventually responds" as **liveness**; safety violations can be pinpointed to an instant and are required to hold even under total network failure, liveness may be conditioned on recovery. | **CORRECT** | Same DDIA "Safety and liveness" section; Alpern–Schneider: a safety violation has a finite witnessing prefix (a "bad moment"); liveness is "eventually." Classifications are correct. | None. |
| 19 | Conditional-write aside: "If your storage supports an atomic compare-and-set or 'conditional write' — S3, Azure Blob, GCS all do — you can get the same guarantee directly." | **CORRECT** (capability claim) | S3 (conditional writes / If-Match, ETag preconditions), Azure Blob (ETag/If-Match), GCS (generation-match preconditions) all support conditional/CAS writes — well-established provider features. Not separately fetched; low-risk capability statement. | None. |

### CORRECT (verified, not individually re-listed): 18

Rows 1–9 and 11–19 verified CORRECT against fetched/canonical sources. Only flagged downgrade: row 10 (Green-Tea-GC-as-1.26-default) — UNVERIFIABLE and contradicts the audit guide's own landmine note that Green Tea is an opt-in experiment.

## Sources fetched
- https://en.wikipedia.org/wiki/Two_Generals%27_Problem (timeout ambiguity; TCP confirms kernel-receipt only, not application completion)
- https://en.wikipedia.org/wiki/Quorum_(distributed_computing) (Vr+Vw>V; Vw>V/2 → only one write majority)
- https://docs.cloud.google.com/spanner/docs/true-time-external-consistency (TrueTime → external consistency "stronger than linearizability"; interval/commit-wait mechanism NOT restated on this page)
- https://en.wikipedia.org/wiki/Spanner_(database) (TrueTime brand / GPS+atomic clocks; interval/commit-wait detail NOT present)
- pkg.go.dev/time monotonic-clock semantics (Go 1.9 dual reading; Since/Sub use monotonic; Round(0)/serialization strip it) — cross-checked against the audit's Go baseline

## Tally
Flagged: 1 (1 UNVERIFIABLE downgrade) + 18 verified CORRECT. Worst finding: **"the Green Tea GC (default in Go 1.26)" overstates Green Tea's status — the audit guide's own landmine list says Green Tea is an opt-in experiment, not the default; this attribution is unverifiable against a fetched go1.26 release note and should be softened (drop "default in Go 1.26").** The distributed-systems core of the chapter (timeout ambiguity, TCP's limits, monotonic-vs-wall clocks, fencing tokens, majority quorums, safety/liveness) is uniformly accurate.

Frontmatter unchanged: yes (REPORT ONLY — no edits to the content file).
