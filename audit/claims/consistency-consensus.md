# Accuracy audit — consistency-consensus

Chapter: `content/part-2/24-consistency-consensus.mdx`
Tier 2 (correctness-critical, highest-stakes: CAP/PACELC, linearizability vs serializability vs sequential consistency, Lamport clocks, consensus + FLP + Raft, quorum). REPORT ONLY — no content file was edited.

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (not applied) |
|---|---|---|---|---|
| 1 | **Linearizability** "makes a replicated system behave as if there were a single copy of the data and every operation on it were instantaneous… a **recency guarantee**: the moment a write completes, every subsequent read — on any client, any replica — must return that value (or a newer one), never an older one." | **CORRECT** | Jepsen (jepsen.io/consistency/models/linearizable): "one of the strongest single-object consistency models"; operations "consistent with the real-time ordering." Linearizability (en.wikipedia.org/wiki/Linearizability): real-time order preserved — "If an operation op1 completes before op2 begins, then op1 precedes op2." Recency/single-copy framing is exactly right. | None. Core definition is correct. |
| 2 | Recency rule: "once **any** read has observed the new value, **every** later read must observe it too. There's an imaginary instant where the value atomically flips, and nothing may read 'backward' across it." (ExecTimeline, GoPlayground `violatesRecency`, Exercise 1) | **CORRECT** | Linearizability article: linearization points — "every operation appears to take place atomically… at specific moments"; real-time order forbids a later-starting op from observing a stale value once a newer one is visible. The `b.start > a.end && b.value < a.value` violation check is a faithful formalization. | None. |
| 3 | "**Linearizability** is NOT **serializability**… Serializability… is about *transactions over multiple objects* behaving like *some* serial order — it can put that order anywhere in time, and even allows stale reads. Linearizability is about *single objects* and *real-time recency*… A database can offer one, the other, both (that combo is 'strict serializability,' like Spanner and FoundationDB), or neither." | **CORRECT** | Linearizability article: real-time ordering "is the last point which is unique to linearizability"; "Serializability allows reordering even when one operation's response preceded another's invocation, whereas linearizability forbids this." Jepsen: for multi-object real-time, use "strict serializability." Strict-serializability = serializability + linearizability is the standard Jepsen/Herlihy framing; Spanner (external consistency) and FoundationDB are correct exemplars. | None. The single-object-vs-multi-object and recency-vs-some-serial-order distinctions are precisely the canonical ones. |
| 4 | "Single-leader can [deliver linearizability] (*if* you always read/write the real leader — but a deluded zombie leader breaks it). Consensus algorithms can… Multi-leader and leaderless generally cannot — concurrent writes on multiple nodes, and even quorum reads (`w + r > n`) can race into a non-linearizable result." | **CORRECT** | DDIA Ch. 9 ("Implementing Linearizable Systems"): single-leader is *potentially* linearizable (reads from leader/sync-replica); consensus is linearizable; multi-leader and leaderless are not; "it is possible… to have nonlinearizable behavior with Dynamo-style quorums" even with w+r>n (read/write timing + concurrent writes). Matches exactly. | None. The "quorum reads can still be non-linearizable" nuance is a frequently-botched point stated correctly here. |
| 5 | **CAP** (Define + Callout): during a **network partition** a system must choose **CP** (refuse on replicas that can't reach majority → unavailable there) or **AP** (serve independently → give up linearizability). "'Pick two of three' is a misleading slogan… The honest statement: when the network is healthy you can have both; when it partitions you must choose consistency or availability." | **CORRECT** | CAP theorem (en.wikipedia.org/wiki/CAP_theorem): on partition "cancel the operation… ensure consistency; [or] proceed… provide availability but risk inconsistency." Brewer (2012): "the often-used 'two out of three' concept can be somewhat misleading because system designers only need to sacrifice consistency or availability in the presence of partitions." "In the absence of a network partition, both availability and consistency can be satisfied." This is the precise statement the audit brief demanded (consistency-or-availability *under partition*, NOT loose "pick 2 of 3"). | None. This is the highest-risk claim in the chapter and it is correct and precisely framed. |
| 6 | "formal CAP only covers linearizability and partitions — it says nothing about ordinary delays. The deeper, everyday cost (**Attiya–Welch**) is that linearizable reads and writes are inherently slow… latency proportional to network delay uncertainty, all the time… (PACELC: CP/AP under partition, else Latency-vs-Consistency.)" | **CORRECT** (concept) / attribution by name only | PACELC (en.wikipedia.org/wiki/PACELC_design_principle): "even when the system is running normally in the absence of partitions, one has to choose between latency (L) and loss of consistency (C)"; "requiring a full network round trip and therefore message delay on both reads and writes to ensure consistency." Abadi: consistency/latency trade-off "is present at all times during system operation, whereas CAP is only relevant in the… rare case of a network partition." The **Attiya–Welch** name is NOT on the PACELC page, but the result (linearizable ops cost ∝ delay uncertainty) is theirs (1994) and is the standard DDIA Ch. 9 citation. | None required (correct). Optional: since "Attiya–Welch" wasn't on the fetched page, it could be hedged as "(Attiya & Welch, 1994)" or "a classic result" — but the claim itself is right and PACELC corroborates the substance. |
| 7 | CAP framing in BeforeAfter / Exercise 4: CP refuses writes when it "can't reach the majority" (`return ErrUnavailable`), AP "serves write anyway"; money picks CP ("Down > wrong"). | **CORRECT** | CAP article (partition → choose); DDIA Ch. 9. The CP=refuse-on-minority / AP=serve-anyway dichotomy and the "money is CP" engineering choice are standard and correct. | None. (Note: CP/AP is a coarse classification — real systems can be CP for some ops and AP for others — but the chapter teaches it as a per-decision trade-off, which is defensible and matches DDIA.) |
| 8 | **Lamport timestamp** `(counter, nodeID)`: "each node increments its counter on every event, and whenever it sees a higher counter from another node, it jumps its own up to match before continuing… if A *happened-before* B then A's timestamp < B's — a **total order consistent with causality**… Ties break by nodeID, so every timestamp is unique." (GoPlayground `tick`/`receive`, Exercise 2) | **CORRECT** | Lamport timestamp (en.wikipedia.org/wiki/Lamport_timestamp): "increments its counter before each local event"; on receive "updated… to the greater of its current counter and the timestamp in the received message. The counter is then incremented by 1"; "if a → b then C(a) < C(b)"; "To break ties, we use any arbitrary total ordering… of the processes." The `receive` code (`if remote > counter { counter = remote }; counter++`) is exactly max-then-increment. | None. |
| 9 | "Lamport clocks give a total order but **not** linearizability… They also can't tell *concurrency* apart from ordering (for that you need **vector clocks**)." | **CORRECT** | Lamport article: the consistency condition "only goes one way" (a<b does NOT imply happened-before), so Lamport clocks "do not capture all causality"; vector clocks are the "more advanced" method to detect concurrency. Linearizability is a recency guarantee Lamport order cannot provide (DDIA Ch. 9: total order broadcast ≠ linearizability). Correct. | None. |
| 10 | "A **hybrid logical clock** (used by **CockroachDB**) bolts a Lamport counter onto a physical timestamp." | **CORRECT** (attribution) | HLC (Kulkarni et al. 2014) = physical time + Lamport-style logical component; CockroachDB documents using HLC. Well-established; not separately fetched, low risk. | None. |
| 11 | UnderTheHood: "On one box, `atomic.CompareAndSwapInt64` *is* linearizable… As of Go 1.26, prefer the typed atomics (`atomic.Int64`, `atomic.Pointer[T]`, Go 1.19+) over the bare functions." | **CORRECT** | pkg.go.dev/sync/atomic: typed atomic types (`atomic.Int64`, `atomic.Pointer[T]`, etc.) added in Go 1.19; docs recommend the types over the bare functions. Hardware CAS providing a single linearizable flip on one machine is correct (the Go memory model gives the happens-before). Consistent with the audit's Go baseline. | None. (Cross-machine caveat — "no amount of `sync/atomic` crosses a network" — is correct.) |
| 12 | "**etcd** — the consensus store behind Kubernetes… is written in Go and implements **Raft**." | **CORRECT** | etcd is Go and implements Raft; it is Kubernetes' backing store. Well-established; corroborated by Raft project ecosystem. Not separately fetched, low risk. | None. |
| 13 | **Consensus** properties (Define): "**agreement** (no two nodes decide differently), **integrity** (no node changes its decision), **validity** (the decided value was actually proposed), and **termination** (every non-crashed node eventually decides)." First three = safety; termination = liveness, needs a majority. | **CORRECT** (per DDIA labeling) / terminology varies in the literature | This is **Kleppmann/DDIA Ch. 9**'s exact labeling: uniform agreement, integrity (a node decides at most once / no take-backs), validity (decided value was proposed by some node), termination. The audit guide names DDIA as authoritative. NOTE the divergence: the Wikipedia *Consensus* article uses different labels — there "integrity" = "if all correct processes proposed v then any decides v" and "validity" = "a message sent is delivered." Atomic broadcast (en.wikipedia.org/wiki/Atomic_broadcast) gives "Uniform Integrity: a message is received… at most once, and only if previously broadcast" (= "no node decides twice"), and notes authors "define the integrity and validity properties… slightly differently." So the chapter's labels are correct against DDIA but not against the Wikipedia consensus article. Safety/termination=liveness split is universally agreed. | None required (it matches the named authoritative source, DDIA). Optional defensive note: add a one-line "(terminology follows Kleppmann/DDIA; other texts label these differently)" so a reader who cross-checks against Wikipedia isn't confused. This is the only genuine terminology landmine in the chapter. |
| 14 | "the most useful packaging of consensus is a **shared, append-only log** — also called **total order broadcast**: every node sees the same entries in the same order… gives you *state machine replication*… Single-value agreement, atomic **compare-and-set**, a **shared log**… and **atomic commit**… are interchangeable… CAS and shared logs have 'consensus number ∞'." | **CORRECT** | DDIA Ch. 9 ("Total Order Broadcast," "Consensus equivalence"): total order broadcast ⇔ consensus ⇔ linearizable CAS ⇔ atomic commit; state-machine replication via same-order log. Herlihy's wait-free hierarchy: CAS has **consensus number ∞**, FIFO queues/registers are finite — correct (the "shared log has ∞" is true because it is built from / equivalent to consensus). | None. The CAS-consensus-number-∞ claim is correctly stated (a frequent place to get the number wrong). |
| 15 | Lab "Compare-and-set IS consensus": a linearizable CAS from the null state decides exactly one value (first proposer wins) and every later proposer learns that value → satisfies agreement/integrity/validity/termination. | **CORRECT** | DDIA Ch. 9 (CAS ⇔ single-value consensus); Herlihy (CAS solves n-process consensus). The single-decider register is the standard pedagogical reduction. The Lab's caveat — "the hard part real systems solve is making this CAS survive the node holding it crashing — that's what Raft adds" — correctly identifies fault tolerance as the missing piece. | None. |
| 16 | Odd node counts / quorum: "you always run an **odd** number of nodes (3 or 5)… a decision needs a strict majority, and only one majority can exist… With 3 nodes you survive 1 failure; with 5, two. …4 nodes survive the same as 3 (1) — so odd counts give the best failure tolerance per machine." (GoPlayground `canDecide(reachable, total) = reachable > total/2`) | **CORRECT** | Quorum (en.wikipedia.org/wiki/Quorum_(distributed_computing)): "Vw > V/2" → only one write majority; two majorities of >N/2 must overlap. 2f+1 tolerates f (3→1, 5→2); 4 nodes need 3 for majority so still tolerate only 1 — odd counts are optimal per machine. `reachable > total/2` is strict majority (3 of 5 true, 2 of 3 true, 1 of 3 false). All correct. | None. |
| 17 | Quorum overlap (Exercise 5): "any two subsets each containing more than half of N must **overlap** in at least one node — and that shared node can't have voted for two different decisions." | **CORRECT** | Quorum article / pigeonhole: two sets each >N/2 of N elements must intersect. Standard quorum-intersection argument. Correct. | None. |
| 18 | **FLP** (Callout): "FLP only proves you can't *guarantee termination* in a purely asynchronous model with **no clocks or timeouts**. Real systems do have timeouts… enough to make consensus solvable in practice." | **CORRECT** | Consensus article (en.wikipedia.org/wiki/Consensus_(computer_science)): FLP shows "a deterministic algorithm for achieving consensus is impossible" in a "fully asynchronous" model where "at least one process may crash," and "concerns guaranteeing termination"; the worst case "is unlikely to occur in practice except in adversarial situations." Chapter's "can't *guarantee termination*… asynchronous… no clocks/timeouts" is precise (FLP is about termination/liveness, not safety; it assumes determinism + ≥1 crash). | None. The frequently-mangled subtleties (it's about *termination* not impossibility-of-everything; needs a *deterministic* algorithm + ≥1 crash + full asynchrony) are all stated correctly. |
| 19 | **Raft** (Callout + Scene): "uses randomized election timeouts so that when a leader goes silent, one follower times out first, becomes a candidate, and wins a majority vote." Term number monotonically increases and IS the fencing token; a stale leader's writes (term 7 < 8) are rejected by replicas. | **CORRECT** | Raft (en.wikipedia.org/wiki/Raft_(algorithm)): "Raft uses a randomized election timeout… a single server will time out, win the election, then become leader and send heartbeat messages… before any of the followers can become candidates." Candidate "increase[s] the term counter, vot[es] for itself… requesting their vote"; "If a candidate receives a majority of votes, then it becomes the new leader." Term acts as logical clock; "if a candidate receives a message… with a term number larger than the candidate's current term, then the candidate's election is defeated" — i.e. higher term wins, stale term rejected (the fencing behavior). raft.github.io: "a cluster of 5 servers can continue to operate even if 2 servers fail." All claims confirmed. | None. Leader election, randomized timeouts, term-as-fencing-token, and 5→tolerate-2 are all correct. |
| 20 | Exercise 5 / dictator: "A single hardcoded 'dictator' node satisfies agreement/integrity/validity but fails **termination**" (if it crashes, nobody decides). | **CORRECT** | Standard result (DDIA Ch. 9): a single fixed coordinator is safe but not fault-tolerant — its crash blocks termination forever; that is precisely why consensus (majority-based) is hard. Termination = the liveness property requiring a live majority. Correct. | None. |
| 21 | "**never implement consensus yourself**… Use etcd, ZooKeeper, or a database that embeds Raft (CockroachDB, TiDB, YugabyteDB)." Keep consensus small; run high-volume writes on single-leader Postgres, use etcd only to elect the leader/guard the lease. | **CORRECT** (engineering canon) | DDIA Ch. 9 ("don't roll your own"; coordination services hold small high-stakes metadata); CockroachDB/TiDB/YugabyteDB do embed Raft. Standard senior-engineering guidance; not separately fetched, low risk. | None. |

### CORRECT (verified, not individually re-listed): 21

All 21 flagged rows verified CORRECT against fetched/canonical sources. The one notable nuance (row 13) is a *terminology-convention* divergence, not an error: the chapter follows Kleppmann/DDIA's labels (the audit's named authority), which differ from the Wikipedia consensus article's labels. No WRONG, no UNVERIFIABLE downgrade required.

## Sources fetched
- https://en.wikipedia.org/wiki/CAP_theorem (partition → consistency-or-availability; Brewer "two of three is misleading"; both satisfiable absent a partition)
- https://en.wikipedia.org/wiki/Linearizability (real-time/recency single-object guarantee; the real-time point "is unique to linearizability" vs serializability)
- https://jepsen.io/consistency/models/linearizable (single-object real-time model; strict serializability for multi-object; sequential consistency contrast)
- https://en.wikipedia.org/wiki/Consensus_(computer_science) (FLP: deterministic impossibility, fully async, ≥1 crash, about termination; consensus property labels — terminology variation)
- https://en.wikipedia.org/wiki/Atomic_broadcast (uniform agreement/integrity/validity/total order; "authors define integrity and validity slightly differently")
- https://en.wikipedia.org/wiki/Raft_(algorithm) (randomized election timeouts; term counter; majority vote; higher-term rejection)
- https://raft.github.io/ (Raft ≈ Paxos; 5 servers tolerate 2 failures)
- https://en.wikipedia.org/wiki/Lamport_timestamp (increment-before-event; max-then-increment on receive; one-way clock consistency; tie-break by process; vector clocks for concurrency)
- https://en.wikipedia.org/wiki/Quorum_(distributed_computing) (Vr+Vw>V; Vw>V/2 → single write majority)
- https://en.wikipedia.org/wiki/PACELC_design_principle (P→A/C, Else→L/C; consistency needs a full round trip; Abadi attribution)
- https://docs.cloud.google.com/spanner/docs/true-time-external-consistency (external consistency "stronger than linearizability")

## Tally
Flagged: 0 WRONG / 0 UNVERIFIABLE; 21 verified CORRECT (1 terminology-convention note on row 13). Worst finding: **none rises to an error.** The closest is the consensus-property labeling (row 13): the chapter's "integrity = no node changes its decision, validity = decided value was proposed" matches Kleppmann/DDIA (the named authoritative source) but diverges from the Wikipedia *Consensus* article's labels — recommend a one-line "(terminology follows DDIA)" hedge so a cross-checking reader isn't tripped. Every load-bearing claim the brief flagged as highest-risk — CAP's precise under-partition statement (not "pick 2 of 3"), PACELC, FLP (termination-only, async, deterministic, ≥1 crash), Raft (randomized timeouts / term-as-fencing-token / 5-tolerate-2), linearizability-vs-serializability, Lamport-vs-vector clocks, and R+W>N quorum overlap — is stated correctly.

Frontmatter unchanged: yes (REPORT ONLY — no edits to the content file).
