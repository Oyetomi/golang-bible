# Accuracy audit — event-driven-kafka

Tier-2 (correctness-critical). Chapter file: `content/part-3/07-event-driven-kafka.mdx`.
Mode: REPORT ONLY — no content file was edited. Fixes below are **proposed**, not applied.

Focus areas per brief: Kafka semantics (partitions, offsets, consumer groups, rebalancing, partition-key ordering, ISR/acks, retention), kafka-go API shapes, Go-version attributions.

| # | Claim (as written, location) | Verdict | Source (quoted) | Proposed action |
|---|---|---|---|---|
| 1 | "`segmentio/kafka-go` is straightforward and widely used; **it supports the cooperative-sticky protocol**." (UnderTheHood, ~line 667) | **WRONG** | kafka-go `groupbalancer.go` provides only **RangeGroupBalancer**, **RoundRobinGroupBalancer**, **RackAffinityGroupBalancer**: "kafka-go does not implement a cooperative-sticky assignor … neither cooperative nor sticky variants are present." | Remove the cooperative-sticky claim for kafka-go. Proposed: "`segmentio/kafka-go` supports range, round-robin and rack-affinity group balancers (eager rebalancing); it does **not** implement the cooperative-sticky assignor. For cooperative-sticky / KIP-848 use `twmb/franz-go`." This also undercuts the Scene frame (line 73) and `ExecTimeline` (line 696) that animate "cooperative-sticky rebalance" while the chapter's client is kafka-go — see #2. |
| 2 | Scene frame: "The coordinator moves P2 from B to C … without interruption (**cooperative-sticky rebalance**)." (line 73); rebalance `ExecTimeline` step "revoke p0 (**cooperative**)" / caption "**Cooperative-sticky rebalancing**" (lines 696–701) | **IMPRECISE** | Cooperative-sticky is real (Confluent: "the CooperativeStickyAssignor works in a two-step process … partitions that are not revoked can continue to be processed"), but it is **not** what kafka-go (this chapter's client) does. The animation depicts a mechanism the chosen client can't perform. | Add a one-line "simplified — depicts the cooperative-sticky strategy, available via `franz-go` / the Java client, not kafka-go's eager balancers" note, OR reframe the prose so the animation isn't tied to the kafka-go examples. |
| 3 | "The **modern default** is **cooperative-sticky**." (UnderTheHood, ~line 665); Define: "The classic 'eager' strategy … **is now deprecated** for most use cases." (line 676) | **IMPRECISE / OUTDATED-framing** | Confluent consumer-config: default `partition.assignment.strategy` = "RangeAssignor,CooperativeStickyAssignor" and "RangeAssignor is the active default … allows upgrading to the CooperativeStickyAssignor with just a single rolling bounce." So the out-of-the-box **active** default is RangeAssignor, with CooperativeStickyAssignor present only as an upgrade fallback; eager is not formally "deprecated." | Soften: "The default `partition.assignment.strategy` lists `[RangeAssignor, CooperativeStickyAssignor]`; RangeAssignor is the active default, and you opt into incremental cooperative rebalancing by dropping RangeAssignor from the list. Eager (range/round-robin) is still fully supported, not deprecated." |
| 4 | "This mirrors what **kafka-go's Hash balancer does internally**." — `partitionForKey` uses `int(h.Sum32()) % numPartitions` (GoPlayground, ~line 785–790) | **IMPRECISE** | kafka-go `balancer.go` Hash.Balance: `partition := int32(hasher.Sum32()) % int32(len(partitions)); if partition < 0 { partition = -partition }`. The real code casts to **int32** (so the sign bit matters) then **negates** negatives. The chapter casts to `int` (64-bit on common platforms), where `int(uint32)` is always non-negative — a *different* result for any key whose FNV-1a hash has the high bit set (~half of keys). | Note the difference, or match kafka-go: `p := int32(h.Sum32()) % int32(numPartitions); if p < 0 { p = -p }`. Same-key determinism (the property the text leans on) still holds either way — that part is correct. Default hasher is fnv-1a: **CORRECT** ("By default, Hash uses the FNV-1a algorithm"). |
| 5 | "`RequireAll` (acks=-1) means the leader waits for **all in-sync replicas (ISR)** to acknowledge … only setting that guarantees durability." (Callout, line 750); Idempotent-producer Define "Requires `acks=all`" (line 707) | **CORRECT** | Confluent producer-configs: acks=all "means the leader will wait for the full set of in-sync replicas to acknowledge the record. This guarantees that the record will not be lost as long as at least one in-sync replica remains alive. This is the strongest available guarantee. This is equivalent to acks=-1"; and "enabling idempotence requires this config value to be 'all'." | Leave as written. |
| 6 | Idempotent producer: "producer ID (PID) … tracking a **sequence number per partition** … broker recognizes the duplicate PID + sequence-number pair and discards it." (Define, line 707; UnderTheHood code comment) | **CORRECT** (could not fetch the exact enable.idempotence text — apache.org page is JS-rendered/unreachable — but corroborated) | Confluent producer-configs confirms enable.idempotence requires acks=all; PID + per-partition sequence-number dedup is the documented mechanism. Marked verified via Confluent + producer-config corroboration; the apache.org anchor itself returned only a redirect/nav shell. | Leave; mechanism description is accurate. (Light downgrade only if you want strict primary-source pinning — the apache.org doc could not be fetched directly.) |
| 7 | Delivery-semantics card: "**exactly-once (EOS)** … correct for financial message flows" and UnderTheHood: Kafka transactions "do **not** automatically include an external Postgres write … the pragmatic fintech pattern is still at-least-once consumption plus an idempotent DB update." (lines 715, 981) | **CORRECT** | Matches the landmine guidance (effectively-once = at-least-once + idempotent processing across the external boundary). The chapter explicitly scopes Kafka EOS to within-Kafka read-process-write and frames the DB boundary as at-least-once + idempotent — the correct framing. | Leave. This is the well-handled case the brief flags as a common failure. |
| 8 | "Ordering is guaranteed within a partition … Messages across different partitions have no ordering relationship." (Define line 573; ConceptGrid Gotcha line 588) | **CORRECT** | Confluent partitions course: "Within a single partition, message order is strictly maintained—messages are read in the exact sequence they were written," and global ordering across partitions is "not guaranteed." | Leave. |
| 9 | Offset: "monotonically increasing integer position of a message within a partition … resumes from its committed offset." (Define line 576; ConceptGrid line 587) | **CORRECT** | Confluent consumers course: "Kafka tracks the offset of each consumed message," offsets "are committed back to Kafka itself, stored in an internal topic," and on restart "it reads its last committed offset and continues processing without missing data." | Leave. |
| 10 | Consumer group: "each partition is assigned to **at most one consumer** in the group … excess consumers sit idle." (Define line 614; rules line 620) | **CORRECT** | Confluent consumers course: "Kafka assigns each partition to one consumer in the group—no two consumers in the same group read from the same partition" → more consumers than partitions ⇒ some idle. | Leave. |
| 11 | "`__consumer_offsets` … keyed by `(group, topic, partition)` … compacted topic — only the latest offset per key is retained." (UnderTheHood, line 663) | **CORRECT** | Confluent consumers course confirms offsets "are committed back to Kafka itself, stored in an internal topic" (`__consumer_offsets`); log-compaction keyed by (group,topic,partition) is the documented design. | Leave. |
| 12 | Key→partition: "`partition = hash(key) % numPartitions`" (Define Partition Key, line 757; ExecTimeline "hash(ACC-001) % 3", line 600) | **CORRECT** (as a model) | Confluent partitions course: "Kafka hashes the key, applies modulo with the number of partitions, and assigns it accordingly … Messages with the same key are always written to the same partition." | Leave. Caveat: the **Java default** partitioner uses **murmur2**, not FNV; the chapter's own runnable example uses FNV (kafka-go). The `% numPartitions` model is right; only the int-vs-int32 detail in #4 is off. |
| 13 | "range-over-int: Go 1.22+" (`for p := range numPartitions`, line 810; line 1232) and "Go 1.22+ … `range` loop variable is per-iteration by default" (UnderTheHood line 323; Exercise 4 note line 1257) | **CORRECT** | go.dev/doc/go1.22: "In Go 1.22, each iteration of the loop creates new variables, to avoid accidental sharing bugs"; and "'For' loops may now range over integers. For example: `for i := range 10`." | Leave. |
| 14 | "`log/slog` // Go 1.21+ structured logging" (import comment, line 871) | **CORRECT** | go.dev/doc/go1.21: "The new log/slog package provides structured logging with levels." | Leave. |
| 15 | kafka-go API surface used in code: `kafka.NewReader(kafka.ReaderConfig{Brokers,Topic,GroupID,MinBytes,MaxBytes})`, `reader.FetchMessage(ctx)`, `reader.CommitMessages(ctx,msg)`, `kafka.Writer{Addr,Topic,Balancer,RequiredAcks,MaxAttempts,Compression}`, `kafka.TCP`, `kafka.RequireAll`, `kafka.Hash{}`, `kafka.Snappy` (lines 731–745, 903–968) | **CORRECT** | pkg.go.dev/github.com/segmentio/kafka-go confirms all of these: `NewReader(config ReaderConfig) *Reader`, `FetchMessage(ctx) (Message, error)`, `CommitMessages(ctx, msgs ...Message) error`, Writer fields `Addr/Topic/Balancer/Compression/RequiredAcks/MaxAttempts`, `TCP(...)`, `Hash` balancer. | Leave. API names are accurate. |

## Tally

- Claims examined (falsifiable, load-bearing): **15**
- **WRONG: 1** (#1 — kafka-go does not support cooperative-sticky)
- **IMPRECISE: 3** (#2 animation tied to wrong client; #3 cooperative-sticky-as-default + "eager deprecated"; #4 int vs int32 in the Hash mirror)
- **OUTDATED: 0** (the #3 framing is folded into IMPRECISE)
- **UNVERIFIABLE: 0** (apache.org anchor could not be fetched, but every Kafka semantic claim was confirmed via Confluent's authoritative course material + Confluent platform config docs; noted on #6)
- **CORRECT (silently confirmed, not all listed): 11** (#5–#15)

**Worst finding:** #1 — the chapter states `segmentio/kafka-go` "supports the cooperative-sticky protocol," but kafka-go ships only eager Range/RoundRobin/RackAffinity balancers and has no cooperative-sticky assignor. This is load-bearing because the rebalance Scene and ExecTimeline both animate a cooperative-sticky rebalance as if the chapter's chosen client performs it.

## Sources fetched (actually opened)

- https://go.dev/doc/go1.22 — loop-var per-iteration; range-over-int; math/rand/v2 added 1.22
- https://go.dev/doc/go1.21 — log/slog added in 1.21
- https://pkg.go.dev/github.com/segmentio/kafka-go — Reader/Writer/ReaderConfig/FetchMessage/CommitMessages/Hash/RequiredAcks/TCP/Snappy
- https://github.com/segmentio/kafka-go/blob/main/balancer.go — Hash balancer: int32 cast + negate, fnv-1a default
- https://github.com/segmentio/kafka-go/blob/main/groupbalancer.go — Range/RoundRobin/RackAffinity only; NO cooperative-sticky
- https://developer.confluent.io/courses/apache-kafka/partitions/ — ordering within partition; key hash % numPartitions
- https://developer.confluent.io/courses/apache-kafka/consumers/ — one consumer per partition per group; offsets in internal topic
- https://developer.confluent.io/courses/architecture/data-replication/ — ISR definition
- https://developer.confluent.io/courses/architecture/consumer-group-protocol/ — eager (stop-the-world) vs CooperativeStickyAssignor
- https://docs.confluent.io/platform/current/installation/configuration/producer-configs.html — acks=0/1/all semantics; enable.idempotence requires acks=all
- https://docs.confluent.io/platform/current/installation/configuration/consumer-configs.html — default partition.assignment.strategy = [RangeAssignor, CooperativeStickyAssignor], RangeAssignor active default
- (attempted, unreachable — JS redirect shell only) https://kafka.apache.org/documentation/ and several `#`-anchored variants and https://kafka.apache.org/40/documentation.html

Frontmatter unchanged: yes (no file edited — report only).
