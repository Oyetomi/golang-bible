# Accuracy audit — functional-go

Chapter: `content/part-1/06-functional-go.mdx`
Mode: REPORT ONLY (no edits applied). Audited every falsifiable claim: closures + escape analysis, the Go 1.22 loop-var fix, range-over-func iterators (Go 1.23, `iter.Seq`), no-HKT, no-TCO, and the **generic-methods "~Go 1.27" preview** (the prompt specifically asks whether this is correctly hedged as accepted-but-not-shipped).

The functional mechanics are accurate. The Go 1.22 loop-variable fix and Go 1.23 `iter.Seq` range-over-func attributions are both CORRECT against primary sources. The no-higher-kinded-types and no-tail-call-optimization claims are correct. The generic-methods callout is **carefully and correctly hedged** as a preview not in 1.26 — but its *specific factual asserts* (accepted "March 2026", issue #75526, spec issue #77273, "~Go 1.27") could not be certified from a fetched source and are downgraded to UNVERIFIABLE. No WRONG mechanical findings.

## Audit log

| # | Claim (as written) | Verdict | Source (fetched) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | "in Go, a function is a **value**. You can store it in a variable, pass it as an argument, return it … and stuff it in a slice or map." | CORRECT | go.dev/ref/spec, Function types: function values are first-class; assignable, passable, returnable, storable. | none |
| 2 | "A **closure** … captures variables from the scope it was defined in. The captured variables live on past the enclosing function's return." | CORRECT | go.dev/ref/spec, Function literals: "Function literals are closures: they may refer to variables defined in a surrounding function. Those variables are then shared between the surrounding function and the function literal, and they survive as long as they are accessible." | none |
| 3 | "Before Go 1.22, every closure created in a `for` loop captured the *same* loop variable … As of **Go 1.22** each iteration gets a fresh copy of the loop variable." | CORRECT | go.dev/doc/go1.22: "Previously, the variables declared by a 'for' loop were created once and updated by each iteration. In Go 1.22, each iteration of the loop creates new variables, to avoid accidental sharing bugs." | none — correctly pinned to 1.22. |
| 4 | "if you read older code that does `v := v` at the top of a loop body, that line was the pre-1.22 workaround — now redundant." | CORRECT | Standard pre-1.22 idiom; with per-iteration variables in 1.22+ the shadow copy is a no-op. | none |
| 5 | UnderTheHood: "a closure value is a small struct: a **pointer to the function's code** plus pointers to the **captured variables**." | CORRECT | Matches the gc closure representation (funcval: code pointer + captured-variable storage). Captured vars that escape are heap-boxed and referenced by pointer. | none |
| 6 | "When a function literal captures a variable whose lifetime must outlive the enclosing function … escape analysis moves that variable from the stack to the **heap**." | CORRECT | go.dev/doc/gc-guide + escape-analysis behavior: captured variables that escape are heap-allocated. Chapter correctly says "may" / "can allocate," not always. | none |
| 7 | "Run `go build -gcflags=-m` and you'll see lines like `moved to heap: count` and `func literal escapes to heap`." | CORRECT | `-gcflags=-m` prints exactly these escape-analysis diagnostics. Reproducible. | none |
| 8 | "`compose(f, g)(x)` means `f(g(x))`" (Define) vs in-code "compose(a,b,c)(x) == c(b(a(x)))" left-to-right | CORRECT (internally consistent, two conventions) | Both are valid composition orders; the Define uses standard right-to-left math notation, the code comment defines its own left-to-right fold and implements it that way (loop applies fs in order). The implementation matches its own stated order. Not a defect — just two conventions, each self-consistent. | Optional: note the Define's `f(g(x))` is the math convention while the `compose` impl folds left-to-right, to avoid reader confusion. |
| 9 | "Go can't write a single variadic `compose` that threads an arbitrary chain of *different* types — that needs higher-kinded types, which Go doesn't have." | CORRECT | go.dev/blog/intro-generics + spec: Go type parameters range over concrete types; you cannot abstract over an arbitrary type-level chain variadically. A fixed-arity `compose2/compose3` is the workaround. | none |
| 10 | "A combinator … with no free variables of its own beyond its function arguments." | CORRECT | Standard combinator definition (a function with no free variables). The chapter's `And/Or/Not/Chain` examples fit. | none |
| 11 | "HTTP middleware — `func(http.Handler) http.Handler` — is **function composition**. Chaining middleware is a combinator." | CORRECT | go.dev `net/http`: `http.Handler` interface; the `func(Handler) Handler` wrapper pattern is composition. `Chain(a,b,c)(h) == a(b(c(h)))` matches the loop (reverse iteration). | none |
| 12 | "`Map` and `Filter` over a slice each allocate a **new slice**. Chain them and every stage allocates an intermediate." | CORRECT | The shown `Map`/`Filter` impls call `make([]T, …)` — one allocation per stage. Self-evident from the code. | none |
| 13 | "Go 1.23 added **range-over-function** iterators … `iter.Seq[T]` is `func(yield func(T) bool)` (Go 1.23+)." | CORRECT | go.dev/blog/range-functions: "Range over function types is a new language feature in the Go 1.23 release." And `package iter; type Seq[V any] func(yield func(V) bool)`. | none — both the version and the exact type signature match. |
| 14 | UnderTheHood: "The compiler rewrites your loop body into a function — the `yield` — and *calls your iterator*, passing that function in. … When `yield` returns `false` … the iterator sees it and stops." | CORRECT | go.dev/blog/range-functions: "the compiler must be adjusting the loop to create a yield function to pass to the iterator" and "If the yield function returns false, no more values are needed, and the iterator can just return." | none |
| 15 | "a lazy `Map`→`Filter`→`take(10)` over a billion-row source touches only as many elements as needed and allocates no intermediate slices" | CORRECT | Inherent to pull-driven range-over-func; the `MapSeq`/`FilterSeq`/`TakeSeq` impls shown thread one element through with early termination via `yield`'s bool. | none |
| 16 | Code comment: "Go 1.23+. … the embedded sandbox may target an older toolchain, so this one is shown rather than run inline." | CORRECT (operational note) | Consistent with the Codapi/playground constraints in `_ACCURACY_AUDIT.md` (runner may be ≤1.24). `iter` requires ≥1.23. | none |
| 17 | "A method can't add its own type parameter in Go, so transforms across types are free functions" / compiler says "`method must have no type parameters`" | CORRECT (for Go ≤ 1.26) | github.com/golang/go/issues/75526 body: the proposal targets exactly the current error "method must have no type parameters." Verified the issue is "proposal: allow type parameters on methods (generic methods)." So today a method cannot declare its own type parameter — accurate for the 1.26 baseline. | none — accurate for the shipped language. |
| 18 | "You cannot write a generic `Monad` interface in Go — no higher-kinded types … a type parameter must be a concrete type, never an unapplied generic." | CORRECT | go.dev/blog/intro-generics + spec: Go has no higher-kinded types; you cannot parameterize over a type constructor `M[_]`. The `Monad`/`Self[B]` interface shown is genuinely inexpressible. | none |
| 19 | "Generic methods … **accepted March 2026** … reversing the 1.18 position ([golang/go#75526], [spec #77273]) … targets **Go 1.27 at the earliest**, with no firm date. … **not** in Go 1.26 … treat the method form below as a preview, not something you can `go build` yet." | UNVERIFIABLE (correctly hedged as preview; specific asserts uncertifiable) | github.com/golang/go/issues/75526 confirms the issue exists and is about generic methods, but it is shown as **closed/duplicate** of the canonical proposal (#49085); WebFetch reads of the proposal threads were inconclusive on a formal "accepted March 2026" status, target "1.27", and spec issue "#77273." Audit cutoff (Jan 2026) cannot certify a March-2026 acceptance from memory. The chapter's *framing* (preview, not in 1.26, not buildable) is correct and safe; the *specific* date/issue-number/version asserts are the unverified part. | Soften the firm "accepted March 2026" → "proposed (and, as of writing, under/recently accepted), targeting a future release (~1.27 at the earliest)"; or add "as of this course's writing" hedge to the date and issue numbers. Do NOT present "March 2026 / #77273 / Go 1.27" as settled fact. The preview code itself is correctly labeled non-buildable. |
| 20 | "a generic method call is compiled like a generic *function* call with the receiver passed as the first argument, reusing the … monomorphization/stenciling that already powers generic functions. So `r.FlatMap(f)` is, under the hood, `FlatMap(r, f)`." | UNVERIFIABLE (plausible, design-level) | This matches what #75526's body sketches ("monomorphization strategy as for generic functions; receiver doesn't present new challenges beyond passing an extra parameter"), but it describes a *not-yet-shipped* implementation, so it cannot be certified as how the compiler will actually behave. Note the chapter elsewhere correctly describes Go generics as **GC-shape stenciling + dictionaries**, not pure monomorphization — calling it "monomorphization/stenciling" here is loose but the chapter's own wording is "the exact … stenciling that already powers generic functions," which is fine. | Tie to the same "preview / proposed" hedge as #19. The "stenciling" wording is acceptable. |
| 21 | "Go has **no tail-call optimization**, so recursion blows the stack; convert to a loop." | CORRECT | The gc compiler does not perform general tail-call optimization for Go function calls; deep self-recursion grows the goroutine stack and can exhaust it. Widely documented; no spec guarantee of TCO exists. | none |
| 22 | "Go's `(T, error)` already IS the Either monad — `Right` is `(value, nil)`, `Left` is `(zero, err)`, and `if err != nil { return ..., err }` is `FlatMap` written by hand." | CORRECT (conceptual framing) | A defensible, standard analogy; `(T,error)` + early-return is the hand-cranked bind. Presented as analogy, not a formal type-class claim. | none |
| 23 | Eager pipeline AlgoGrid: Map(abs) → Filter(≥100) → Reduce(+), survivors 1250+4000+500 = 5750; "2 intermediate slices for a 5-element pipeline" | CORRECT | Arithmetic: |−90|=90 (<100, dropped), |−500|=500 (kept); survivors {1250,4000,500} sum 5750. Map allocates one slice, Filter another → 2 intermediates. Matches. | none |
| 24 | Exercise-5: `TakeSeq` "can run over an *infinite* source without hanging … returning from `TakeSeq` ends `src`'s production." | CORRECT | go.dev/blog/range-functions: when the consumer stops (yield path returns), the producing iterator returns/cleans up. Pull-driven laziness makes take-over-infinite safe. | none |

## Sources fetched
- https://go.dev/blog/range-functions (range-over-func is new in Go 1.23; `iter.Seq[V] = func(yield func(V) bool)`; compiler rewrites body to yield; yield→false stops the iterator)
- https://go.dev/doc/go1.22 (per-iteration loop variables — the loop-var capture fix)
- https://github.com/golang/go/issues/75526 (generic-methods proposal: confirms topic + the "method must have no type parameters" error; status shown closed/duplicate — acceptance/version specifics not certifiable)
- https://github.com/golang/go/issues/49085 (canonical generic-methods proposal — status inconclusive via fetch)
- https://go.dev/doc/go1.26 (confirmed generic methods NOT shipped in 1.26 — chapter's "not in 1.26" is correct)

## Tally
- Claims logged: 24
- CORRECT: 21 (closures, escape analysis, 1.22 loop-var fix, 1.23 iter.Seq + range-over-func mechanics, no-HKT, no-TCO, the (T,error)=Either framing, all arithmetic)
- Flagged rows: 2 UNVERIFIABLE (#19, #20 — the generic-methods preview specifics) + 1 cosmetic (#8 compose convention).
- WRONG: 0
- Worst finding: **#19** — the generic-methods callout states as hard fact that the proposal was "accepted **March 2026**", cites spec issue **#77273**, and targets **Go 1.27**. These specific date/issue/version asserts could not be confirmed from any fetched source (issue #75526 is shown closed-as-duplicate; the threads were inconclusive on a formal acceptance/date/target). The chapter's *overall framing* is exactly right and safe — it loudly labels the method form a preview "not something you can `go build` yet" and confirms it's absent from 1.26 — so the reader is not handed a dangerous false certainty about the *language*. The fix is to hedge the precise acceptance date and issue/version numbers ("as of this writing, recently proposed/accepted, targeting a future release"), not to remove the section. Verdict on the prompt's specific question: **yes, the generic-methods feature is correctly hedged as accepted-but-not-shipped at the framing level; only the exact metadata (date/issue/version) is over-asserted.**

Frontmatter unchanged: yes (no edits made).
