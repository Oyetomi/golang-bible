# Accuracy audit — watermill

Tier-2 (correctness-critical). Chapter file: `content/part-3/08-watermill.mdx`.
Mode: REPORT ONLY — no content file was edited. Fixes below are **proposed**, not applied.

Focus: Watermill API shapes (Publisher/Subscriber, Router, middleware, message Ack/Nack, CQRS, Forwarder) against pkg.go.dev / github.com/ThreeDotsLabs/watermill.

| # | Claim (as written, location) | Verdict | Source (quoted) | Proposed action |
|---|---|---|---|---|
| 1 | `poisonQueue, err := middleware.**NewPoisonQueue**(publisher, "payment.completed.dlq")` (Middleware §2, line 452) | **WRONG** | pkg.go.dev `.../message/router/middleware`: the constructor is `func PoisonQueue(pub message.Publisher, topic string) (message.HandlerMiddleware, error)` (and `PoisonQueueWithFilter`). There is **no** `NewPoisonQueue`. | Rename to `middleware.PoisonQueue(publisher, "payment.completed.dlq")`. The returned value is already a `HandlerMiddleware`, so the later `poisonQueue.Middleware` reference (line 508) is also wrong — see #2. |
| 2 | `poisonQueue.Middleware` used as the middleware value in `AddMiddleware(...)` (wiring block, line 508; BeforeAfter line 524) | **WRONG** | `PoisonQueue` already **returns** a `message.HandlerMiddleware` (a `func(HandlerFunc) HandlerFunc`), not a struct with a `.Middleware` field. (Contrast `Retry`, which is a struct exposing `.Middleware`.) | Pass the returned value directly: `router.AddMiddleware(..., poisonQueue)` where `poisonQueue, err := middleware.PoisonQueue(pub, topic)`. |
| 3 | `retryMiddleware := middleware.Retry{ MaxRetries:5, InitialInterval:…, MaxInterval:…, Multiplier:2.0, **/* Jitter… */ MaxElapsedTime:…**, Logger }.Middleware` (Retry §1, lines 433–441) | **IMPRECISE** | pkg.go.dev: `middleware.Retry` has fields `MaxRetries, InitialInterval, MaxInterval, Multiplier, MaxElapsedTime, RandomizationFactor, OnRetryHook, …, Logger` and method `func (r Retry) Middleware(h message.HandlerFunc) message.HandlerFunc`. The struct shape and `.Middleware` method are **correct**; but the inline comment "// Jitter prevents the thundering-herd problem" is misleading — there is no `Jitter` field. Jitter is controlled by **`RandomizationFactor`**, which the example never sets. | Either set `RandomizationFactor: 0.5` (or similar) and update the comment to name it, or change the comment to "RandomizationFactor (unset here) controls jitter." Do not imply a `Jitter` field exists. |
| 4 | Publisher / Subscriber interface definitions (lines 62–73) | **CORRECT** | pkg.go.dev `.../message`: `type Publisher interface { Publish(topic string, messages ...*Message) error; Close() error }` and `type Subscriber interface { Subscribe(ctx context.Context, topic string) (<-chan *Message, error); Close() error }`. Exact match. | Leave. |
| 5 | `message.Message` "tiny struct with four fields" — UUID string, Metadata (map[string]string), Payload []byte; `Ack() bool` / `Nack() bool` (lines 84–100; Define line 80) | **CORRECT** (count nit) | pkg.go.dev: `type Message struct { UUID string; Metadata Metadata; Payload Payload }` with `type Metadata map[string]string`, `type Payload []byte`; `func (m *Message) Ack() bool`, `func (m *Message) Nack() bool`. The three exported fields + Ack/Nack are exact. | Optional: "four fields" counts the unexported ack/nack channels — fine since the code comment says `// unexported: Ack/Nack channels`. No change needed; the field types and method signatures are right. |
| 6 | `HandlerFunc func(msg *message.Message) ([]*message.Message, error)` and `HandlerMiddleware func(h HandlerFunc) HandlerFunc`; "composed := m1(m2(m3(yourHandler)))"; outermost runs first (lines 306, 353–364) | **CORRECT** | pkg.go.dev: `type HandlerFunc func(msg *Message) ([]*Message, error)`; `type HandlerMiddleware func(h HandlerFunc) HandlerFunc`. Decorator composition + order semantics match Watermill (and net/http). | Leave. |
| 7 | Router API: `message.NewRouter(RouterConfig{CloseTimeout:…}, logger)`; `AddHandler(name, inTopic, sub, outTopic, pub, handler)`; `router.Run(ctx)`; `router.Running()` (CodeWalk lines 269–294; Exercise 4 lines 1026–1056) | **CORRECT** | pkg.go.dev: `NewRouter(config RouterConfig, logger watermill.LoggerAdapter) (*Router, error)`; `AddHandler(handlerName, subscribeTopic string, subscriber Subscriber, publishTopic string, publisher Publisher, handlerFunc HandlerFunc) *Handler`; `Run(ctx) error`; `Running() chan struct{}`. Argument order and `Running()` semantics exact. | Leave. |
| 8 | `router.AddNoPublisherHandler("test-payment-handler", "payments", broker, func(msg)error{...})` (Exercise 4, lines 1036–1044) | **CORRECT** (but Deprecated upstream) | pkg.go.dev: `func (r *Router) AddNoPublisherHandler(handlerName, subscribeTopic string, subscriber Subscriber, handlerFunc NoPublishHandlerFunc) *Handler` — signature matches; the 4-arg form with a `func(*Message) error` handler is right. Note: upstream marks it Deprecated (prefer `AddHandler` with a no-op publisher), but it still exists and compiles. | Leave; optionally add "(deprecated upstream; still works)". |
| 9 | `middleware.CorrelationID` and `middleware.Recoverer` used as bare `func(HandlerFunc) HandlerFunc` values in `AddMiddleware(...)` (lines 472, 482, 497–501) | **CORRECT** | pkg.go.dev: `func CorrelationID(h message.HandlerFunc) message.HandlerFunc` and `func Recoverer(h message.HandlerFunc) message.HandlerFunc` — both are plain middleware functions, correctly passed directly (unlike Retry/PoisonQueue). Recoverer "Recovers from any panic in the handler and appends RecoveredPanicError." | Leave. |
| 10 | CQRS: `cqrs.**NewFacade**(cqrs.**FacadeConfig**{ GenerateCommandsTopic, GenerateEventsTopic, CommandHandlers, EventHandlers, Router, CommandsPublisher, CommandsSubscriber, EventsPublisher, EventsSubscriber, CommandEventMarshaler: cqrs.JSONMarshaler{}, Logger })` (lines 566–594) | **OUTDATED** | pkg.go.dev `.../components/cqrs` (v1.5.2): `NewFacade`/`FacadeConfig` are **Deprecated** — "DEPRECATED: use CommandHandler and EventHandler instead." Current API: `NewCommandProcessorWithConfig(router, CommandProcessorConfig)`, `NewEventProcessorWithConfig(router, EventProcessorConfig)`, `NewCommandBusWithConfig`, `NewEventBusWithConfig`. | Modernize to the `*WithConfig` processors/buses, or add a one-line note: "`NewFacade` is deprecated in current Watermill; the modern API splits this into `NewCommandProcessorWithConfig` / `NewEventProcessorWithConfig` + the typed `cqrs.NewCommandHandler[T]` / `cqrs.NewEventHandler[T]` constructors. Shown here for shape." |
| 11 | CommandHandler iface: `HandlerName() string`, `NewCommand() interface{}`, `Handle(ctx, cmd interface{}) error`; EventHandler: `HandlerName()`, `NewEvent() interface{}`, `Handle(ctx, event interface{}) error` (lines 605–650) | **CORRECT** | pkg.go.dev: `CommandHandler interface { HandlerName() string; NewCommand() any; Handle(ctx context.Context, cmd any) error }` and `EventHandler { HandlerName() string; NewEvent() any; Handle(ctx, event any) error }`. `interface{}` ≡ `any`, so the signatures are equivalent and correct. | Leave (the `interface{}` vs `any` spelling is cosmetic). |
| 12 | `cqrs.JSONMarshaler{}` is the marshaler (line 592) | **CORRECT** | pkg.go.dev: `type JSONMarshaler struct { NewUUID func() string; GenerateName func(v interface{}) string }` implements `CommandEventMarshaler`. The zero value is usable. | Leave. |
| 13 | Forwarder: `forwarder.NewForwarder(outboxSubscriber, publisher, logger, forwarder.Config{ ForwarderTopic: forwarder.TopicPrefix + "payment.completed" })`; `go fwd.Run(ctx)` (lines 688–701) | **UNVERIFIABLE** | Could not fetch a clean pkg.go.dev page for `.../components/forwarder` in this pass (only the message + middleware + cqrs packages were retrieved). The general shape (Subscriber + Publisher + Config + Run) is consistent with Watermill conventions, but the exact `NewForwarder` arg order, the `Config.ForwarderTopic` field, and `forwarder.TopicPrefix` were **not** confirmed against a fetched source. | Downgrade confidence: verify `forwarder.NewForwarder` signature and `forwarder.Config` field names against pkg.go.dev before publishing as gospel; or soften to "shape illustrative." Flag for human check. |
| 14 | Router internal loop (UnderTheHood line 736): `producedMessages, err := composedHandler(msg)`; on err `msg.Nack()`; publish outputs **before** `msg.Ack()`; "output messages are published before the input is Acked" → at-least-once (lines 738–767) | **CORRECT** (labeled "simplified") | Consistent with Watermill's at-least-once contract and the QuickCheck (handler error ⇒ Nack ⇒ broker redelivers). The text already says "simplified from the Watermill source," satisfying the simplified-animation rule. Ack/Nack-on-message (not on consumer) matches `func (m *Message) Ack()/Nack() bool`. | Leave. |
| 15 | "As of Go 1.26, the goroutine-per-handler model is efficient … GMP scheduler multiplexes them onto OS threads" and "With GOMAXPROCS cores available, handler goroutines for different topics run truly in parallel" (UnderTheHood lines 366, 769) | **CORRECT (generic, not load-bearing)** | Goroutines are M:N-multiplexed by the GMP scheduler; cheap stacks grown dynamically. No false specific number is asserted. (GOMAXPROCS container-awareness since 1.25 is not contradicted here — the claim is only "with GOMAXPROCS cores," which is correct phrasing.) | Leave. |

## Tally

- Claims examined (falsifiable, load-bearing): **15**
- **WRONG: 2** (#1 `NewPoisonQueue` does not exist; #2 `poisonQueue.Middleware` field does not exist)
- **OUTDATED: 1** (#10 `cqrs.NewFacade`/`FacadeConfig` deprecated)
- **IMPRECISE: 1** (#3 Retry "Jitter" comment — field is `RandomizationFactor`)
- **UNVERIFIABLE: 1** (#13 Forwarder `NewForwarder`/`Config` shape — not fetched; downgrade + flag)
- **CORRECT (confirmed): 10** (#4–#9, #11, #12, #14, #15)

**Worst finding:** #1/#2 — the poison-queue wiring will **not compile**. The chapter calls `middleware.NewPoisonQueue(...)` (no such function; it's `middleware.PoisonQueue(pub, topic)`) and then references `poisonQueue.Middleware` (the constructor already returns a `HandlerMiddleware`, not a struct with a `.Middleware` field). A reader copying this into a real module gets two compile errors on a core production-middleware example.

## Sources fetched (actually opened)

- https://github.com/ThreeDotsLabs/watermill — Publisher/Subscriber interfaces; handler `func(*Message) ([]*Message, error)`
- https://pkg.go.dev/github.com/ThreeDotsLabs/watermill/message — Message struct (UUID/Metadata/Payload), Ack/Nack, Publisher, Subscriber, NewRouter, AddHandler, AddNoPublisherHandler, AddMiddleware, Run, Running, HandlerFunc, HandlerMiddleware
- https://pkg.go.dev/github.com/ThreeDotsLabs/watermill/message/router/middleware — Retry struct (RandomizationFactor, Multiplier, MaxElapsedTime, Logger, .Middleware method), PoisonQueue / PoisonQueueWithFilter constructors, CorrelationID, Recoverer
- https://pkg.go.dev/github.com/ThreeDotsLabs/watermill/components/cqrs — NewFacade/FacadeConfig DEPRECATED; NewCommand/EventProcessorWithConfig; CommandHandler/EventHandler interfaces (NewCommand() any / Handle(ctx, any)); JSONMarshaler
- https://go.dev/doc/go1.22 — range-over-int / per-iteration loop var (for the stdlib illustration + Lab)
- (not fetched this pass — flagged on #13) pkg.go.dev `.../components/forwarder`

Frontmatter unchanged: yes (no file edited — report only).
