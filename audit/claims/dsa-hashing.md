# Accuracy audit — dsa-hashing

Chapter: `content/appendix/03-dsa-hashing.mdx`
Mode: REPORT ONLY (no edits applied). Tier-3 (appendix/DSA); hash-map complexity and Go map internals audited rigorously.

Summary: Frequency-map / set / grouping / two-sum-family complexities are all derivable-correct. The map-internals UnderTheHood is well done: Swiss Tables pinned to Go 1.24, "groups of 8 slots + control bytes," open-addressing-not-chaining, unspecified+randomized iteration order, hash-seed randomization vs hash-flooding, amortized growth, worst-case O(n) under adversarial collisions — all accurate. The headline landmines (chaining-like-Java, nil-map, iteration order) are handled correctly. Flags are minor: one undocumented-but-correct group-count detail, one `[26]int` alphabet-scope caveat that's implicit, and a `groupAnagrams` sort-key complexity nuance. **0 WRONG, 0 UNVERIFIABLE, 3 IMPRECISE/scoping, ~25 CORRECT.**

## Audit log (flagged rows; confirmed-correct summarized after)

| # | Claim (as written) | Verdict | Source (fetched) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | `<UnderTheHood>` "**The group structure (as of Go 1.24).** A `map[K]V` is backed by a Swiss Table-style hash table. Go's runtime stores key-value pairs in groups of 8 slots plus a control word. The control bytes store occupancy state and hash bits…" + Recap "groups of 8 slots, control bytes for fast rejection" (line 1489) + MapAnim/ExecTimeline group probing | CORRECT | go.dev/doc/go1.24: "a new builtin `map` implementation based on Swiss Tables." Groups-of-8 + control bytes matches the `internal/runtime/maps` Swiss-table design. Version pinned to 1.24; "open addressing/probing, not separate linked-list chaining" avoids the chaining landmine. | none. (Group count "8" and "control word" are accurate impl details not spelled out in the release notes; correct nonetheless.) |
| 2 | "**Hash collision and worst-case O(n).** If an attacker can craft keys that all collide along the same probe paths, lookups can degrade to O(n). Go mitigates this with **hash randomization** — the hash seed is chosen randomly…" | CORRECT | Go runtime seeds map hashing with a per-process random seed (hash-flooding mitigation); adversarial collisions degrade probing toward O(n). Accurate and appropriately hedged. | none |
| 3 | "**Why iteration order is unsafe.** The Go spec says map iteration order is not specified. The standard runtime deliberately varies iteration order… use `slices.Sorted(maps.Keys(m))` (Go 1.23+)." | CORRECT | go.dev/ref/spec (For statements / range): "The iteration order over maps is not specified and is not guaranteed to be the same from one iteration to the next." `slices.Sorted` + `maps.Keys` (iterator form) are Go 1.23. | none |
| 4 | isAnagram / firstUniqChar / Exercise-1 use `var count [26]int` and index `s[i]-'a'`, claimed "O(1) space (bounded alphabet)" | CORRECT (complexity) but SCOPE-IMPLICIT | The `[26]int` assumes input is strictly lowercase ASCII `a`–`z`; uppercase, digits, spaces, or any byte ≥ 'a'+26 / < 'a' index out of `[0,26)` and panic. The chapter's prose ("the alphabet is fixed at 26 or 128 chars") acknowledges this but the runnable code silently assumes lowercase. Complexity claim O(n)/O(1) is correct. | Optional: note in prose that `[26]int` requires lowercase-only input (else use `[128]int` or a map). Not a complexity error — a robustness caveat. |
| 5 | group-anagrams sort-key: "O(n · k log k) where k is max word length. For fixed alphabets, a character-count signature … turns this into O(n · k)." (line 713) and `sortedKey` uses `sort.Slice` | CORRECT | Sorting each word's bytes is O(k log k); n words ⇒ O(n·k log k). The `[26]int` count-key variant is O(k) per word ⇒ O(n·k). `sort.Slice` here is fine (sorting bytes for a canonical key; stability irrelevant since elements are indistinguishable bytes). | none — complexity correct. (Pedantic: `sort.Slice` unstable doesn't matter for a bag of bytes.) |
| 6 | Define `map[T]struct{}` "**`bool` wastes 1 byte per entry; at a million entries that's 1 MB**" (line 520) and ConceptCard "saves one byte per entry" | CORRECT (first-order) | `struct{}` has size 0; `bool` size 1. The per-value byte saving is real; "1 MB at 1M entries" is the value-byte arithmetic (ignoring per-slot bucket padding/alignment, which the chapter doesn't claim to account for). The stronger correctness argument it gives — that `map[T]bool` lets you write `m[k]=false` and conflate absent vs false — is also correct. | none. (Note: actual memory delta can differ from a clean 1 MB due to Swiss-table slot layout/alignment, but the stated value-size logic is sound and not overstated.) |

## Spot-checked and CORRECT (not individually flagged)

`freq[item]++` zero-value insert (missing key reads as 0, then +1) — CORRECT per spec (index expression on map yields zero value of element type for absent keys; this is a *read*, not the nil-map-write landmine). Reading a missing key returns zero value (line 211 `freq["logout"]` → 0) — correct. buildFreqMap O(n) — correct; QuickCheck "O(n) average, each insert O(1) amortized" — correct. isAnagram O(n)/O(1) — correct. firstUniqChar O(n)/O(1) two-pass — correct. topKFrequent min-heap-of-size-k O(n log k)/O(n+k) — correct (matches container/heap O(log n) per op; pkg.go.dev/container/heap). majorityElement freq-map O(n)/O(n) (and Boyer-Moore O(n)/O(1) mention) — correct. containsDuplicate set O(n)/O(n) — correct. intersection O(n+m)/O(min(n,m)) with `delete` to dedup — correct. union/difference — correct. groupBy generic O(n)/O(n), `append` to nil slice works — CORRECT (`append(nil, v)` allocates). `[26]int` usable as map key because arrays are comparable; slices are not — CORRECT per spec (array types comparable iff element type comparable; slices not comparable). two-sum complement trick O(n)/O(n) and the "store AFTER lookup so you don't pair with yourself" (lab hint, line 1474) — correct. subarraySum prefix+map O(n)/O(n), `{0:1}` sentinel — correct. countPairs O(n)/O(n) — correct. fourSumCount split into two O(n²) passes (meet-in-the-middle) vs O(n⁴) brute — correct. longestConsecutive set-based O(n) (only walk from sequence starts; each element touched ≤ twice) — correct. minWindow sliding window two-maps O(n+m) — correct. comma-ok distinguishes absent vs zero-value — correct. Lab twoSum cases incl. `[1,5,3,2] target=4 → [0,2]` (1+3) — verified correct; `[3,3] → [0,1]` — correct.

## Sources fetched
- https://go.dev/doc/go1.24 (Swiss Tables map) · https://pkg.go.dev/container/heap (heap op O(log n)) · https://go.dev/ref/spec (map iteration order unspecified; comparability of arrays vs slices; map index zero-value read) · https://pkg.go.dev/sort (sort.Slice unstable — relevant to #5)

## Tally
WRONG: 0 · OUTDATED: 0 · UNVERIFIABLE: 0 · IMPRECISE/scoping: 3 (#4 `[26]int` lowercase-only assumption in runnable code; #5/#6 pedantic nuances, both ultimately correct) · CORRECT: ~25 spot-checked + all 6 flagged rows resolve CORRECT-with-nuance.
Worst finding: #4 — the `[26]int` frequency-array code panics on any non-lowercase-ASCII byte (`s[i]-'a'` out of range), and the prose's hedge ("26 or 128") isn't reflected in the runnable snippets. It's a robustness/scoping gap, not a complexity or internals error; all stated complexities and all map-internals claims are correct. Frontmatter unchanged: yes (report-only).
