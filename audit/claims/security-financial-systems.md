# Accuracy audit — security-financial-systems

Chapter: `content/part-3/12-security-financial-systems.mdx`
Tier 2 (correctness-critical). REPORT ONLY — no content file was edited.

Focus: AES-GCM / AEAD properties, nonce uniqueness, envelope encryption (DEK/KEK), `crypto/rand` vs `math/rand`, constant-time comparison (`hmac.Equal` / `subtle.ConstantTimeCompare`), HMAC request signing, and the Go 1.26 crypto claims (`crypto/hpke`, `runtime/secret`, always-secure randomness in ecdsa/rsa/dsa). Verified against go.dev/doc/go1.26 and pkg.go.dev.

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (not applied) |
|---|---|---|---|---|
| 1 | "`crypto/rand` … wraps the OS CSPRNG (`/dev/urandom` on Linux, `BCryptGenRandom` on Windows)." | **OUTDATED / IMPRECISE** | pkg.go.dev/crypto/rand: "On Linux, FreeBSD, Dragonfly, and Solaris, Reader uses **getrandom(2)**" ("On legacy Linux (< 3.17), Reader opens /dev/urandom"); "On Windows, Reader uses the **ProcessPrng** API." The chapter names `/dev/urandom` and `BCryptGenRandom` — both are no longer what current Go uses. | Update to "`getrandom(2)` on Linux (… `/dev/urandom` only on legacy kernels), `ProcessPrng` on Windows." Or hedge: "the OS CSPRNG (e.g. `getrandom(2)` on Linux, `ProcessPrng` on Windows)." Substance (it wraps the OS CSPRNG) is correct; the named syscalls are stale. |
| 2 | `math/rand` is "a deterministic PRNG … completely wrong for security-sensitive values"; a known/guessed seed reproduces the whole stream; UnixNano seed is 64-bit. | **CORRECT** | math/rand is a deterministic PRNG; output is a function of its seed. Not a CSPRNG. The chapter's QuickCheck explanation correctly notes the practical seed-window narrowing. | None. |
| 3 | Go 1.20 note: "`math/rand` global functions are auto-seeded with a random value by default (no longer the fixed seed=1 from older Go)." | **CORRECT** | go.dev/doc/go1.20: the top-level `math/rand` functions are auto-seeded by default in Go 1.20 (previously seeded with 1 unless `Seed` was called). Version attribution correct. | None. |
| 4 | "**Go 1.26 (always-secure crypto):** Several functions in `crypto/ecdsa`, `crypto/rsa`, and `crypto/dsa` that previously accepted a `rand io.Reader` … now **ignore the supplied reader** and always use `crypto/rand` internally. The signatures are unchanged … passing a custom reader no longer has any effect." | **CORRECT** | go.dev/doc/go1.26: "The random parameter to GenerateKey is now ignored. Instead, it now always uses a secure source of cryptographically random bytes." Affected across crypto/dsa, crypto/ecdh, crypto/ecdsa (incl. Sign/SignASN1/PrivateKey.Sign), crypto/ed25519, crypto/rsa (incl. EncryptPKCS1v15), crypto/rand.Prime. The chapter's ecdsa/rsa/dsa list is a correct subset. | None. (Could add: a GODEBUG `cryptocustomrand=1` / `testing/cryptotest.SetGlobalRandom` exists to restore/override for tests — the chapter's "use RFC 6979 for deterministic ECDSA tests" advice is still valid.) |
| 5 | "**`crypto/hpke` (new in Go 1.26):** … implements Hybrid Public Key Encryption (RFC 9180). HPKE combines an asymmetric KEM … with a symmetric AEAD." | **CORRECT** | go.dev/doc/go1.26: "The new crypto/hpke package implements Hybrid Public Key Encryption (HPKE) as specified in RFC 9180, including support for post-quantum hybrid KEMs." New in 1.26, RFC 9180 — confirmed. | None. (Optional enrichment: it also adds post-quantum hybrid KEM support.) |
| 6 | "**`runtime/secret` (experimental in Go 1.26):** … provides a `Value[T]` type that holds sensitive material … attempts to zero memory when the value is garbage-collected or explicitly released." | **PARTIALLY WRONG / UNVERIFIABLE (API detail)** | go.dev/doc/go1.26: "The new runtime/secret package is available as an experiment, which can be enabled by setting … `GOEXPERIMENT=runtimesecret` … It provides a facility for securely erasing **temporaries** used in code that manipulates secret information … such as **registers, stack, new heap allocations**." The release notes do NOT describe a `Value[T]` type, nor "zero on GC/release" semantics — the documented purpose is erasing transient temporaries (registers/stack/heap), and it's gated behind a GOEXPERIMENT flag (chapter omits that). | (1) Add that it requires `GOEXPERIMENT=runtimesecret`. (2) Soften the API description: the release notes describe "securely erasing temporaries (registers, stack, new heap allocations)," not a `Value[T]` container that zeroes on GC. Either cite the package's own API for `Value[T]`, or restate to match the documented "erase temporaries" framing. Flag the `Value[T]`/GC-zero specifics as unverified. |
| 7 | AES-GCM Define: "an **AEAD** cipher … confidentiality … and integrity … any modification to the ciphertext is detected before you get plaintext back." | **CORRECT** | pkg.go.dev/crypto/cipher AEAD: Open returns an error on tag mismatch and does not return plaintext. AES-GCM is an authenticated-encryption (AEAD) scheme. | None. |
| 8 | Nonce Define: "For AES-GCM, the standard nonce is 12 bytes … If you reuse a nonce with the same key, an attacker who sees both ciphertexts can XOR them to cancel the keystream and recover the XOR of the two plaintexts." | **CORRECT** | pkg.go.dev/crypto/cipher: NewGCM uses "the standard nonce length" (12 bytes / 96 bits); AEAD.Seal nonce "must be … unique for all time, for a given key." Nonce-reuse: C1⊕C2 = P1⊕P2 (keystream cancels) — confirmed (Böck et al. and standard CTR-mode analysis). | None. (Understatement only: nonce reuse also enables GHASH auth-key recovery → forgery; the stated confidentiality break is accurate.) |
| 9 | "In 2016, researchers demonstrated a practical nonce-reuse attack against **TLS 1.3 candidates**." | **WRONG (version attribution)** | Böck, Zauner, Devlin, Somorovsky, Jovanovic, "Nonce-Disrespecting Adversaries: Practical Forgery Attacks on GCM in TLS," USENIX WOOT 2016: the Internet-wide scan found **184 HTTPS servers repeating nonces** in **deployed TLS (AES-GCM, i.e. TLS 1.2)** — it attacked live TLS 1.2 servers, not "TLS 1.3 candidates." | Change "against TLS 1.3 candidates" → "against deployed TLS servers using AES-GCM (TLS 1.2)" (the 2016 'Nonce-Disrespecting Adversaries' work). The year and "practical nonce-reuse on GCM in TLS" are correct; the TLS-version label is wrong. |
| 10 | UnderTheHood: GCM = CTR keystream (confidentiality) + GHASH tag (integrity), tag encrypted under a derived subkey; "`gcm.Open` recomputes the tag … if it doesn't match … returns an error *before yielding any plaintext* — a constant-time comparison so timing attacks learn nothing." | **CORRECT (with a precision caveat)** | The CTR+GHASH structure and tag-then-decrypt order are accurate; Go's GCM tag check uses a constant-time comparison (`subtle.ConstantTimeCompare`). **Caveat:** pkg.go.dev/crypto/cipher NewGCM states "the GHASH operation … is **not** constant-time" except when AES-NI hardware is present — so the *GHASH computation* is not constant-time generally, even though the *final tag comparison* is. The chapter's sentence is about the comparison, which is correct, but the surrounding "constant-time" framing could be read as covering GHASH. | Optional: tighten to "the tag *comparison* is constant-time," and (if desired) note that GHASH itself is constant-time only on AES-NI hardware. Substance of the comparison claim is correct; low severity. |
| 11 | "Go's implementation uses hardware AES-NI on amd64 and arm64 (Go 1.21+). The `crypto/aes` package probes CPU features at init time." | **CORRECT (UNVERIFIABLE on exact "1.21+" pin, low risk)** | `crypto/aes` has dispatched to AES-NI (amd64) / hardware AES (arm64) asm with CPU-feature detection for many releases; pkg.go.dev/crypto/cipher confirms the AES-NI hardware path ("when the underlying Block was created by aes.NewCipher on systems with hardware support for AES"). Could not pin "1.21+" as the specific version this began. | Optional: drop or soften the "(Go 1.21+)" version pin unless a source is cited — hardware AES predates 1.21. The mechanism (AES-NI on amd64/arm64, CPU-feature probe) is correct. |
| 12 | Constant-time Define + Callout: naive `==` / `bytes.Equal` short-circuits at first mismatch → timing oracle; fix is `subtle.ConstantTimeCompare` / `hmac.Equal`, which "always examine every byte." | **CORRECT** | pkg.go.dev/crypto/subtle ConstantTimeCompare: "The time taken is a function of the length of the slices and is independent of the contents" (returns 1/0; returns 0 immediately if lengths differ). pkg.go.dev/crypto/hmac Equal: "compares two MACs for equality without leaking timing information." `bytes.Equal`/`==` do short-circuit. | None. |
| 13 | "`hmac.Equal` is a convenience wrapper around `subtle.ConstantTimeCompare`." | **CORRECT** | `crypto/hmac.Equal` is implemented via `subtle.ConstantTimeCompare` (length-prefixed) in the stdlib source; the doc confirms it "compares two MACs … without leaking timing information." Calling it a wrapper around `subtle.ConstantTimeCompare` is accurate. | None. |
| 14 | "In 2009, a timing attack was used against **HMAC-MD5** in the Keyczar library … reconstruction of the full signature across ~2^16 requests." | **WRONG (algorithm) + IMPRECISE (framing/number)** | Nate Lawson, 2009 (rdist / Threatpost / Keyczar advisory): the timing attack was against Keyczar's **HMAC-SHA1** verification (early-exit byte compare), demonstrated/disclosed by a researcher — **not HMAC-MD5**, and not "used against" in the wild. The "~2^16 requests" figure is unsourced (plausible order of magnitude for such attacks but no citation found). | Change "HMAC-MD5" → "**HMAC-SHA1**"; soften "was used against" → "was demonstrated against" (it was Lawson's disclosure, not an in-the-wild exploit); either cite or hedge the "~2^16 requests" estimate. |
| 15 | Envelope encryption Define: DEK (per-record random AES key) encrypts the value; KEK (master key in KMS) wraps the DEK; store EncDEK + ciphertext; KMS unwraps EncDEK on read; "An attacker who steals only the database gets ciphertexts and encrypted DEKs — useless without KMS access." | **CORRECT** | Standard envelope-encryption / KMS `GenerateDataKey` pattern (AWS KMS, GCP Cloud KMS). DEK/KEK two-tier hierarchy, plaintext DEK ephemeral, blast-radius containment — all accurate. | None. |
| 16 | AES-GCM GoPlayground + FieldEncryptor: `cipher.NewGCM`, `gcm.NonceSize()` (12-byte) nonce from `crypto/rand`, `gcm.Seal(nonce, nonce, …)` (prepend) / split on `ns`; tamper flips a tag byte → `gcm.Open` returns "message authentication failed." | **CORRECT** | pkg.go.dev/crypto/cipher: NewGCM "standard nonce length"; AEAD.Seal nonce "unique for all time, for a given key"; Open errors on tag mismatch without returning plaintext. Code uses `io.ReadFull(rand.Reader, nonce)` — correct CSPRNG nonce. Flipping the last byte (auth tag) makes Open fail. All sound and compiles. | None. (The `mockKMS` XOR "wrapping" is explicitly labeled test-only/never-production — appropriate.) |
| 17 | Request signing: canonical request = method + path + timestamp + hex(sha256(body)); HMAC-SHA256 over it; verify recomputes + `hmac.Equal`; ±5-minute clock-skew window prevents replay of stale requests. | **CORRECT** | Matches AWS SigV4-style canonical-request discipline; HMAC-SHA256 over a deterministic canonical string is standard request signing; a bounded timestamp window is the conventional replay defense for short-lived signatures. Code uses `hmac.Equal` (constant-time). Sound. | None. |
| 18 | Callout: "As of Go 1.26, `ecdsa.Sign` ignores any supplied `rand io.Reader` and always uses `crypto/rand` internally." (asymmetric-signing callout) | **CORRECT** | go.dev/doc/go1.26: crypto/ecdsa `Sign`/`SignASN1`/`PrivateKey.Sign` random parameter "is now ignored … always uses a secure source of cryptographically random bytes." Confirmed. | None. |
| 19 | Replay/nonce section: a nonce is "a unique value … the server persists after first use and rejects on any subsequent use"; client nonce from `crypto/rand`; store with TTL matching the signature window; idempotency key ≈ server-enforced nonce. | **CORRECT** | Standard replay-protection (seen-nonce store with TTL) and the idempotency-key/nonce relationship are correct distributed-systems / API-security canon. Generating the nonce from `crypto/rand` is right. | None. |
| 20 | PII / `slog` redaction: a `Redacted` type implementing `slog.LogValuer` logs `[REDACTED]`; `slog.Any` calls `LogValue()`, but `slog.String("k", string(val))` casts away the type and bypasses `LogValue()`; `fmt.Stringer.String()` is called by fmt, not slog. | **CORRECT** | log/slog: a value implementing `LogValuer` has `LogValue()` called when passed via `slog.Any`; casting to `string` before passing it removes the type so `LogValue()` is never invoked. `fmt.Stringer` is a separate interface used by `fmt`, not `slog`. All accurate. | None. |
| 21 | Row-level authorization: adding `AND owner_id = $2` to the query enforces ownership; "Postgres also supports it at the database level (`ALTER TABLE accounts ENABLE ROW LEVEL SECURITY`)." Exercise 3 uses `r.PathValue("account_id")` "(Go 1.22+ net/http routing)." | **CORRECT** | Parameterized ownership predicate is the correct row-level-auth pattern; Postgres `ENABLE ROW LEVEL SECURITY` exists. `http.Request.PathValue` + pattern routing was added to net/http in Go 1.22 — version pin correct. | None. |

### CORRECT (verified, not individually flagged): 16

Rows 2-5, 7-8, 12-13, 15-21 verified CORRECT against fetched sources (rows 4, 5, 18 are the Go 1.26 crypto claims — all confirmed). Notes-only: row 10 (constant-time precision), row 11 (1.21+ pin softening). Hard/notable errors: row 1 (outdated OS sources), row 6 (runtime/secret API + missing GOEXPERIMENT), row 9 (TLS 1.3 vs 1.2), row 14 (HMAC-MD5 vs SHA1).

## Sources fetched
- go.dev/doc/go1.26 (crypto/hpke new; runtime/secret experiment + GOEXPERIMENT=runtimesecret + "erase temporaries: registers/stack/heap"; ecdsa/rsa/dsa/ecdh/ed25519/rand random-param ignored; Green Tea default; io.ReadAll; slog.NewMultiHandler)
- https://pkg.go.dev/crypto/cipher (NewGCM standard nonce length; AEAD nonce "unique for all time"; GHASH "not constant-time" except AES-NI; Open errors on tag mismatch)
- https://pkg.go.dev/crypto/rand (Reader OS sources: getrandom(2) Linux / ProcessPrng Windows / arc4random_buf macOS)
- https://pkg.go.dev/crypto/hmac (Equal "compares two MACs … without leaking timing information")
- https://pkg.go.dev/crypto/subtle (ConstantTimeCompare: time independent of contents; 0 immediately on length mismatch)
- WebSearch → AES-GCM nonce-reuse (XOR keystream cancellation; auth-key recovery); 2016 Böck et al. "Nonce-Disrespecting Adversaries" (TLS 1.2 servers, 184 found); 2009 Keyczar timing attack (Nate Lawson, HMAC-SHA1)

## Tally
Flagged: 6 (1 OUTDATED [row 1], 1 PARTIALLY-WRONG/UNVERIFIABLE [row 6 runtime/secret], 2 WRONG historical-facts [row 9 TLS version, row 14 HMAC-MD5→SHA1], 2 notes/softenings [rows 10, 11]) + 16 verified CORRECT. Worst finding: **the 2009 Keyczar timing attack was against HMAC-SHA1, not "HMAC-MD5" (row 14), and the 2016 GCM nonce-reuse attack hit deployed TLS 1.2 servers, not "TLS 1.3 candidates" (row 9) — two confidently-stated historical facts are wrong. Also notable: the `runtime/secret` description invents a `Value[T]`/zero-on-GC API the Go 1.26 release notes do not document and omits the required `GOEXPERIMENT=runtimesecret` flag (row 6).**
