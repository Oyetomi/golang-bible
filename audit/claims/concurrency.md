# Accuracy audit — concurrency

Chapter: `content/part-1/04-concurrency.mdx` (Tier 1 — internals; every falsifiable claim audited)
Mode: REPORT ONLY — no edits applied to the chapter.

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | (L65, L68, L217, L229) Goroutine "starts with a ~2 KB growable stack" / "starts at 2 KB, grows/shrinks dynamically". | CORRECT | go.dev/src/runtime/stack.go: `// The minimum size of stack used by Go code` → `stackMin = 2048` (2 KB). | None. |
| 2 | (L68, L217) OS thread "~1–8 MB" fixed stack (vs goroutine). | CORRECT (no single canonical source; matches platform defaults) | Linux `pthread` default stack is typically 8 MB (`ulimit -s` 8192 KB); other platforms 1–2 MB. Range "1–8 MB" is accurate and hedged ("~"). No go.dev fetch applies; OS-defined. | None. Leave hedged. |
| 3 | (L65) Goroutine is "cooperatively-and-preemptively scheduled". | CORRECT | go.dev/doc/go1.14: "Goroutines are now asynchronously preemptible. As a result, loops without function calls no longer potentially deadlock the scheduler…" Combined with cooperative yield points at function calls/channel ops → both modes hold. | None. |
| 4 | (L204, L208, L220) GOMAXPROCS default "container-aware as of Go 1.25"; reads "Linux cgroup CPU quotas"; "a container with a 2-CPU limit gets 2 Ps". | CORRECT | go.dev/doc/go1.25: "In prior versions of Go, `GOMAXPROCS` defaults to the number of logical CPUs… Go 1.25 introduces two changes: 1. On Linux, the runtime considers the CPU bandwidth limit of the cgroup… If the CPU bandwidth limit is lower than the number of logical CPUs available, `GOMAXPROCS` will default to the lower limit." | None. Landmine correctly avoided (does not say "defaults to NumCPU"). |
| 5 | (L204) "it considers logical CPUs, CPU affinity, and Linux cgroup CPU quota". | IMPRECISE | go.dev/doc/go1.25 names two changes: cgroup CPU **bandwidth limit** (Linux) and **periodic update** of GOMAXPROCS. The release notes do **not** list "CPU affinity" as a 1.25 GOMAXPROCS input. (Affinity-aware NumCPU is a separate/older concern.) | Drop "CPU affinity" from the 1.25 list, or hedge: "considers logical CPUs and Linux cgroup CPU bandwidth limit (and periodically re-checks them)." |
| 6 | (L227–231) GMP: G = goroutine; M = OS thread; P = logical processor with local run queue; "exactly `GOMAXPROCS` Ps"; "An M must hold a P to execute Go code". | CORRECT | go.dev/src/runtime/runtime2.go `type p struct` (per-P `runq`); standard GMP model. `GOMAXPROCS` Ps is the documented invariant. | None. |
| 7 | (L234) "Local run queues (one per P, ~256 slots)". | CORRECT | go.dev/src/runtime/runtime2.go, `type p struct`: `runq [256]guintptr` (fixed-size array of 256). | None. The "~" hedge is fine (plus the `runnext` slot). |
| 8 | (L236, L238) "Work stealing: when a P's local queue is empty, it steals half the goroutines from another P's queue." | CORRECT (mechanism); steal-half line not directly fetched | Corroborated: per-P queue is `[256]guintptr` (runtime2.go, fetched) and Go's documented work-stealing grabs half (`runqgrab`/`runqsteal`, `n = n - n/2`). The exact `runqgrab` line is below proc.go's fetch-truncation point (file truncates ~L2432; function is ~L7000) and go.dev/cs.opensource source viewers returned empty/JS-only. Chapter already defers full detail to Part 2 Ch.5. | None needed; mechanism is correct. If a reviewer wants belt-and-suspenders, confirm the `runqgrab` "Grab a batch… n = n - n/2" line against a non-truncated mirror. |
| 9 | (L240) "Go 1.14 added asynchronous preemption via POSIX signals — the runtime sends `SIGURG`… A CPU-bound goroutine can no longer starve everything else." | CORRECT (SIGURG name not in release notes) | go.dev/doc/go1.14: "Goroutines are now asynchronously preemptible. As a result, loops without function calls no longer potentially deadlock the scheduler… programs built with Go 1.14 will receive more signals." The specific signal SIGURG is the implementation choice (`runtime.preemptM` → `sigPreempt`); release notes confirm signal-based, not the name. | None. (Optionally hedge "SIGURG" as "a signal (SIGURG)" — it is correct but not stated in the release notes.) |
| 10 | (L240) "Before Go 1.14, a tight compute loop without a function call could hold a P hostage (no yield point)." | CORRECT | go.dev/doc/go1.14 (same quote as #9): pre-1.14, loops without function calls had no preemption point. | None. |
| 11 | (L246–250 QuickCheck) GOMAXPROCS=2 → "At most 2 goroutines execute in parallel at any moment". | CORRECT | go.dev/doc/go1.25 + GMP: GOMAXPROCS = max Ps executing Go code simultaneously. Answer index 1 is right. | None. |
| 12 | (L258) Data race definition: "two goroutines access the same memory location concurrently and at least one… is a write — with no synchronization". | CORRECT | go.dev/ref/mem (race defined as concurrent unsynchronized accesses, ≥1 write). Standard definition. | None. |
| 13 | (L262, L284, L293–295) Unbuffered channel = rendezvous; send/recv synchronize; "the goroutine's send happened-before this line". | CORRECT | go.dev/ref/mem: "A send on a channel is synchronized before the completion of the corresponding receive from that channel"; and "A receive from an unbuffered channel is synchronized before the completion of the corresponding send." Both directions → the hand-off is fully ordered. | None. (Plain-language "happened-before" gloss is acceptable; the spec's exact term is "synchronized before.") |
| 14 | (L348, L361, L365) Buffered channel cap N: sender deposits up to N without blocking; (N+1)th send blocks; "absorbs bursts up to N, then blocking backpressure". | CORRECT | go.dev/ref/mem: "The kth receive from a channel with capacity C is synchronized before the completion of the (k+C)th send" — capacity-N admits N sends before the next blocks. | None. |
| 15 | (L558–582 UnderTheHood) `hchan` struct fields: qcount, dataqsiz, buf (circular), elemsize, closed, sendq, recvq, lock; "circular buffer". | CORRECT (explicitly "simplified") | go.dev/src/runtime/chan.go: `qcount uint // total data in the queue`; `dataqsiz uint // size of the circular queue`; `buf unsafe.Pointer // points to an array of dataqsiz elements`; `elemsize uint16`; `closed uint32`; `sendq waitq // list of send waiters`; `recvq waitq // list of recv waiters`; `lock mutex`. | None. The chapter omits `elemtype`, `sendx`, `recvx`, `timer`, `bubble` but says "simplified… key fields" — acceptable per audit guide. |
| 16 | (L574–580) On a full/unbuffered send: lock hchan → create `sudog` → enqueue on `sendq` → park (gopark) → unlock+schedule; "the scheduler is the channel's blocking mechanism… no OS mutex, no kernel involvement for the blocking itself". | CORRECT | go.dev/src/runtime/chan.go chansend: `mysg := acquireSudog()` … `c.sendq.enqueue(mysg)` … `gopark(chanparkcommit, unsafe.Pointer(&c.lock), reasonChanSend, …)`. Blocking is user-space scheduler (gopark/goready), receiver wakes sender. | None. |
| 17 | (L545, L548, L552–554) Nil channel "blocks forever on both send and receive"; useful for disabling a `select` case. | CORRECT | go.dev/ref/spec (Receive/Send): receive from a nil channel blocks forever; send on a nil channel blocks forever. Nil-ing a select case to disable it is the documented idiom. | None. |
| 18 | (L477, L479, L539) Closed channel: already-buffered values remain readable; `v, ok := <-ch` → ok=false when closed-and-empty; only the sender closes; double-close/close-from-multiple panics. | CORRECT | go.dev/ref/spec: receive from a closed channel returns the zero value with ok=false (after buffer drained); sending on a closed channel causes a run-time panic; closing an already-closed (or nil) channel panics. | None. |
| 19 | (L604, L607, L650, L711) `select`: blocks until a case is ready; "If multiple cases are ready simultaneously, Go picks one at random (not the first written)" / "pseudo-randomly". | CORRECT | go.dev/ref/spec Select: "If one or more of the communications can proceed, a single one that can proceed is chosen via a uniform pseudo-random selection." Landmine correctly avoided (NOT "first ready"). | None. |
| 20 | (L607, L716, L722) `default` case makes select non-blocking — fires immediately if no channel is ready. | CORRECT | go.dev/ref/spec Select: "Otherwise, if there is a default case, that case is chosen. If there is no default case, the 'select' statement blocks until at least one of the communications can proceed." | None. |
| 21 | (L732, L745) `time.After(d)` returns `<-chan time.Time`, sends once after d; "creates a new timer every call, not a goroutine"; "Since Go 1.23, unreferenced timers can be garbage-collected before they fire, so this is no longer the old 'timer leak' trap." | CORRECT | pkg.go.dev/time func After: "It is equivalent to NewTimer(d).C. Before Go 1.23… the underlying Timer would not be recovered by the garbage collector until the timer fired… As of Go 1.23, the garbage collector can recover unreferenced, unstopped timers." | None. Excellent version-pinning. |
| 22 | (L745) "In hot paths, reuse a `time.Timer` with `Reset()`". | CORRECT (advice) | pkg.go.dev/time Timer.Reset; standard hot-loop guidance. Not a falsifiable internals claim. | None. |
| 23 | (L821) "Closing a channel broadcasts to all receivers simultaneously — every goroutine blocked on `<-done` unblocks at once." | CORRECT | go.dev/src/runtime/chan.go closechan walks `recvq`, readies every waiter; go.dev/ref/spec: each gets zero value, ok=false. Broadcast-on-close is correct. | None. |
| 24 | (L840) `sync.Mutex` binary lock, one holder; `sync.RWMutex` "many concurrent readers but only one writer". | CORRECT | pkg.go.dev/sync Mutex/RWMutex semantics. Standard. | None. |
| 25 | (L955–971 ExecTimeline) After Unlock, "parked goroutines wake one at a time… the lock simply changes hands down the line." | IMPRECISE (animation simplification) | go.dev/src/runtime — `sync.Mutex` has a normal mode (FIFO-ish handoff with barging) and a starvation mode (strict FIFO handoff, triggered after 1ms wait). Waiters do NOT strictly wake "in turn" in normal mode; an arriving goroutine can barge. The animation depicts strict in-order handoff. | Add a one-line "simplified" note: real Mutex allows a newly-arriving goroutine to barge ahead in normal mode; strict FIFO only kicks in under starvation (>1ms wait). Order shown is illustrative, not guaranteed. |
| 26 | (L880, L927, L1005, L1033, L1537, and all `wg.Go` GoPlayground blocks) `wg.Go(func())` "Go 1.25"; does Add(1)+spawn+Done in one call; "must be called before the goroutine starts". | CORRECT | go.dev/doc/go1.25: "The new `WaitGroup.Go` method makes the common pattern of creating and counting goroutines more convenient." (Added 1.25; encapsulates Add/go/Done.) | None for the claim. **BUT see #27 (runnability).** |
| 27 | Runnability: every `<GoPlayground>` using `wg.Go` (L889, L1075, L1161, L1295, L1449-ish solutions, L1517) requires Go ≥ 1.25 to compile. Prompt asserts Codapi runs Go 1.24, so these blocks won't run. | UNVERIFIABLE (exact Codapi version) / STRUCTURAL DEFECT (load-bearing) | Repo wires the hosted widget `@antonz/codapi@0.19.10` (app/layout.tsx) against Codapi's public Go sandbox; the live sandbox's Go version is not a fetchable static URL, so I cannot certify "1.24" by fetch. Independent of the exact number: `wg.Go` is a **Go 1.25** API, so any GoPlayground here will `undefined: wg.Go` / not compile on any runtime < 1.25. Multiple runnable blocks in this chapter depend on it. | Either (a) confirm/upgrade the Codapi Go image to ≥1.25 before relying on `wg.Go` in runnable cells, or (b) keep the prose `wg.Go` teaching but make the **runnable** `<GoPlayground>` variants use the pre-1.25 `wg.Add(1)`/`defer wg.Done()` form so they execute on the current sandbox. This is the single highest-impact finding. |
| 28 | (L1033) "If the goroutine starts and calls `wg.Done()` before `wg.Add(1)` lands, you underflow and panic." | CORRECT | pkg.go.dev/sync WaitGroup: "Note that calls with a positive delta that start when the counter is zero must happen before a Wait." Negative counter panics ("sync: negative WaitGroup counter"). | None. |
| 29 | (L1118, L1145, L1208–1212 QuickCheck) `sync.Once`: runs f exactly once across all callers; concurrent callers block until first f returns; then no-ops. | CORRECT | pkg.go.dev/sync Once.Do: "Do calls the function f if and only if Do is being called for the first time for this instance… If f panics, Do considers it to have returned." go.dev/ref/mem: "The completion of a single call of f() from once.Do(f) is synchronized before the return of any call of once.Do(f)." Answer index 1 correct. | None. |
| 30 | (L1205 Gotcha) "Never copy a Mutex, WaitGroup, or Once after first use." | CORRECT | pkg.go.dev/sync: "A Mutex must not be copied after first use." (same for WaitGroup/Once); `go vet` copylocks enforces. | None. |
| 31 | (L1495 Gotcha) Loop-variable capture "pre-Go 1.22… all goroutines captured the same `v`… In Go 1.22+, loop variables are per-iteration so this bug is gone." | CORRECT | go.dev/doc/go1.22: "Previously, the variables declared by a 'for' loop were created once and updated by each iteration. In Go 1.22, each iteration of the loop creates new variables, to avoid accidental sharing bugs." | None. (Note: chapter still writes defensive `v := v` / `id := i` captures throughout — harmless and correct under 1.22+, just no longer required.) |
| 32 | (L1151, L1191, L1633, L1762) `for range 5` / `for range 1000` / `for range numWorkers` — range-over-integer. | CORRECT | go.dev/doc/go1.22: "'For' loops may now range over integers." | None. (Same Go 1.22+ floor as `wg.Go`'s 1.25 — these compile on 1.22, but co-located `wg.Go` still forces 1.25; see #27.) |
| 33 | (L1401 caption) Worker pool wall time "~3 × 10ms = ~30ms instead of 9 × 10ms = 90ms". | CORRECT (illustrative) | 9 jobs / 3 workers = 3 sequential batches × 10ms each = ~30ms; arithmetic and model sound (ignores scheduling jitter, which is fine for the lesson). | None. |
| 34 | (L1498–1503 Callout, L1934 Recap) "Go 1.26 adds an experimental `goroutineleak` pprof profile at `/debug/pprof/goroutineleak`… Enable it with `GOEXPERIMENT=goroutineleakprofile`; it is not on by default." | CORRECT | go.dev/doc/go1.26: profile "is named `goroutineleak`… available as a net/http/pprof endpoint at `/debug/pprof/goroutineleak`"; "enabled by setting `GOEXPERIMENT=goroutineleakprofile` at build time"; "only considered an experiment for the purposes of collecting feedback." (Detects goroutines "blocked on some concurrency primitive… that cannot possibly become unblocked.") | None. Strong, accurate version-pin. |
| 35 | (L1643–1645 Callout) "Go 1.25 introduced `testing/synctest`, which runs goroutines on a virtual clock". | CORRECT | go.dev/doc/go1.25: "The new `testing/synctest` package provides support for testing concurrent code… Within the bubble, time is virtualized: time package functions operate on a fake clock…" | None. (1.25 is GA; it was an experiment in 1.24.) |
| 36 | (L548 Define Deadlock) Runtime detects all-goroutines-asleep and panics "all goroutines are asleep — deadlock!"; "a `sync.Mutex` locked twice by the same goroutine" is a deadlock trigger. | CORRECT | go.dev/src/runtime: `checkdead` → "all goroutines are asleep - deadlock!" Self-relock of a non-reentrant `sync.Mutex` blocks the goroutine (deadlock for that G). | None. (Minor: runtime message uses a hyphen "asleep - deadlock"; chapter uses an em/en dash "asleep — deadlock". Cosmetic, not falsifiable.) |
| 37 | (L1641 Exercise 3 solution) "`go run -race main.go` would fire… race detector". | CORRECT (Codapi caveat noted in audit guide) | Race detector is real; `-race` works under `go run`. Per `_ACCURACY_AUDIT.md`, Codapi runs plain `go run` (no `-race`), so this `-race` invocation is illustrative, not runnable in the sandbox — acceptable as prose. | None. |

## Landmine sweep (explicitly checked — all PASS)

- "Goroutines are OS threads" → AVOIDED. Chapter teaches M:N multiplexing explicitly (L227–238, L1928): "multiplexes thousands of goroutines onto a handful of OS threads via work-stealing." CORRECT.
- "GOMAXPROCS defaults to NumCPU" → AVOIDED. Pinned to Go 1.25 container-awareness (L204, L208, L220). CORRECT.
- "`select` picks the first ready case" → AVOIDED. Says uniform pseudo-random / "not the first written" (L604, L650, L711). CORRECT.
- "Reading a nil map panics" / float money → N/A (not in chapter); money is integer cents throughout (999¢ etc.). CORRECT.
- "`defer` is slow" → not claimed; chapter uses `defer mu.Unlock()` idiomatically without cost claims. CORRECT.

## Net verdict

- CORRECT (silently or tabled): **33** of the 37 tabled rows are CORRECT, plus all 5 landmine-sweep items.
- IMPRECISE: **3** — #5 (CPU affinity not a 1.25 GOMAXPROCS input), #25 (Mutex strict-FIFO animation simplification), and the "synchronized before" vs "happened-before" gloss (#13, sub-note only).
- UNVERIFIABLE / STRUCTURAL: **1** — #27 (`wg.Go` GoPlayground blocks require Go 1.25; won't compile on a <1.25 Codapi sandbox). The exact Codapi version could not be fetched; the 1.25 API floor is certain.
- WRONG: **0**.
- Not-directly-fetched (mechanism corroborated): #8 steal-half exact line (proc.go truncated by fetcher; per-P `[256]` array confirmed, mechanism not in doubt).

## Sources fetched (URLs actually opened)

1. https://go.dev/doc/go1.25 — WaitGroup.Go, GOMAXPROCS cgroup-awareness, testing/synctest
2. https://go.dev/ref/spec — Select statements ("uniform pseudo-random selection"), default case
3. https://go.dev/ref/spec#Receive_operator — closed/nil channel receive & send, close panics
4. https://go.dev/ref/mem — channel happens-before ("synchronized before"), unbuffered + kth-receive rules
5. https://go.dev/src/runtime/chan.go — hchan struct, chansend sudog/sendq/gopark
6. https://go.dev/src/runtime/stack.go — stackMin = 2048 (2 KB goroutine stack)
7. https://go.dev/src/runtime/runtime2.go — type p struct, `runq [256]guintptr`
8. https://go.dev/doc/go1.14 — asynchronous preemption (signals)
9. https://go.dev/doc/go1.22 — per-iteration loop variables, range-over-int
10. https://go.dev/doc/go1.23 — Timers/Tickers GC'd before firing (time.After leak no longer)
11. https://go.dev/doc/go1.26 — experimental goroutineleak pprof profile, GOEXPERIMENT=goroutineleakprofile
12. https://pkg.go.dev/time — func After doc (NewTimer(d).C; Go 1.23 GC note)
13. (attempted, fetcher-truncated/JS-only, NOT certified): go.dev/src/runtime/proc.go, cs.opensource.google + raw.githubusercontent proc.go mirrors for runqgrab "steal half"

One-line tally: 37 claims tabled — 33 CORRECT, 3 IMPRECISE, 1 UNVERIFIABLE/structural (wg.Go runnability), 0 WRONG; all 5 landmines avoided; 13 source URLs fetched.

Frontmatter unchanged: yes (report-only; no edits applied).
