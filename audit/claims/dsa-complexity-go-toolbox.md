# Accuracy audit — dsa-complexity-go-toolbox

Chapter: `content/appendix/01-dsa-complexity-go-toolbox.mdx`
Mode: REPORT ONLY (no edits applied). Tier-3 (appendix/DSA) — but algorithmic complexity / Go-library claims are hard, falsifiable, so audited rigorously per the fan-out brief.

Summary: This is a complexity-intuition + Go-toolbox chapter. The textbook Big-O claims are all derivable-correct. The Go-specific internals (slice growth, Swiss-table map, `container/heap` `O(log n)`, generics GC-shape stenciling, `sort.Slice` NOT stable) are all stated correctly — notably the chapter correctly flags that `sort.Slice` is unstable and that `slices.SortStableFunc` is the stable variant, dodging the headline landmine. The few flags are version-pin / phrasing imprecisions, not wrong mechanisms. **0 WRONG. 0 UNVERIFIABLE. 3 IMPRECISE/version-pin. ~30 spot-checked claims CORRECT.**

## Audit log (flagged rows only; confirmed-correct claims summarized after)

| # | Claim (as written) | Verdict | Source (fetched) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | `<UnderTheHood>` "**Growth strategy (as of Go 1.24):** For small slices (cap below ~256), the runtime doubles capacity… For larger slices it shifts to a 1.25× factor… The exact threshold and formula live in `runtime/slice.go`" | CORRECT | go.googlesource.com `runtime/slice.go`: `const threshold = 256` + comment "Transition from growing 2x for small slices to growing 1.25x for large slices. This formula gives a smooth-ish transition between the two." Above threshold: `newcap += (newcap + 3*threshold) >> 2`. | none — threshold (256), factors (2× / 1.25×), file, and "exponential ⇒ amortized O(1)" all correct. (Minor: this growth formula dates to Go 1.18, not 1.24; "as of Go 1.24" is harmlessly conservative but the behavior is older.) |
| 2 | Define "current Go **doubles small slices and transitions toward ~1.25x growth for larger ones**" (line 349); Recap "Growth is about 2× for small slices and trends toward ~1.25× for large slices" (line 1405) | CORRECT | same as #1 | none |
| 3 | `<UnderTheHood>` "Go's `map` is a hash table. As of Go 1.24, the standard runtime uses a Swiss Table-style implementation: groups of eight key-value slots plus control bytes… This is open addressing/probing, not separate linked-list chaining." | CORRECT | go.dev/doc/go1.24: "a new builtin `map` implementation based on Swiss Tables." Groups-of-8 + control bytes is the `internal/runtime/maps` design (release notes don't enumerate the slot count, but it's accurate to the implementation). Correctly avoids the "chaining like Java" landmine and pins the 1.24 version. | none — pinned and accurate. (Group count "8" is impl detail, not in the release notes, but correct.) |
| 4 | `slices.SortFunc` "Go 1.21+, generic, no interface overhead"; comment `// negative = a before b` (line 760, 795) | CORRECT | pkg.go.dev/slices: "cmp(a, b) should return a negative number when a < b, a positive number when a > b and zero when a == b." `slices` package added in Go 1.21. | none |
| 5 | `<Callout>` "`sort.Slice` is NOT stable — equal elements can be reordered. Use `sort.SliceStable` or `slices.SortStableFunc` when the original relative order of equal elements must be preserved." | CORRECT | pkg.go.dev/sort: sort.Slice — "The sort is not guaranteed to be stable: equal elements may be reversed from their original order. For a stable sort, use SliceStable." pkg.go.dev/slices: SortStableFunc "sorts the slice x while keeping the original order of equal elements." | none — this is the headline landmine and the chapter gets it exactly right. |
| 6 | `<UnderTheHood>` generics "compiled via **GC shape stenciling** (as of Go 1.24): … one machine-code instantiation per shape, not per type. … all pointer types share one shape… A dictionary (essentially a vtable) carries type-specific behavior… This is the opposite of Java generics (type erasure + boxing)." | CORRECT | go.dev/blog/intro-generics + "Generics implementation: GC Shapes" design doc: instantiations are shared per GC shape; all pointer-shaped types share one shape; runtime dictionaries carry per-type info. Correctly avoids the "monomorphized like Rust/C++" landmine. | none. IMPRECISE nit: "dictionary (essentially a vtable)" — a gcshape dictionary is not literally a vtable (it carries type descriptors + sub-dictionaries, and method calls may still go through itab/interface); "essentially a vtable" is a loose analogy. Optional soften to "a per-instantiation table of type info." "as of Go 1.24" is again conservative (stenciling+dictionaries shipped 1.18). |
| 7 | `container/heap` "`O(log n)` push and pop"; Define "Push and Pop are both `O(log n)`" (line 613, 616) | CORRECT | pkg.go.dev/container/heap: Push — "The complexity is O(log n) where n = h.Len()." Pop — "The complexity is O(log n) where n = h.Len()." | none |
| 8 | Comment in interface snippet: `// heap.Interface embeds sort.Interface (Len, Less, Swap) plus Push and Pop. // You provide all five.` (line 621-627) | CORRECT | pkg.go.dev/container/heap: `type Interface interface { sort.Interface; Push(x any); Pop() any }`. Five methods. | none |
| 9 | ExecTimeline n=6 note: "O(n log n) at n=6: 6×2.6≈16 ops" and caption "At n=1000: … O(2ⁿ) = 10^301" | CORRECT (by derivation) | log₂6 ≈ 2.585, 6×2.585 ≈ 15.5 ≈ 16. 2^1000 ≈ 1.07×10^301. Both check out. | none |
| 10 | ExecTimeline n=1 note: "O(log n) at n=1 → 0 ops — log₂(1)=0" | CORRECT (by derivation) | log₂1 = 0. Pedantically a real binary search on a 1-element list does 1 comparison, but as a growth-class illustration (log₂1=0) it's fine and labeled as such. | none — within the stated framing. |
| 11 | Two-sum Define: "Go's `append` … when the backing array is full, the runtime allocates a **2× array** and copies everything" (line 119); SliceAnim notes say "doubled: cap 4" then "cap 8" (line 325-339) | CORRECT (for the small-slice regime shown) | runtime/slice.go: doubling holds for cap < 256, which covers every example here (caps 2→4→8). The animations stay entirely in the doubling regime, so "2×" is accurate for what's shown. The UnderTheHood correctly notes the 1.25× transition for large slices. | none — but note the bare "2×" in the Define would be wrong if generalized; it is saved by context (all examples are tiny) and by the later UnderTheHood. Optional: add "(small slices; larger slices grow ~1.25×)" to the Define for consistency with line 349. |

## Spot-checked and CORRECT (not individually flagged)

Big-O class table (O(1)/O(log n)/O(n)/O(n log n)/O(n²)/O(2ⁿ)) and the Gotcha "O(n log n) is total work across log n levels, not O(n)×O(log n)" — correct. Worst/average/amortized framing — correct (hash lookup O(1) avg / O(n) worst; amortized append O(1)). Loop-nest analysis (nested O(n)→O(n²); sequential blocks add; sort dominates scan) — correct. QuickCheck answers (nested loops + sort → O(n²) dominates; duplicate detection Ω(n)/O(n) tight) — correct. twoSum O(n)/O(n) — correct. `map[T]struct{}` zero-byte value vs `map[T]bool` 1 byte — correct (empty struct is size 0). Map iteration order unspecified + deliberately randomized + not goroutine-safe (race ⇒ fatal) — correct per spec + runtime. Stack push/pop O(1) amortized via slice — correct. Exercise complexities: pairwiseSum O(n²)/O(1) with O(n) math improvement — correct; isValid O(n)/O(n) — correct; intersection O(n+m)/O(n+k) — correct; topKFrequent O(n log k)/O(n+k) — correct (and the "O(n log k) beats O(n log n) when k≪n" justification is right); BoundedStack O(1) ops — correct.

## Sources fetched
- https://pkg.go.dev/sort
- https://pkg.go.dev/slices
- https://pkg.go.dev/container/heap
- https://go.dev/doc/go1.24
- https://go.dev/doc/go1.21
- https://go.googlesource.com/go/+/refs/heads/master/src/runtime/slice.go
- https://go.googlesource.com/go/+/refs/heads/master/src/sort/zsortinterface.go (pdqsort confirmation, used in Ch.2)

## Tally
WRONG: 0 · OUTDATED: 0 · IMPRECISE: 2 (#6 "vtable" analogy; #11 generalizable "2×") · version-pin nit: 1 (#1/#6 "as of Go 1.24" conservative for 1.18-era behavior) · UNVERIFIABLE: 0 · CORRECT: ~30 spot-checked + all 11 flagged rows resolve to CORRECT-with-nuance.
Worst finding: none material — the most notable is that "as of Go 1.24" is attached to behavior that actually predates 1.24 (slice growth: 1.18; GC-shape stenciling: 1.18), which could mislead a reader into thinking these are recent changes. Frontmatter unchanged: yes (report-only).
