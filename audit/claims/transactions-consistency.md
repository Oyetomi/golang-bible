# Accuracy audit — transactions-consistency

Chapter: `content/part-3/03-transactions-consistency.mdx`
Tier 2 (correctness-critical: ACID isolation levels + saga canon). REPORT ONLY — no content file was edited.

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (not applied) |
|---|---|---|---|---|
| 1 | "Postgres defaults to **Read Committed**." (Define, prose, table, QuickCheck) | **CORRECT** | postgresql.org/docs/current/transaction-iso.html: "Read Committed is the default isolation level in PostgreSQL." | None. |
| 2 | "The SQL standard defines four isolation levels — Read Uncommitted, Read Committed, Repeatable Read, and Serializable." | **CORRECT** | Same source describes "the four standard transaction isolation levels"; names match. | None. |
| 3 | Isolation×anomaly table — **Dirty Read** column: Read Uncommitted "possible", others "prevented"; "Postgres doesn't allow dirty read at any isolation level." | **CORRECT** | Table 13.1: Read uncommitted dirty read "Allowed, but not in PG"; "PostgreSQL's Read Uncommitted mode behaves like Read Committed." So PG prevents dirty reads at every level. Chapter's "prevented" rows match PG behavior. | None. (The chapter table lists Read Uncommitted dirty read as "possible," which matches the SQL *standard*; a footnote that PG never allows it would be ideal but the prose already says so.) |
| 4 | Table — **Non-Repeatable Read**: RU/RC "possible", RR/Serializable "prevented". | **CORRECT** | Table 13.1: Read committed nonrepeatable read "Possible"; Repeatable read "Not possible"; Serializable "Not possible". | None. |
| 5 | Table — **Phantom Read**: RU/RC "possible", RR "prevented*", Serializable "prevented"; footnote "Postgres's Repeatable Read also prevents phantoms, exceeding the SQL standard." | **CORRECT** | Table 13.1: Repeatable read phantom "Allowed, but not in PG"; docs: "PostgreSQL's Repeatable Read implementation does not allow phantom reads. This is acceptable under the SQL standard." | None. |
| 6 | Table — **Lost Update** column: RU "possible", RC "possible", RR "prevented", Serializable "prevented". | **CORRECT** (with note) | The PG docs do not name "lost update" in Table 13.1 (their 4th column is "Serialization Anomaly"). But the substance is right per the SQL/ANSI canon and PG behavior: at RC a read-modify-write can lose updates; at RR, "the repeatable read transaction will be rolled back with the message `ERROR: could not serialize access due to concurrent update` … When an application receives this error message, it should abort … and retry" — i.e. RR *prevents* a silently lost update (it errors instead). Serializable likewise. | Optional: add a one-line note that PG's own table calls the 4th column "Serialization Anomaly," and that RR "prevents" lost updates by raising a serialization error (forcing retry), not by silent serialization. The verdicts in the column are correct. |
| 7 | "Read Committed is not safe for concurrent balance updates … two concurrent transactions can each read the same balance, compute the same new balance, and both write it back — losing one update." | **CORRECT** | Standard lost-update under RC; PG docs confirm RC re-reads on direct `UPDATE … SET x = x - n` but a read-then-write-back-computed-value (the pattern shown) is unprotected. Matches the canon. | None. (Accurate caveat: a single in-place `UPDATE balance = balance - n` is *not* lost at RC because PG re-evaluates; the chapter's danger case is the SELECT-then-UPDATE-with-app-computed-value, which is exactly what it depicts.) |
| 8 | `SELECT ... FOR UPDATE` "reads the row AND acquires an exclusive row-level lock"; "Any other transaction that tries to SELECT ... FOR UPDATE the same row will block until your transaction commits or rolls back." | **CORRECT** | postgresql.org/docs/current/transaction-iso.html: "UPDATE, DELETE, SELECT FOR UPDATE, and SELECT FOR SHARE commands behave the same as SELECT in terms of searching for target rows"; the would-be locker "will wait for the first updating transaction to commit or roll back." (FOR UPDATE locking semantics also in sql-select / explicit-locking docs.) | None. |
| 9 | UnderTheHood: MVCC — "Every row has a system column xmin (the transaction ID that created it) and xmax (the transaction ID that deleted it)"; readers see old versions, writers create new; "Postgres reads don't block writes and vice versa at Read Committed." | **CORRECT** | PG MVCC: `xmin`/`xmax` are the system columns recording inserting/deleting xid; MVCC means "readers and writers don't block each other" (transaction-iso / mvcc-intro). Matches. | None. |
| 10 | UnderTheHood: deadlock — "Postgres detects deadlocks by periodically traversing the wait-for graph … picks one victim (**the transaction with the lower cost to abort**) and rolls it back with `ERROR: deadlock detected`." | **WRONG** (victim-selection clause) | postgresql.org/docs/current/explicit-locking.html: "PostgreSQL automatically detects deadlock situations and resolves them by aborting one of the transactions involved"; "**Exactly which transaction will be aborted is difficult to predict and should not be relied upon.**" There is no documented "lower cost to abort" victim heuristic; the docs explicitly say it is unpredictable. (The `ERROR: deadlock detected` message and wait-for-graph detection are correct.) | Delete "(the transaction with the lower cost to abort)". Replace with: "rolls back one of the involved transactions — which one is not guaranteed and should not be relied upon." Keep the rest. |
| 11 | Define Deadlock: "Postgres detects this within `deadlock_timeout` (default **1 second**) and aborts one transaction with `ERROR: deadlock detected`." | **CORRECT** | postgresql.org/docs/current/runtime-config-locks.html: "The default is one second (1s)…"; explicit-locking confirms `ERROR: deadlock detected` and automatic abort. | None. |
| 12 | "always lock accounts in ascending order of their ID … prevents this entire class of [deadlock] bug." | **CORRECT** | explicit-locking.html: "The best defense against deadlocks is generally to avoid them by being certain that all applications … acquire locks on multiple objects in a consistent order." Deterministic ordering is the documented prevention. | None. |
| 13 | `SELECT FOR UPDATE` "is a database-level lock — not a Go-level mutex … Other Go processes hitting the same Postgres instance are also blocked … cluster-wide, not just in-process." | **CORRECT** | Row lock lives in the PG lock manager (server-side), shared across all clients/connections — not a per-process construct. Follows directly from row 8. ("Cluster-wide" = across all sessions on that DB; correct for a single primary.) | None. |
| 14 | Optimistic locking: version column, `UPDATE … WHERE id=$2 AND version=$3`; "if zero rows are affected, another transaction won the race … retry." | **CORRECT** | Standard compare-and-swap optimistic concurrency; `RowsAffected()==0` ⇒ version moved ⇒ conflict. Conceptually correct and matches PG `UPDATE` row-count semantics. | None. |
| 15 | UnderTheHood: `db.BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelSerializable})`. | **CORRECT** | pkg.go.dev/database/sql: `TxOptions{Isolation IsolationLevel}`; `LevelSerializable` constant exists; doc example: `db.BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelSerializable})`. Exact match. | None. |
| 16 | `for range workers` / `for attempt := range maxRetries` "uses Go 1.22's range-over-integer syntax, unchanged in Go 1.26." | **CORRECT** | Range-over-integer was added in Go 1.22 (`range` over an int). Still valid syntax in current Go. | None. |
| 17 | Saga: "a sequence of local ACID transactions, one per service … if any step fails, every preceding step is reversed by a compensating action"; achieves "eventual consistency." | **CORRECT** | microservices.io/patterns/data/saga.html: "A saga is a sequence of local transactions"; "if a local transaction fails … the saga executes a series of compensating transactions that undo the changes that were made by the preceding local transactions"; "maintain data consistency … without using distributed transactions." | None. |
| 18 | "A compensating action is NOT a `ROLLBACK` … it's a new, committed transaction that reverses the business effect … both appear in the audit log." | **CORRECT** | Same source: "Lack of automatic rollback - a developer must design compensating transactions that explicitly undo changes." A compensation is forward-committed, not a DB rollback. Matches canon (and the append-only ledger requirement from ch. 2). | None. |
| 19 | "Lack of isolation (the 'I' in ACID)" is implied by saga eventual consistency. | **CORRECT** | Same source explicitly lists "Lack of isolation (the 'I' in ACID)" as a saga drawback. | None. |
| 20 | Orchestration vs choreography definitions (central coordinator vs event-driven). | **CORRECT** | Same source: "Choreography - each local transaction publishes domain events that trigger local transactions in other services"; "Orchestration - an orchestrator (object) tells the participants what local transactions to execute." | None. |
| 21 | Outbox: "every state transition … written to an `outbox` table inside the same local ACID transaction … a separate relay … at-least-once delivery and idempotent handlers." Frames exactly-once as at-least-once + idempotency (landmine check). | **CORRECT** | Transactional-outbox canon (microservices.io outbox pattern); the chapter correctly says "at-least-once delivery and idempotent handlers" — it does **not** claim true exactly-once *delivery*, avoiding the Part-3 landmine. | None. |

### CORRECT (verified, not individually flagged): 20

Rows 1–9, 11–21 verified CORRECT against fetched sources. Only hard error: row 10 (deadlock victim-selection clause). Row 6 correct-with-note.

## Sources fetched
- https://www.postgresql.org/docs/current/transaction-iso.html (fetched twice: isolation table + lost-update/RR-serialization-error detail)
- https://www.postgresql.org/docs/current/runtime-config-locks.html (deadlock_timeout default)
- https://www.postgresql.org/docs/current/explicit-locking.html (deadlock detection + victim selection)
- https://microservices.io/patterns/data/saga.html (saga semantics)
- https://pkg.go.dev/database/sql#TxOptions (LevelSerializable / BeginTx)

## Tally
Flagged: 2 (1 WRONG, 1 CORRECT-with-note) + 20 verified CORRECT. Worst finding: **the UnderTheHood claim that Postgres picks the deadlock victim "with the lower cost to abort" is wrong — the PG docs state which transaction is aborted "is difficult to predict and should not be relied upon"; there is no documented cost-based victim heuristic.**
