# Accuracy audit — dsa-heaps-priority-queues

Chapter: `content/appendix/06-dsa-heaps-priority-queues.mdx`
Mode: REPORT ONLY (no edits applied). Tier-3 (appendix/DSA); heap complexities, index arithmetic, sift invariants, container/heap semantics, and the two-heap median audited rigorously.

Summary: This is the most internals-dense DSA chapter and it is accurate throughout. Heap index arithmetic (`parent=(i-1)/2`, `children=2i+1,2i+2`), sift-up/sift-down O(log n), peek O(1), heapify/`heap.Init` O(n) (not O(n log n)), top-K O(n log k)/O(k), merge-K O(N log K), and the two-heap running median O(log n) insert / O(1) query are all correct. The `container/heap` description (owns no storage, five-method interface, predates generics 2011/Go 1.0, `Pop` cuts the tail / `heap.Pop` is `Remove(h,0)`, `old[n-1]=nil` to avoid leaking struct/pointer elements) is verified against the package docs. The animations' index walks are arithmetically correct. Flags are cosmetic. **0 WRONG, 0 UNVERIFIABLE, 2 IMPRECISE, ~26 CORRECT.**

## Audit log (flagged rows; confirmed-correct summarized after)

| # | Claim (as written) | Verdict | Source (fetched) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | `<UnderTheHood>` "**Heapify** … runs in **O(n)** — not O(n log n)… call sift-down from the last internal node down to the root. About half the nodes are leaves… Summing the geometric series gives O(n). Go's `heap.Init` uses this exact approach (as of Go 1.24)." (line 265) | CORRECT | pkg.go.dev/container/heap: `Init` — "The complexity is O(n) where n = h.Len()." The bottom-up sift-down heapify is the standard O(n) construction (Σ over levels of (nodes × height) telescopes to O(n)). Version-pin "as of Go 1.24" is harmlessly conservative (Init has been O(n) since Go 1.0). | none — both the O(n) claim and the "why" are correct. |
| 2 | `container/heap`: Push/Pop "both `O(log n)`" (Define line 131, 139) + peek O(1) (line 170, 264) + table "Min-heap of size k … O(n log k)" (line 800) | CORRECT | pkg.go.dev/container/heap: Push — "O(log n) where n = h.Len()"; Pop — "O(log n) where n = h.Len()." Peek = read index 0 = O(1). n elements each doing ≤1 push+pop on a size-k heap ⇒ O(n log k). | none |
| 3 | Define "`container/heap` … provides heap logic but owns **no storage**. You hand it a slice that implements `heap.Interface` — five methods: `Len`, `Less`, `Swap` (from `sort.Interface`), plus `Push` … and `Pop` … It predates generics (Go 1.0, 2011) and uses `any`" + UnderTheHood (line 515) "written in 2011 for Go 1.0 and has not changed structurally since" | CORRECT | pkg.go.dev/container/heap: `type Interface interface { sort.Interface; Push(x any); Pop() any }`. The package is from Go 1.0 (2011) and uses `any` (formerly `interface{}`) for Push/Pop — pre-generics. Accurate. | none |
| 4 | QuickCheck "in container/heap, `Pop()` removes **the last element of the backing slice**" + explain "heap.Pop first swaps the root with the last element… then calls your Pop() which cuts the tail" (line 543) + Define "If you forget to nil out the last element in `Pop`, you leak memory in long-running services. The `old[n-1] = nil` line in struct heaps is not optional." (line 135) | CORRECT | pkg.go.dev/container/heap: `heap.Pop` is documented "equivalent to Remove(h, 0)" — it moves the root to the tail (via Swap + sift-down) then calls your `Pop()` which removes the tail. For `[]*T`/struct-pointer heaps, not niling the popped slot retains the pointer in the backing array ⇒ leak; the PriorityQueue example correctly does `old[n-1] = nil`. | none — both the mechanism and the leak caveat are correct. |
| 5 | Heap index arithmetic: `parent(i)=(i-1)/2`, `left=2i+1`, `right=2i+2`; "array representation only works for a **complete** binary tree" (lines 164-173); GraphAnim/Scene/ExecTimeline index walks (e.g. "parent(5)=(5−1)/2=2", "parent(6)=(6-1)/2=2") | CORRECT (by derivation) | Standard 0-based array-heap arithmetic; every concrete index computation in the animations checks out (parent(6)=2, parent(5)=2, parent(2)=0, etc.). Completeness requirement for the array mapping is correct, and Push-appends / Pop-swaps-last preserve completeness. | none |
| 6 | Sift-up/Define: insert appends at end then swaps with parent while smaller (min-heap) — "O(log n) swaps upward"; sift-down moves last element to root then swaps with the **smaller child** until both children larger — "O(log n) swaps downward" (line 131, 197-199) | CORRECT | Standard binary-heap repair ops; height ⌊log₂ n⌋ ⇒ O(log n) each. "Swap with the smaller child" (min-heap) is the correct rule (swapping with the larger child would violate the heap property). | none |
| 7 | Two-heap running median: `lo` max-heap (smaller half), `hi` min-heap (larger half), invariant `||lo|−|hi|| ≤ 1`; median = larger root, or `(lo.root+hi.root)/2` when equal; AddNum O(log n), FindMedian O(1) (lines 991-1198) | CORRECT | The two-heap partition is the canonical O(log n)-insert / O(1)-query running median; the boundary values sit at the two roots. The rebalance (`lo.Len() > hi.Len()+1` ⇒ move root across) maintains the size invariant. ExecTimeline trace for [5,15,1,3] (→ lo=[1,3], hi=[5,15], median (3+5)/2=4.0) is arithmetically correct. QuickCheck "max-heap for lo so its root (largest small value) is O(1)-readable" — correct. | none |
| 8 | KLargest "returns the k largest values" with comment `// [8 11 12] in heap order` (line 605) and `result := make([]int, h.Len()); copy(result, *h)` | CORRECT (with caveat the chapter states) | A size-k min-heap retains the k largest; dumping the backing slice yields them in *heap array order*, not sorted — the chapter explicitly says "in heap order," so it's not claiming sorted output. Correct. | none — the "heap order" caveat is the right disclaimer. |
| 9 | Lab starter ships two intentional bugs: `Less` returns `h[i] > h[j]` ("BUG: this is a max-heap — flip to < for min-heap") and `Add` never evicts ("BUG: never evicts"); hints fix both (lines 1682-1709, 1791-1793) | CORRECT | For a top-K-largest tracker you need a **min**-heap of size k (root = weakest survivor, evicted when a larger value arrives). The shipped `>` makes a max-heap (wrong: would evict the largest), and the missing `if h.Len() > k { heap.Pop }` lets it grow unbounded. Both fixes are correct and the verifier checks the size invariant. | none — the lab's "wrong Less direction" is exactly the heap landmine, used pedagogically. |
| 10 | Exercise 5 sliding-window median: comment `loSz := lo.Len() - deleted[(*lo)[0]] // approximate; prune for exact` then `_ = loSz` (lines 1615-1618) | IMPRECISE (dead/confusing code, not a correctness claim) | `loSz`/`hiSz` are computed, commented "approximate," then discarded with `_ =`; the actual rebalance uses `pruneMax/pruneMin` + size comparisons. Harmless (lazy-deletion median is correct) but the dead variables muddy the example. The stated O(n log k) is correct. | Optional: delete the unused `loSz`/`hiSz` block; it's vestigial and the "approximate; prune for exact" comment implies a precision the code doesn't use. |
| 11 | Merge-K: "naïve flatten+sort is O(N log N)… heap approach is **O(N log K)**… heap never holds more than K elements" (lines 815, 970) | CORRECT (by derivation) | K-way merge with a K-element min-heap: each of N elements does one O(log K) push and one O(log K) pop ⇒ O(N log K); heap size bounded by K (one cursor per list). Correct, and the K≪N win is real. | none |

## Spot-checked and CORRECT (not individually flagged)

Min-heap = parent ≤ both children, root = min; max-heap flips `Less` to `>` — correct (and the BeforeAfter min/max `Less` is the only change needed). 7-element heap `[1,3,2,7,4,5,6]` → drawn tree and the parent/child(0)=1,2 arithmetic — correct. Sift-up GraphAnim (insert 2 into [1,3,5,7,4]: parent(5)=2 holds 5, 2<5 swap, then parent(2)=0 holds 1, 2<1 false → stop, 1 swap) — arithmetically correct. Sift-down ExecTimeline (extract-min from [1,3,2,7,4,5]: move last to root, swap with smaller child) — correct. PriorityQueue over `[]*Item` with `Less` on priority and `old[n-1]=nil` — correct. Generic MinHeap[T] adapter "valid in interviews" — correct (compiles; the `x.(T)` assertion works because heap.Push passes the value through `any`). Top-K table (sort O(n log n)/O(1); min-heap O(n log k)/O(k); quickselect O(n) avg) — correct. "min-heap for k-largest because root is the smallest of the k candidates / weakest competitor" — correct. TopKFrequent freq-map + size-k heap O(n log k) — correct. KthLargest-in-stream: size-k min-heap, root = k-th largest, Add O(log k) — correct (trace 4,5,8,2 then adds 3,5,10,9,4 → 4,5,5,8,8 verified). KClosest max-heap-of-k by **squared** distance (avoids math.Sqrt) O(n log k)/O(k) — correct. MergeKLists with `[]*ListNode` heap, advance via `node.Next` — correct, O(N log K). Task Scheduler greedy max-heap by frequency, n+1 cycle, O(N log U) with U≤26 — correct. Sliding-window median lazy deletion O(n log k) — correct (the deleted-map + prune-tops technique).

## Sources fetched
- https://pkg.go.dev/container/heap (Init O(n); Push/Pop O(log n); Pop ≡ Remove(h,0); Interface = sort.Interface + Push(any) + Pop() any) · https://go.dev/doc/go1.24 (version-pin context) · https://go.dev/doc/go1.21 (slices package, used elsewhere in appendix)

## Tally
WRONG: 0 · OUTDATED: 0 · UNVERIFIABLE: 0 · IMPRECISE: 2 (#1/#10 — #1 "as of Go 1.24" conservative for behavior present since Go 1.0; #10 vestigial dead variables in Exercise 5) · CORRECT: ~26 spot-checked + all 11 flagged rows resolve CORRECT.
Worst finding: none material. Every hard, falsifiable heap claim — index arithmetic, sift invariants, O(log n) ops, O(n) heapify, O(N log K) merge-K, O(1) median query — is correct and matches the `container/heap` docs. The only blemish is cosmetic dead code (`loSz`/`hiSz`) in the hardest exercise. Notably the Lab uses a backwards `Less` as its *intended* bug, teaching the exact min-vs-max landmine. Frontmatter unchanged: yes (report-only).
