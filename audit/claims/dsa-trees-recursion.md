# Accuracy audit — dsa-trees-recursion

Chapter: `content/appendix/05-dsa-trees-recursion.mdx`
Mode: REPORT ONLY (no edits applied). Tier-3 (appendix/DSA); recursion/tree complexities, BST invariants, DFS/BFS space bounds, and Go call-stack internals audited rigorously.

Summary: The whole chapter is mechanically clean. Tree-recursion complexities (O(n) time, O(h) space), DFS-vs-BFS memory asymmetry (O(height) vs O(width)), in-order-of-a-BST-is-sorted, and the global-bounds BST validation are all correct, and the chapter explicitly debunks the classic "check only immediate children" BST bug. The Go-internals claims (goroutine stacks start ~2 KB, grow by contiguous copying, contiguous stacks replaced segmented stacks in Go 1.4+, frame ~64–128 B, `slices.Reverse` Go 1.21, `math.MinInt`/`MaxInt`) are all verified accurate. Only minor phrasing flags. **0 WRONG, 0 UNVERIFIABLE, 2 IMPRECISE, ~24 CORRECT.**

## Audit log (flagged rows; confirmed-correct summarized after)

| # | Claim (as written) | Verdict | Source (fetched) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | `<UnderTheHood>` "As of Go 1.24, goroutine stacks start small (a **few KB**)… grow dynamically via stack copying" (line 263) and Callout "start small (**about 2 KB** in current Go implementations) and grow dynamically" (line 958) and "**each frame is typically 64–128 bytes**, so a million-node chain can use 64–128 MB just in stack frames" (line 263) | CORRECT (well-hedged) | runtime/stack.go: `stackMin = 2048` ("The minimum size of stack used by Go code"); `newstack` grows via `newsize := oldsize * 2` with contiguous `copystack`. "~2 KB" and "few KB" both accurate. The per-frame "64–128 bytes" figure is plausible/implementation-defined for these small functions and is hedged with "typically"; the derived 64–128 MB is arithmetic on that estimate, also hedged. | none — figures appropriately hedged. (Frame size is impl/optimizer-dependent; "typically" carries it.) |
| 2 | `<UnderTheHood>` "the runtime grows the stack by allocating a larger buffer and copying (**as of Go 1.4+ with contiguous stacks replacing the old segmented-stack design**). Growth is O(1) amortized per frame push/pop." (line 968) | CORRECT | Go switched from segmented to contiguous (copying) stacks in Go 1.4 — accurate version pin. Per-frame push/pop is O(1); occasional whole-stack copies amortize (doubling), so "O(1) amortized" is the right characterization. | none |
| 3 | "**BFS** holds at most one level… For a balanced binary tree, the widest level is the leaf level, which contains roughly **n/2 nodes**. So BFS space is O(n/2) = O(n) in the worst case." (line 638) and Define "Space cost is O(w), where w is the maximum width" (line 31) | CORRECT | A complete/perfect binary tree has ~n/2 leaves; the BFS queue holds at most one level, so peak queue ≈ n/2 = O(n) for a balanced tree, O(w) in general. Correct, and correctly contrasted with DFS O(h). | none |
| 4 | "**DFS** holds at most one root-to-leaf path… balanced tree O(log n)… degenerate tree O(n)" + ConceptCard "O(h)… Balanced: O(log n). Degenerate: O(n)" (line 246, 636) | CORRECT (by derivation) | Recursion/explicit-stack DFS keeps only the current path's frames ⇒ O(h); h = Θ(log n) balanced, Θ(n) degenerate. Correct. | none |
| 5 | "In-order traversal visits left → node → right… the BST property guarantees the output is sorted ascending by induction" (line 389) + QuickCheck (line 398) | CORRECT (by derivation) | BST invariant (all-left < node < all-right, recursively) ⇒ in-order yields a strictly ascending sequence. Standard, correctly justified. | none |
| 6 | BST validation: "checking only `node.Left.Val < node.Val < node.Right.Val` is **not sufficient**"; correct approach carries min/max bounds; "Right child: pass `node.Val` as new minimum, max unchanged" (lines 692-704, 847 QuickCheck) | CORRECT | The counterexample (node 4 in 5's right subtree with valid local children 3,6 but 4<5 globally) is a real failure of the local-only check; bound-propagation (`min < node.Val < max`, tighten max going left, tighten min going right) is the correct algorithm. The Lab even ships the buggy local-only version as the thing to fix. | none — exemplary handling of a classic landmine. |
| 7 | isValidBST uses strict `node.Val <= min || node.Val >= max` (rejects equals); bstInsert ignores duplicates (line 738, 753) | CORRECT (consistent convention) | The chapter defines BST as "strictly less / strictly greater" (line 648), so strict bounds and duplicate-ignoring insert are internally consistent. (A different course might allow duplicates on one side; this one doesn't, and says so.) | none — consistent with the stated invariant. |
| 8 | kthSmallest "O(h + k) time — you stop as soon as you've found the k-th node" (line 1099) | IMPRECISE (but defensible) | Iterative in-order with early stop: you descend the left spine (up to O(h)) and then visit k nodes, each costing amortized work; the standard tight bound is O(h + k). For a degenerate tree h can be O(n), but O(h+k) already captures that. Correct as written. | none — O(h+k) is the right bound; no change needed. |
| 9 | maxDepth ExecTimeline + "the work happens on the way back up… all four classics are O(n) — every node visited exactly once" (line 245) | CORRECT | maxDepth/sameTree/invert/symmetric each visit every node once ⇒ O(n) time, O(h) stack. Animation step order (descend to leaves, combine upward) matches recursion semantics. | none |
| 10 | Recap + UnderTheHood: "**heap-allocated BFS queues grow without bound**… the GC will clean up dequeued nodes, but the live portion lives on the heap and is subject to GC pause pressure" (line 642) | IMPRECISE (slightly loaded) | True that the live queue is heap-allocated and contributes to GC working set; "grow without bound" is only in the pathological wide-tree sense (the queue is still bounded by the widest level, O(w)). "subject to GC pause pressure" is directionally right but mild hand-waving. Not wrong. | Optional: "the live queue (bounded by the widest level, O(w)) lives on the heap and adds to GC working set." |

## Spot-checked and CORRECT (not individually flagged)

TreeNode zero value `nil` = "no child" = base case — CORRECT per spec (zero value of a pointer is nil). Recursive skeleton (base nil → identity; recurse L/R; combine) — correct. isSameTree/isMirror nil-guard-before-`.Val` (Gotcha) — correct (reading `.Val` on nil panics; both-nil and one-nil checks precede field access). DFS pre/in/post definitions and uses (pre=serialize, in=BST-sorted, post=delete/expr-eval) — correct. inOrderIterative left-spine-push pattern — correct. levelOrder BFS O(n) time / O(w) space with the `levelSize := len(queue)` snapshot trick — CORRECT (snapshotting before the inner loop isolates exactly one level). rightSideView / levelAverages variants — correct. BST search/insert/LCA all O(h) (O(log n) balanced, O(n) degenerate); "tree is NOT auto-balanced on insert" — correct. lcaOfBST (first split point) and lcaGeneral (post-order: both-sides-nonnil ⇒ root is LCA) — correct; the note "returning root when root==p doesn't mean p IS the LCA — info propagates up" is precisely right. countNodes `1+left+right` O(n)/O(h) — correct. hasPathSum leaf check `Left==nil && Right==nil`, O(n)/O(h) — correct. preOrderIterative "push right before left so left pops first (LIFO)" — correct. Recursive-vs-iterative DFS "same algorithm, O(n)/O(h), different memory manager (call stack vs heap slice)" — correct. zigzagLevelOrder O(n)/O(w), `slices.Reverse` Go 1.21+ — CORRECT (slices package added Go 1.21, per go.dev/doc/go1.21). `math.MinInt`/`math.MaxInt` as BST sentinels — correct (defined in math, Go 1.17+).

## Sources fetched
- https://go.googlesource.com/go/+/refs/heads/master/src/runtime/stack.go (stackMin=2048, contiguous copying growth) · https://go.dev/doc/go1.21 (slices package, slices.Reverse) · https://go.dev/ref/spec (nil zero value of pointers; in-order/BST reasoning)

## Tally
WRONG: 0 · OUTDATED: 0 · UNVERIFIABLE: 0 · IMPRECISE: 2 (#8 already-correct O(h+k) flagged only for completeness; #10 "grow without bound" loaded phrasing) · CORRECT: ~24 spot-checked + all 10 flagged rows resolve CORRECT.
Worst finding: none material. The most notable positive: the chapter ships the classic local-only BST-validation bug *as the lab's intentional defect* and teaches the correct bound-propagation fix — the one true landmine in this topic is handled correctly. Internals claims (2 KB stacks, contiguous-copying since 1.4, frame size, slices/math versions) all verify. Frontmatter unchanged: yes (report-only).
