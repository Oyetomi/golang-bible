# Accuracy audit — auditability-compliance

Chapter: `content/part-3/10-auditability-compliance.mdx`
Tier 2 (correctness-critical). REPORT ONLY — no content file was edited.

Focus areas: append-only audit logs / hash-chaining, SHA-256 properties, monotonic-vs-wall clock, crypto-shredding (AES-GCM, per-user key delete), PCI-DSS / AML retention windows.

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (not applied) |
|---|---|---|---|---|
| 1 | Hash chain: "each entry's hash covers all its content fields *plus* the previous entry's hash. If you change any content byte anywhere in the chain, you change that entry's hash. That invalidates every subsequent `PrevHash` pointer — the chain breaks and verification fails." | **CORRECT** | Standard hash-chain construction. The chapter's own runnable `computeHash`/`Verify` (lines 255-296) hashes `…|PrevHash` and re-derives forward, so altering entry N changes N's hash and breaks N+1's `PrevHash` check. Mechanism is sound. | None. |
| 2 | "SHA-256 … it is computationally infeasible (as of Go 1.26 and current cryptography) to find two different inputs that produce the same output. That property is called *collision resistance*." | **CORRECT** | Standard cryptographic property of SHA-256; the claim is already hedged ("as of Go 1.26 and current cryptography"). No known practical SHA-256 collision. | None. |
| 3 | "Go's `crypto/sha256` package (standard library since Go 1.0) is backed by an optimized assembly implementation on amd64 and arm64." | **CORRECT (UNVERIFIABLE on exact "since Go 1.0" wording, low risk)** | `crypto/sha256` has been in the stdlib since Go 1.0; asm paths for amd64/arm64 exist in the runtime source. Could not pin a single doc line stating "since Go 1.0" + "asm on amd64 and arm64" together. Substance is correct. | Optional: soften to "in the standard library" without the exact version, or cite the package source. Low risk. |
| 4 | "a hash chain is tamper-evident, not tamper-proof. An attacker with write access … could … recompute every subsequent hash." Defenses: append-only DB permissions, external witnesses, signed entries. | **CORRECT** | Correct distributed-systems / append-only-ledger framing; matches the canon that a bare hash chain only detects tampering unless an out-of-reach anchor (external witness / signature) exists. The chapter states this explicitly. | None. |
| 5 | Monotonic vs wall: "Go's `time.Time` (since Go 1.9) carries *both* readings … `time.Since` and `Sub` automatically use the monotonic component; serialization to a string or database discards it." | **CORRECT** | pkg.go.dev/time: "the Time returned by time.Now contains both a wall clock reading and a monotonic clock reading … later time-measuring operations, specifically comparisons and subtractions, use the monotonic clock reading." Go 1.9 is the documented introduction (release history). | None. |
| 6 | UnderTheHood: "`time.Sub`, `time.Since`, and comparison operators use the monotonic reading when both operands have one … strip with `.Round(0)`, you lose the monotonic reading." | **CORRECT** | pkg.go.dev/time: "If Times t and u both contain monotonic clock readings, the operations t.After(u), t.Before(u), t.Equal(u), t.Compare(u), and t.Sub(u) are carried out using the monotonic clock readings alone"; "The canonical way to strip a monotonic clock reading is to use t = t.Round(0)." | None. |
| 7 | UnderTheHood model struct: `type Time struct { wall uint64; ext int64; loc *Location }` with "top bit = has monotonic; lower bits = wall seconds or nsec" — labeled "simplified — this is a model, not the exact source". | **CORRECT (hedged model)** | Matches the actual `time.Time` field layout (`wall uint64`, `ext int64`, `loc *Location`) in the runtime source; the flag bit and dual encoding are real. Already labeled a simplified model. | None. |
| 8 | DST ambiguity: "November 7, 2027 at 1:30 AM in the US Eastern timezone happens *twice* — once at UTC-04:00 and once at UTC-05:00." | **CORRECT** | US DST "fall back" is the first Sunday of November; 2027-11-07 is the first Sunday. The 01:00–02:00 local hour repeats: first pass EDT (UTC-04:00), second pass EST (UTC-05:00). 01:30 occurs in both. Date and offsets are correct. | None. |
| 9 | Retention table + Define: "PCI-DSS (cardholder data audit logs: 12 months, 3 months online)"; "PCI-DSS Requirement 10 mandates audit logs … 12 months total, 3 months immediately accessible." | **CORRECT** | PCI DSS Req 10.5.1: retain audit log history "for at least 12 months, with the most recent three months immediately available for analysis." Matches the chapter. | None. |
| 10 | Retention: "AML/BSA (transaction records: 5 years)." | **CORRECT** | FFIEC BSA/AML Appendix P / 31 CFR 1010.430: "a bank must maintain most records for at least five years"; transaction records retained five years; SAR + supporting docs five years from filing. Matches. | None. |
| 11 | Crypto-shredding: "encrypting data with a per-subject key and then deleting the key to render the data permanently indecipherable … the encrypted blobs remain on disk but are now computationally indistinguishable from random noise." Already hedged: "accepted as equivalent to deletion under most interpretations of GDPR Article 17, but consult your data protection officer." | **CORRECT** | Crypto-shredding / crypto-erasure is a recognized technique (NIST SP 800-88 "Cryptographic Erase"). With a deleted AES-256 key and AES-GCM ciphertext, recovery is computationally infeasible. The legal-equivalence claim is appropriately hedged and routed to a DPO. | None. |
| 12 | Sequence numbers beat wall-clock for ordering: "Sequence numbers … are strictly monotonic … Timestamps from wall clocks are not: two events on different servers can have the same millisecond timestamp (or even a backward one, if NTP has been adjusting the clock)." | **CORRECT** | Standard distributed-systems result; NTP slew/step can move wall time backward, and cross-host clocks are not totally ordered. A DB sequence / Kafka offset is monotonic by construction. | None. |
| 13 | Crypto-shredding GoPlayground (Exercise 5): AES-256-GCM with random 12-byte nonce via `gcm.NonceSize()`, `gcm.Seal(nonce, nonce, …)` then split on `nonceSize`; `rand.Read(key)` for a 32-byte key. | **CORRECT** | pkg.go.dev/crypto/cipher: `NewGCM` returns the AEAD "with the standard nonce length"; AEAD.Seal "must be NonceSize() bytes long and unique for all time, for a given key." Code uses `crypto/rand` for both key and nonce. Sound. | None. (Compiles; correct AEAD usage. `rand.Read(key) //nolint:errcheck` ignores error but is harmless in a demo.) |
| 14 | `slog.LogValuer` is "the idiomatic Go 1.21+ hook for custom log representations." (Exercise 3 explanation) | **CORRECT** | `log/slog` and the `LogValuer` interface were added to the stdlib in Go 1.21. Version attribution is correct. | None. |
| 15 | "auto-incrementing sequence number" `ID int64` is used as the canonical ordering key, while the chain links by `PrevHash`. The Scene/ExecTimeline animations show the chain breaking forward from the tampered entry. | **CORRECT (animation matches mechanism)** | The `<Scene>` and `<ExecTimeline>` steps (entry 1 = no predecessor / empty PrevHash; each subsequent hash = SHA256(content + prev.Hash); tamper at N invalidates N and every forward link; verifier recomputes from scratch) match the runnable code's behavior exactly. | None. |

### CORRECT (verified, not individually flagged): 15

All 15 load-bearing falsifiable claims verified CORRECT (rows 3 carries a low-risk "since Go 1.0 / asm" softening note; row 7 is a hedged model). No WRONG or OUTDATED findings in this chapter. This chapter's conceptual-correctness claims (hash-chaining, monotonic vs wall, retention windows, crypto-shredding) all hold against fetched primary sources.

## Sources fetched
- https://pkg.go.dev/time (monotonic vs wall clock, Round(0), Equal/Before/After/Sub semantics)
- https://pkg.go.dev/crypto/cipher (NewGCM standard nonce length; AEAD nonce-uniqueness requirement)
- https://pkg.go.dev/crypto/rand (CSPRNG OS sources)
- WebSearch → PCI DSS Req 10.5.1 (12 months / 3 months online) and FFIEC BSA/AML Appendix P (5-year retention)

## Tally
Flagged: 0 hard errors. 15 verified CORRECT (1 low-risk version-softening suggestion on row 3, 1 hedged-model note on row 7). Worst finding: **none — no incorrect mechanical/numeric claim found. The only suggestion is cosmetic: soften "standard library since Go 1.0 … asm on amd64 and arm64" (row 3) unless a single citation is added; the substance is correct.**
