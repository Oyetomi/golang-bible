# Accuracy audit — batch-large-file-processing

Chapter: `content/part-3/11-batch-large-file-processing.mdx`
Tier 2 (correctness-critical). REPORT ONLY — no content file was edited.

Focus: NACHA/ACH fixed-width record + field positions, ISO 20022 (pain.001/pain.008) XML claims, `bufio.Scanner` token limits, streaming = O(1) memory, Go 1.26 stdlib/GC claims, checkpoint + idempotency. Plus the KNOWN DEFECT (NACHA-streaming GoPlayground imports `bufio` but never uses it).

## Audit log

| # | Claim (as written) | Verdict | Source (quoted) | Proposed fix (not applied) |
|---|---|---|---|---|
| 1 | **KNOWN DEFECT (confirmed):** the NACHA-streaming `<GoPlayground>` (lines 319-385) `import "bufio"` but never references `bufio` anywhere in `sumACHReturns`/`main` — it operates on a `[]string`. | **WRONG (won't compile)** | Reproduced exactly with `go build` (go1.24): `./main.go:4:2: "bufio" imported and not used`. Go treats an unused import as a hard compile error, so this Codapi block fails `go run`. | Remove the `"bufio"` import line from this block's import group. (The real `bufio.Scanner` usage is shown in the *separate* prose snippet at lines 391-409, which is not a runnable Codapi block, so `bufio` belongs only there.) |
| 2 | NACHA: "every line is exactly 94 characters, terminated by a newline, with no delimiters. Fields are identified by byte offset." | **CORRECT** | NACHA ACH file spec (Nacha dev guide / bank format specs): "The NACHA format is composed of 94 character records"; fixed-width positional, no delimiters. | None. |
| 3 | Record types: "`1` = File Header, `5` = Batch Header, `6` = Entry Detail, `7` = Addenda, `8` = Batch Control, `9` = File Control." | **CORRECT** | NACHA spec: Record Type Code in position 1; 1/5/6/7/8/9 = File Header / Batch Header / Entry Detail / Addenda / Batch Control / File Control. | None. |
| 4 | Entry Detail (type 6) field positions in the struct comment: TransactionCode pos 2-3 (2), RoutingNumber pos 4-12 (9), AccountNumber pos 13-29 (17), AmountCents pos 30-39 (10), IndividualName pos 55-76 (22), TraceNumber pos 80-94 (15). | **CORRECT** | treasurysoftware.com NACHA Entry Detail layout: Transaction Code 2-3; Receiving DFI ID 4-11 (8) + Check Digit 12 (→ 4-12 = the 9-digit routing); DFI Account Number 13-29 (17); **Amount 30-39 (10)**; Receiving Company/Individual Name 55-76 (22); **Trace Number 80-94 (15)**. All chapter positions match. | None. (Minor labeling note: pos 55-76 is officially "Receiving Company/Individual Name"; "individual name" is accurate for consumer/PPD entries. Pos 40-54 "Identification Number" is correctly not parsed.) |
| 5 | Parse code slices: routing `line[3:12]`, account `line[12:29]`, amount `line[29:39]`, name `line[54:76]`, trace `line[79:94]` (1-based→0-based). | **CORRECT** | Direct conversion of the verified 1-based positions in row 4: pos 4-12 → `[3:12]`, 13-29 → `[12:29]`, 30-39 → `[29:39]`, 55-76 → `[54:76]`, 80-94 → `[79:94]`. Off-by-one conversion is correct throughout. | None. |
| 6 | "Amount is in whole cents — never a float." (`AmountCents int64`, parsed with `strconv.ParseInt`). | **CORRECT** | NACHA Amount field is a 10-digit numeric in cents (implied 2 decimals). Integer-minor-unit handling is the correct money representation (landmine: money-as-float — chapter avoids it). | None. |
| 7 | ISO 20022: "The payment initiation message is **pain.001** … a direct debit is **pain.008**." | **CORRECT** | ISO 20022 message catalogue: pain.001 = CustomerCreditTransferInitiation; pain.008 = CustomerDirectDebitInitiation. Matches. | None. |
| 8 | "A pain.001 document has a `GrpHdr` (Group Header) … a sequence of `PmtInf` (Payment Information) blocks, each containing one or more `CdtTrfTxInf` … The amount in ISO 20022 is a decimal string with a currency code." | **CORRECT** | ISO 20022 pain.001 structure: GrpHdr + PmtInf blocks + CdtTrfTxInf transaction elements; instructed amount `InstdAmt` carries a `Ccy` currency attribute. Matches. | None. |
| 9 | Trace number is "your **natural idempotency key** … guaranteed unique per ACH file by the NACHA spec." | **CORRECT** | NACHA Trace Number (pos 80-94) is a 15-digit numeric unique within an ACH file (ODFI routing prefix + sequence). Using it as a per-file dedup key is sound. | None. (Caveat the chapter already implies: uniqueness is *per file*, so cross-file dedup needs file-id qualification — but the chapter scopes the claim to "per ACH file," which is correct.) |
| 10 | `bufio.Scanner` "has a default max token size of 64 KiB (`bufio.MaxScanTokenSize`)"; oversized line → `Scan()` returns false and `scanner.Err()` returns `bufio.ErrTooLong`; "does **not** truncate silently." | **CORRECT** | pkg.go.dev/bufio: `MaxScanTokenSize = 64 * 1024`; "Scanning stops unrecoverably at … a token too large to fit in the Scanner.Buffer"; `ErrTooLong = errors.New("bufio.Scanner: token too long")`. No silent truncation. | None. |
| 11 | ExecTimeline step: "`bufio.Scanner` allocates its internal read buffer (default 4 KiB, or your custom size). This is the ceiling for NACHA streaming." | **IMPRECISE** | pkg.go.dev/bufio: the documented *maximum* token size default is `MaxScanTokenSize` (64 KiB); the Scanner grows its buffer as needed up to that max. The starting buffer is small (the implementation begins at 4096 bytes) but 4 KiB is not a hard "ceiling" — the ceiling is 64 KiB. | Reword to: "starts around 4 KiB and grows up to the 64 KiB `MaxScanTokenSize` default." Calling 4 KiB "the ceiling" understates the real max. Low-severity. |
| 12 | "never load a batch file into memory … Memory usage stays flat … O(1) in file size." Streaming `Decoder.Token()`/`DecodeElement` holds "one decoded element … regardless of the total file size." | **CORRECT** | encoding/xml `Decoder` is a streaming token reader; `DecodeElement` materializes only the targeted element subtree. Live heap is bounded by buffer + one element, independent of file size — the O(1)-in-file-size argument holds. | None. |
| 13 | "As of **Go 1.26**, `io.ReadAll` is roughly 2× faster than it was in 1.25." | **CORRECT** | go.dev/doc/go1.26: "ReadAll now allocates less intermediate memory and returns a minimally sized final slice. It is often about two times faster while typically allocating around half as much total memory." Matches "roughly 2× faster." | None. |
| 14 | "Go 1.26's **Green Tea GC** (the new default garbage collector) improves locality and scalability when marking and scanning many small objects." | **CORRECT** | go.dev/doc/go1.26: "The Green Tea garbage collector, previously available as an experiment in Go 1.25, is now enabled by default after incorporating feedback." So "new default" is accurate for 1.26. (Note: this does NOT trip the "default GC is generational" landmine — Green Tea is the new default and the chapter does not call it generational.) | None. |
| 15 | UnderTheHood: "Go 1.26's Green Tea GC can reduce the cost of scanning many small objects, but it does not change the O(1) live-memory argument." | **CORRECT** | Consistent with go.dev/doc/go1.26 (10–40% GC-overhead reduction in heavy-GC programs) and basic streaming reasoning: collector throughput is orthogonal to live-heap bound. | None. |
| 16 | "As of **Go 1.26**, `slog.NewMultiHandler` (new in 1.26) lets you fan structured log output to multiple destinations." | **CORRECT** | go.dev/doc/go1.26: "The NewMultiHandler function creates a MultiHandler that invokes all the given Handlers." Confirmed new in 1.26. | None. |
| 17 | "Pair with `slog.GroupAttrs` (1.25) to attach job ID and checkpoint position to every log line." | **UNVERIFIABLE** | go.dev/doc/go1.26 release notes do NOT reference `slog.GroupAttrs`, and I could not fetch a go1.25 release-note line confirming it was added in 1.25. `slog.GroupAttrs` does exist in current `log/slog`, but the "(1.25)" version attribution is unconfirmed by a fetched source. | Either cite a go1.25 source for the version, or soften to "`slog.GroupAttrs`" without the "(1.25)" pin. Function existence is fine; only the version tag is unverified. |
| 18 | Checkpoint + idempotency: "Checkpoint without idempotency still double-posts the in-flight window. Idempotency without a checkpoint still re-processes the whole file … Together: you skip what's already committed, and safely re-run what isn't." Checkpoint must commit "in the **same transaction** as the ledger entries." | **CORRECT** | Standard at-least-once + idempotent-consumer / atomic-checkpoint canon. Committing checkpoint and ledger writes in one transaction makes the checkpoint a consistent snapshot; idempotent dedup (trace-number `ON CONFLICT DO NOTHING`) absorbs the replayed in-flight window. Sound. | None. |
| 19 | Idempotency SQL: `INSERT … ON CONFLICT (trace_number) DO NOTHING` (requires a UNIQUE constraint on `trace_number`); alternative pre-insert `SELECT EXISTS(...)` check. | **CORRECT** | Postgres `ON CONFLICT … DO NOTHING` performs atomic upsert-skip against a unique constraint; this is the canonical idempotent-insert pattern. The pre-check variant is correct but race-prone without the constraint — the chapter prefers the constraint, which is right. | None. |
| 20 | Lab starter `processFile` references `_ = bufio.NewScanner` (keeps the `bufio` import used) and the harness uses `strings.NewReader` "no real files." | **CORRECT** | Unlike the row-1 GoPlayground, the Lab starter explicitly references `bufio.NewScanner`, `strconv.ParseInt`, `strings.TrimSpace`, so its imports are all used — it compiles. No defect here. | None. |

### CORRECT (verified, not individually flagged): 16

Rows 2-10, 12-16, 18-20 verified CORRECT against fetched sources. Imprecise: row 11 (4 KiB "ceiling"). Unverifiable: row 17 (`slog.GroupAttrs` "1.25" version tag). Hard error: row 1 (unused-import compile failure).

## Sources fetched
- https://pkg.go.dev/bufio (MaxScanTokenSize = 64 KiB, ErrTooLong, stop-at-too-large-token)
- go.dev/doc/go1.26 (io.ReadAll ~2× faster; Green Tea GC now default; slog.NewMultiHandler new in 1.26; slog.GroupAttrs not referenced)
- https://www.treasurysoftware.com/ach/how-do-i-create-an-ach-nacha-file-detailed-file-format.aspx (Entry Detail field-position table)
- WebSearch → NACHA 94-char records + record types (Nicolet/Chase/Regions bank specs) and ISO 20022 pain.001/pain.008 message names + GrpHdr/PmtInf/CdtTrfTxInf/InstdAmt/Ccy structure
- Local `go build` (go1.24) reproducing the unused-`bufio`-import compile error

## Tally
Flagged: 3 (1 WRONG/won't-compile, 1 IMPRECISE, 1 UNVERIFIABLE) + 16 verified CORRECT. Worst finding: **CONFIRMED — the NACHA-streaming `<GoPlayground>` (lines 319-385) imports `"bufio"` but never uses it; `go build` fails with `"bufio" imported and not used`, so the Codapi `go run` does not compile. Fix: delete the `"bufio"` import from that block (its real `bufio.Scanner` usage lives only in the adjacent non-runnable prose snippet).**
