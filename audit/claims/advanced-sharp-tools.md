# Accuracy audit — advanced-sharp-tools

Chapter: `content/part-1/08-advanced-sharp-tools.mdx`
Mode: REPORT ONLY (no edits applied). Audited every falsifiable claim: the reflect laws + interface-header representation, struct tags, the unsafe.Pointer conversion rules + uintptr/GC semantics, struct padding numbers, the zero-copy []byte→string trick, the pprof toolchain, and the Go 1.25/1.26 feature attributions (reflect.TypeAssert, reflect iterators, pprof flame-graph default, runtime/secret).

Strong, careful chapter. The reflect Three Laws (Rob Pike), the interface two-word header, struct-tag mechanics, the unsafe.Pointer/uintptr/GC distinction, and the unsafe.String (Go 1.20) zero-copy pattern are all correct. Every version-attributed feature checks out: `reflect.TypeAssert` (1.25), reflect iterator methods (1.26), pprof flame-graph default (1.26), `runtime/secret` experiment (1.26). The struct-padding arithmetic is internally consistent (would benefit from a recompile to fully certify, noted below). **One real defect:** the `unsafeptr` vet analysis is described as "introduced in Go 1.22," which is wrong — vet's unsafeptr check long predates 1.22. A couple of UNVERIFIABLE-but-hedged perf figures. No other WRONG mechanical findings.

## Audit log

| # | Claim (as written) | Verdict | Source (fetched) | Proposed fix (NOT applied) |
|---|---|---|---|---|
| 1 | "The Reflect Laws … Rob Pike wrote them. **Law 1:** interface value → reflection object. **Law 2:** reflection object → interface value. **Law 3:** To modify a reflection object, the value must be settable (addressable)." | CORRECT | go.dev/blog/laws-of-reflection (Rob Pike, 2011): "1. Reflection goes from interface value to reflection object. 2. Reflection goes from reflection object to interface value. 3. To modify a reflection object, the value must be settable." The chapter's "(addressable)" gloss on Law 3 is accurate — settability requires the Value be addressable. | none |
| 2 | "`reflect.Type` describes the *shape* … `reflect.Value` holds an actual *value* … You get them from a concrete value via `reflect.TypeOf` and `reflect.ValueOf`." | CORRECT | pkg.go.dev/reflect: TypeOf returns a Type; ValueOf returns a Value holding the concrete value. | none |
| 3 | "`reflect.ValueOf` … Returns a reflect.Value holding a *copy* of p — not a pointer to it. You can read fields but not set them yet." | CORRECT | pkg.go.dev/reflect: a Value from ValueOf of a non-pointer is not addressable → CanSet() is false; settability needs `reflect.ValueOf(&p).Elem()`. | none |
| 4 | "Unexported fields exist but are inaccessible from outside the package." / `IsExported()` guards; `CanInterface()` would panic on unexported | CORRECT | pkg.go.dev/reflect: StructField.IsExported(); Value.Interface() panics if "obtained by accessing unexported struct fields." | none |
| 5 | "Struct tags … live in the type's metadata, readable only at runtime via reflection. The format is a `key:"value"` pair … `reflect.StructTag.Get("json")` returns `"amount_cents"`." | CORRECT | pkg.go.dev/reflect, StructTag: "By convention, tag strings are a concatenation of optionally space-separated key:\"value\" pairs." Get/Lookup parse exactly this. | none |
| 6 | Callout: malformed tags (`key=value`, missing quotes) "silently returns an empty string — `go vet` can catch malformed tags." | CORRECT | pkg.go.dev/reflect StructTag.Get returns "" on malformed/absent; `go vet`'s `structtag` analyzer flags malformed struct tags. | none |
| 7 | structToMap: dereference pointer with Elem(); skip unexported via IsExported(); honor `json:"-"`; `rv.Field(i).Interface()` boxes back to any | CORRECT | pkg.go.dev/reflect: Type.Elem()/Value.Elem() peel a pointer; the `-` tag = omit (encoding/json convention); Value.Interface() re-boxes. | none |
| 8 | UnderTheHood: "Every Go interface value is a **two-word header**: type pointer (itab/type) + data pointer." | CORRECT | go.dev/blog/laws-of-reflection + runtime: a non-empty interface is `(*itab, data)`, an empty interface is `(*_type, data)` — two words. | none |
| 9 | "Type metadata is always there … it's what makes type assertions (`x.(T)`) and type switches work." / "Reflection doesn't add runtime overhead at import time — it uses metadata the compiler already generates." | CORRECT | go.dev/blog/laws-of-reflection: reflection reads the type descriptor the compiler already emits; assertions/switches use the same descriptors. | none |
| 10 | "`reflect.Value.Field(i).Interface()` is dozens [of instructions] … the reflect package has been progressively optimized (as of Go 1.26), but it will never match a direct field access." | CORRECT (UNVERIFIABLE exact "dozens", hedged) | Reflection goes through interface dispatch + bounds checks the compiler can't inline; qualitatively correct. "Dozens" is illustrative, hedged with "as of Go 1.26." | none — fair. |
| 11 | "`encoding/json` … traverses your struct once at first use, caches the result in a `sync.Map`, and replays the cached plan." | CORRECT | src/encoding/json: `encoder` caches per-type encoders in a `sync.Map` (`encoderCache`); first encode builds the plan, later encodes reuse it. | none |
| 12 | Go 1.26 callout: reflect iterator methods `Type.Fields()`, `Value.Fields()`, `Type.Methods()`, `Value.Methods()` returning rangeable iterators | CORRECT | go.dev/doc/go1.26: "The new methods `Type.Fields`, `Type.Methods`, `Type.Ins` and `Type.Outs` return iterators … Similarly, the new methods `Value.Fields` and `Value.Methods` return iterators over a value's fields or methods." Chapter lists a correct subset of the four it names. | none — correctly pinned to 1.26. |
| 13 | "`sf.Index[0]` is the field's position … a `[]int` to support embedded fields" | CORRECT | pkg.go.dev/reflect, StructField.Index: "[]int — index sequence for Type.FieldByIndex." Top-level field → single-element slice. | none |
| 14 | Go 1.25 callout: "`reflect.TypeAssert[T](v reflect.Value) (T, bool)` … performs the same conversion as `v.Interface().(T)` while avoiding unnecessary allocations." | CORRECT | go.dev/doc/go1.25: "The new `TypeAssert` function permits converting a `Value` directly to a Go value of the given type … like using a type assertion on the result of `Value.Interface`, but avoids unnecessary memory allocations." | none — correctly pinned to 1.25. |
| 15 | "Profiling … a **CPU profile** samples the call stack ~100 times per second … a **heap profile** records allocation call sites." | CORRECT | go.dev runtime/pprof + pprof docs: CPU profiler default rate is 100 Hz (SIGPROF); heap profile samples allocations. | none |
| 16 | ExecTimeline: "installs a signal handler (SIGPROF on Unix) that fires ~100 times/second"; "Every 10ms, the runtime interrupts execution, captures the current goroutine's call stack." | CORRECT | runtime: CPU profiling uses SIGPROF at 100 Hz (≈10 ms). Statistical sampling. | none |
| 17 | "cpu.prof … a gzip'd protobuf"; pprof reads it and gives `top`, `list`, `web` | CORRECT | pprof format is gzip-compressed protobuf; `go tool pprof` provides top/list/web/peek interactive commands. | none |
| 18 | "Import `net/http/pprof` for its side effects … registers /debug/pprof/* handlers on DefaultServeMux"; endpoints `/debug/pprof/`, `/profile?seconds=`, `/heap`, `/goroutine` | CORRECT | pkg.go.dev/net/http/pprof: blank import's init() registers `/debug/pprof/` plus profile/heap/goroutine/etc. on DefaultServeMux. | none |
| 19 | "`top10` shows: `flat` (time in the function, not callees), `cum` (cumulative)." | CORRECT | go.dev pprof docs: flat = self time; cum = function + callees. | none |
| 20 | Go 1.26 callout: "`go tool pprof -http=:6060 cpu.prof` opens with the **flame graph view by default**." | CORRECT | go.dev/doc/go1.26: "The `pprof` tool web UI, enabled with the `-http` flag, now defaults to the flame graph view." | none — correctly pinned to 1.26. |
| 21 | "import `unsafe` … doesn't even appear in your `go.mod`, because it's part of the language itself." | CORRECT | go.dev/ref/spec, Package unsafe: built into the toolchain; the compiler knows it specially; not a fetched dependency. | none |
| 22 | "The garbage collector *does* trace `unsafe.Pointer` as a live reference … but it provides zero type-safety guarantees. The Go spec defines six legal conversion patterns." | CORRECT | pkg.go.dev/unsafe (Pointer doc): the GC treats unsafe.Pointer as a real pointer (keeps the referent alive); the doc enumerates exactly **six** valid Pointer-conversion patterns. | none |
| 23 | "`go vet` enforces these rules via the `go/analysis/passes/unsafeptr` pass (**introduced in Go 1.22**, still active and expanded in Go 1.26)." AND "go vet catches the most obvious violations (since Go 1.22…)" AND UnderTheHood "go vet enforces them (since Go 1.22)." | WRONG (version) | go.dev/doc/go1.22 (fetched): no mention of unsafeptr. The `unsafeptr` vet check long predates Go 1.22 — `go/analysis/passes/unsafeptr` and vet's unsafe.Pointer check have existed for many releases (vet has flagged invalid uintptr→unsafe.Pointer conversions since well before 1.22). The "introduced in Go 1.22" attribution is incorrect and is repeated three times. | Drop "introduced/since Go 1.22"; the unsafeptr analyzer is long-standing. Replace with "go vet enforces these rules via the `unsafeptr` analysis pass (long-standing)." Flag for human check. |
| 24 | "`uintptr` is an unsigned integer wide enough to hold an address — but it is **not** a pointer. The garbage collector does not trace through `uintptr`." | CORRECT | go.dev/ref/spec, Numeric types: "uintptr — an unsigned integer large enough to store the uninterpreted bits of a pointer value." pkg.go.dev/unsafe: uintptr is not a reference; GC may collect/move the referent. | none |
| 25 | "The only safe use of `uintptr` in an `unsafe.Pointer` chain is as part of a single expression — never stored to a variable across a GC-safe point." | CORRECT | pkg.go.dev/unsafe (Pointer doc, pattern (3)): "the conversion must appear … in the same expression … only the addition of an offset to a pointer … there is no way to use it safely if it is split across statements." | none |
| 26 | UnderTheHood: "Go's GC is a **concurrent tri-color mark-sweep collector** (non-moving through Go 1.26, though the conversion rules are written to be safe even against a future moving collector)." | CORRECT | go.dev/doc/gc-guide: concurrent tri-color mark-sweep; the runtime GC is non-moving as of 1.26 (Green Tea, default in 1.26, is still a non-moving mark-sweep). pkg.go.dev/unsafe rules are explicitly future-moving-GC-safe. Avoids the "generational" landmine. | none — correctly avoids calling it generational/moving. |
| 27 | float32→uint32 reinterpret via `*(*uint32)(unsafe.Pointer(&f))`; "Pattern: *T1 → unsafe.Pointer → *T2 … Legal only when T1 and T2 share the same memory layout"; f=1.5 → 0x3FC00000 | CORRECT | pkg.go.dev/unsafe Pointer pattern (1): "*T1 → Pointer → *T2" when layouts match. IEEE 754: 1.5 = sign 0, exp 127 (0x7F), mantissa 0x400000 → 0x3FC00000. Reproducible. | none |
| 28 | "Normally, `string(b)` … allocates a new backing array and copies every byte." | CORRECT | go.dev/ref/spec, Conversions to/from string: `[]byte`→string conversion copies the bytes (compiler may elide only in proven non-escaping cases). | none |
| 29 | "`unsafe.String(&b[0], len(b))` … Go 1.20+ ships unsafe.String and unsafe.SliceData for exactly this pattern. Prefer them over the older `*(*string)(unsafe.Pointer(&b))` hack." | CORRECT | go.dev/doc/go1.20: "The unsafe package defines three new functions SliceData, String, and StringData." pkg.go.dev/unsafe: `String(ptr *byte, len IntegerType) string`. | none — correctly pinned to 1.20. |
| 30 | "the danger: mutating `buf[0]='X'` changes the zero-copy string s" → "s is now 'Xayment_completed'" | CORRECT | unsafe.String shares the backing array; the string is normally immutable but the underlying bytes are aliased, so mutating buf mutates s — exactly the documented hazard. Reproducible. | none |
| 31 | Stdlib uses unsafe: "`strings.Builder` … convert its internal `[]byte` to `string` without copying"; "`sync.Map` … atomic pointer ops"; "`reflect` … reach into memory at computed offsets" | CORRECT | src: strings.Builder.String() uses `unsafe.String` for zero-copy; sync/reflect use unsafe.Pointer internally. | none |
| 32 | Go 1.26 callout: "`runtime/secret` … experimental package for securely erasing cryptographic temporaries … guaranteed not to be optimized away … like C's `explicit_bzero` or Rust's `zeroize`. Experimental in 1.26." | CORRECT | go.dev/doc/go1.26: "The new `runtime/secret` package is available as an experiment … enabled by setting `GOEXPERIMENT=runtimesecret` … provides a facility for securely erasing temporaries used in code that manipulates secret information." | none — correctly pinned + correctly labeled experimental. |
| 33 | unsafe layout: AccountBad {bool, int64, byte} = **24** (1+7pad+8+1+7pad); AccountGood {int64, byte, bool} = **16** (8+1+1+6pad); "33% smaller"; Offsetof Balance 0/Code 8/Active 9 | CORRECT (consistent; recompile recommended) | go.dev/ref/spec alignment rules + pkg.go.dev/unsafe Sizeof/Offsetof/Alignof: int64 has 8-byte alignment on amd64/arm64; bool/byte size 1. The 24 vs 16 layout and offsets follow exactly from declaration-order + max-alignment padding. 24→16 = 33.3% reduction. (Empirical recompile not run in this pass; arithmetic is sound and matches the data-semantics chapter's verified numbers for analogous structs.) | none — verify by recompiling on amd64 if a hard check is desired. |
| 34 | "Sizeof/Offsetof/Alignof are **compile-time constants** — the compiler computes them." | CORRECT | pkg.go.dev/unsafe: "The return value of Sizeof/Offsetof/Alignof is a Go constant" (when the type has no variable size). | none |
| 35 | Lab/Exercise: `sync.Pool{New: func() any { return &bytes.Buffer{} }}`; copying `raw` into `out` before `Put` so the returned string's backing memory is independent of the pooled buffer | CORRECT | pkg.go.dev/sync, Pool: Get returns New() when empty; a buffer returned to the pool may be Reset/reused, so the zero-copy string must own independent memory — the explicit copy is correct. | none |
| 36 | Exercise-4 Solution comment: "unsafe.Offsetof requires a field selector expression, not an index. We use reflect to get the offset instead — `reflect.StructField.Offset`." | CORRECT | pkg.go.dev/unsafe Offsetof takes a selector `x.f`, not an index; pkg.go.dev/reflect StructField.Offset gives the runtime offset. Accurate. | none |

## Sources fetched
- https://go.dev/blog/laws-of-reflection (Three Laws + author Rob Pike; interface two-word header)
- https://go.dev/doc/go1.26 (reflect iterator methods; pprof flame-graph default; runtime/secret experiment)
- https://go.dev/doc/go1.25 (reflect.TypeAssert)
- https://go.dev/doc/go1.20 (unsafe.String / SliceData / StringData)
- https://go.dev/doc/go1.22 (used to REFUTE the "unsafeptr introduced in 1.22" claim — no such entry)
- pkg.go.dev/unsafe (six conversion patterns, uintptr rules, Sizeof/Offsetof const) + go.dev/ref/spec (uintptr definition, string conversion copies) cross-referenced
- go.dev/doc/gc-guide (concurrent tri-color non-generational mark-sweep)

## Tally
- Claims logged: 36
- CORRECT: 34 (reflect laws + header, struct tags, unsafe.Pointer/uintptr/GC semantics, six-pattern rule, unsafe.String 1.20, padding arithmetic, pprof toolchain, and every 1.20/1.25/1.26 version pin)
- Flagged rows: 1 WRONG-version (#23, the "unsafeptr introduced in Go 1.22" misattribution, repeated 3×) + 1 recompile-recommended note (#33 padding, arithmetic sound) + UNVERIFIABLE-but-hedged perf note (#10).
- WRONG: 1 (#23).
- Worst finding: **#23** — the chapter states three times that go vet's `unsafeptr` analysis pass was "introduced in Go 1.22." That is incorrect: the `go/analysis/passes/unsafeptr` analyzer and vet's invalid-uintptr→unsafe.Pointer check long predate Go 1.22 (it is one of vet's older checks), and the Go 1.22 release notes contain no such entry. The surrounding guidance ("always run go vet; it enforces the conversion rules") is correct and valuable — only the version attribution is fabricated. Recommend striking "introduced in / since Go 1.22" in all three places. (Note: the chapter correctly avoids the GC landmines — it explicitly calls the GC non-generational, non-moving-through-1.26, and frames the conversion rules as future-moving-GC-safe.)

Frontmatter unchanged: yes (no edits made).
